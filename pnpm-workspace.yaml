packages:
  - 'packages/*'

# Vite depends on esbuild, whose install script fetches the platform binary.
allowBuilds:
  electron: true
  electron-builder: true
  electron-winstaller: true
  esbuild: true
  # lefthook 은 설치 스크립트가 git 훅 러너 바이너리를 링크한다.
  lefthook: true
