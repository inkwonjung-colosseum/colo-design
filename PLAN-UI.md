# PLAN-UI — 비개발자의 화면: 말하기 · 찍기 · 제출

PLAN.md 가 사이클(보관 · 제출 · 반영됨)의 **데몬 쪽**을 적었다면, 이 문서는 그 위에
얹히는 **화면**을 적는다. 기준은 눌러 볼 수 있는 목업 `mockups/redesign.html` 이다 —
문장으로 다투지 말고 목업을 열어 본다. 결정에는 `U` 번호를 단다(`A`~`F` · `L` 은 이미
쓰인다).

줄 번호는 2026-09-25 의 `lifecycle` 브랜치 기준이다.

## 0. 이 판의 한 문장

Claude Desktop 의 「대화 + 아티팩트」 배치를 그대로 빌리되 아티팩트 자리에 서비스의
실제 화면을 놓고, 그 위에 **상태 줄 하나와 `제출` 하나**만 얹는다. Claude 를 써 본
사람이 새로 배울 것은 셋뿐이다 — 상태 줄, 찍기, 작업 기록.

### 0.1 약속

| # | 사용자에게 하는 약속 | 지키는 결정 |
| --- | --- | --- |
| J1 | 화면이 결과물이다. 답마다 `고친 화면` 카드가 서고, 누르면 미리보기가 그리로 간다 | U5 |
| J2 | 핀은 첨부다. 적는 곳은 핀 옆, 보내는 곳은 입력창 하나 | U4 |
| J3 | 상태는 한 줄의 여정이다. `제출 전 → 개발자 확인 → 반영됨`, 누르면 `이번 작업` | U2 |
| J4 | 제출은 무엇이 가는지 보고 누른다. 확인 한 장, Enter 로 끝 | U3 |
| J5 | 개발자 말이 화면에 0 이다 — 턴 · 경로 · git · 데몬은 개발자용 폴드 안에만 | U10 |
| J6 | 도구가 스스로 한 일(코멘트 반영 · 준비 · 업데이트)은 카드 한 장으로 알리고 할 일을 만들지 않는다 | U8 · U12 · U13 · U14 |

### 0.2 사용자 가정 (PLAN.md 0.2 그대로)

Claude Desktop 을 써 본 사람. 모델 · 생각 시간 · 사용량 · `@` · 메시지 고쳐 보내기를
안다. git · GitHub · 터미널 · 레포 구조는 모른다.

### 0.3 콜드 리뷰 (2026-09-25)

맥락 없는 검토자가 기획자 역할로 목업의 아홉 과제를 수행했다. 막힌 순간 15개 중
설계 결함 12개는 목업에 반영했고(어휘 충돌 · 제출 확인 · 찍기 띠의 밀림 · 핀 빼기 ·
되돌리기 문구 · 고쳐서 다시 보내기 · 다른 프로젝트 줄 · 초대 파일 지우기 · 개발자
말 · 화면 확인 카드 · 알림 · 토스트), 셋은 목업의 가짜 데이터 탓이었다. 검토자가
좋았다고 한 셋 — 말하면 화면이 바뀌는 것, 찍기, 누를 것이 제출 하나 — 이 이 판의
뼈대다.

### 0.4 결정된 것

| 물음 | 결정 | 근거 |
| --- | --- | --- |
| 개발자 코멘트를 AI 가 바로 반영할지 | **바로 반영**(PLAN L9 그대로). 카드에 `답하기`, 되돌리기 문구가 「코멘트 반영 포함」 을 말한다 | 흔한 코멘트는 개발자 몫의 요구라 동의 버튼은 의미 없는 클릭이 된다 |
| 제출 전 확인 창 | **둔다**. 보낼 화면 목록 + 개발자에게 한마디(선택) → `제출`, Enter 로 보냄 | 제출은 유일하게 밖으로 나가는 동작. 검토자가 무엇이 가는지 몰라 멈칫했다 |
| 핀 메모 방식 | **핀 옆 말풍선**. 상태의 주인은 입력창 칩 | 시선이 있는 자리에서 적고, 여러 핀을 한 문장으로 보내는 길은 그대로 |
| 다크 모드 · 프로젝트가 많을 때의 사이드바 · 키보드만으로 쓰기 | 이 판에서 다루지 않음 | 2026-09-25 결정 |

### 0.5 용어

| 말 | 뜻 |
| --- | --- |
| **상태 줄** | 대화와 미리보기 위에 걸친 한 줄. 대화 제목 · 여정 세 점 · `제출` |
| **여정** | `제출 전 ─ 개발자 확인 ─ 반영됨` 세 점. `만드는 중` 은 앞에 잠시 붙는 표식 |
| **이번 작업** | 여정을 누르면 뜨는 팝오버. 바뀐 화면 · 제출 기록 · 코멘트 장부 |
| **고친 화면 카드** | AI 답 아래의 카드. 그 답이 만진 화면의 제목 |
| **말풍선** | 핀을 찍은 자리에 뜨는 메모 입력. 입력창 칩의 원격 조작기 |
| **다른 프로젝트 줄** | 사이드바 전환기 아래, 활성이 아닌 프로젝트마다 한 줄 |
| **제출** · **AI 답** | 개발자에게 가는 일은 전부 `제출`(제출했어요 · 제출하지 못했어요). AI 가 답하는 일은 `보내기` · `AI가 답을 못 했어요`. 두 뜻에 같은 말을 쓰지 않는다 |

## 1. 진단 — 지금 웹의 빈틈

모두 코드에서 확인한 것이다.

| 영역 | 빈틈 | 자리 | 사용자가 겪는 것 |
| --- | --- | --- | --- |
| 구조 | 홈 · 대화가 `PageWorkspace` 의 `useState<"home" \| "thread">` 하나에 걸려 있고, `ScreenPanel` 이 상태 칩 · 코멘트 창 · 동결 캡처 · 화면 확인 · 승인까지 한 파일에 든다 | PageWorkspace.tsx:152 · ScreenPanel.tsx(1,400여 줄) | 어느 면을 고쳐도 다른 면이 흔들린다 |
| 상태 | `제출 전` 이라는 값이 선로에 없다 — 웹이 `deriveDelivery` 로 만든다. 제출 실패는 재시도 예산 뒤 알림 하나로만 나온다 | delivery.ts:116 · cycle-supervisor.ts:1585 · budgets.ts:61 | 제출이 막혀도 상태 줄은 `제출 전` 그대로, 이유가 없다 |
| 제출 | `repo.submit` 에 실리는 것이 `sessionId` 뿐. 사용자 메모 자리가 없다 | messages.ts:511 · repo-publish.ts:543 | 개발자에게 한마디를 남길 길이 없다 |
| 핀 | 핀 상태가 프로젝트별 sessionStorage 에 있고, `pinsSync` 가 `tone` · `n` · `screenMark` 를 채우지 않는다 | usePins.ts:70, 113 · preview.ts:81 | 보낸 핀의 회색 배지가 안 선다 |
| 화면 | `고친 화면` 판정(`lastTurnScreens`)은 있으나 ScreenPanel 의 이동에만 쓰이고 답에는 서지 않는다 | turn-screens.ts:85 · ScreenPanel.tsx:866 | 답과 화면이 잇닿지 않는다 |
| 입력창 | 빠르게 토글이 없다(`session.setFastMode` 는 선로에 있다). 작성 이름 입력칸이 설정에 없다 | ChatColumn.tsx:725-799 · SettingsDialog.tsx | README 가 약속한 둘이 화면에 없다 |
| 프로젝트 | 다른 프로젝트의 진행은 행의 배지 하나뿐 | Sidebar.tsx:1401 | 준비 중 · 답을 기다림 · 코멘트가 구분되지 않는다 |
| 준비 | 처음 여는 프로젝트의 준비에 숫자 진행이 없고, 준비 중에 보낸 말을 줄 세우는 카드가 없다 | repo.ts:131-133 · session.ts:140 | 몇 분 동안 무엇을 해야 할지 모른다 |
| 초대 | 다시 받기의 행이 `add` · `update` 뿐, `keep` 이 없다. 가져온 뒤 파일을 지우라는 안내가 스쳐 지나간다 | invite.ts:418 · invite-import.ts:117 | 무엇이 그대로인지, 파일을 어떻게 지우는지 모른다 |
| 업데이트 | 앱만 확인한다. 에이전트는 현재 버전만 보고한다(`providers[].version`) | update.ts:105 · project.ts:296 | Claude Code · Codex 업데이트에 터미널이 필요하다 |
| 어휘 | `보내다` 가 AI 메시지와 제출에 함께 쓰인다. 턴 · 경로 · git · 데몬이 사용자 면에 남아 있다 | (전 표면) | 제출 직후 `보내지 못했어요` 가 제출 실패로 읽힌다 |

## 2. 결정

### U1. 배치 — 사이드바 · 상태 줄 · 대화 · 미리보기

Claude Desktop 과 같은 자리다. 사이드바(264px, 접힘) · 대화(360~420px) · 미리보기(나머지).
상태 줄이 대화와 미리보기 위에 걸친다. 창이 900px 아래로 좁아지면 사이드바는 `≡` 뒤로,
대화와 미리보기는 탭이 된다(U16). 사이드바 구성은 위에서부터 `새 대화 · 홈 · 찾기`,
프로젝트 전환기, **다른 프로젝트 줄**, 대화 목록, 접힌 `도구가 한 일`, 바닥에 설정.

살아남는 것: `useDaemon`(daemon-client.ts:1064) · `useSessions` · `usePins` 의 상태와
API, `Transcript` 의 블록 렌더링, `Onboarding` 의 게이트 로직, `SettingsDialog` 의
알림 · 업데이트 · 개발자용 조각, `Palette`, `Splitter`, `HomeInbox` 의 `buildHomeFeed`.

### U2. 상태 줄과 `이번 작업`

여정 세 점은 `deriveDelivery`(delivery.ts:116) 의 값에서 나온다 — 이 함수를 순수
`deriveJourney(repo, diffStatus, handoff, submitState)` 로 넓힌다. 세 점의 글자:

| 사이클 | 첫 점 | 둘째 점 | 셋째 점 |
| --- | --- | --- | --- |
| 제출 전 | `제출 전 · 화면 N개` (막힘: `제출 전 · 제출하지 못했어요`) | `개발자 확인` | `반영됨` |
| 개발자 확인 | `제출됨` | `개발자가 보고 있어요 · 코멘트 N` | `반영됨` |
| 반영됨 | `제출됨` | `확인됨` | `반영됐어요` |

`만드는 중 · 12초` 는 `session.state.startedAt` 으로 앞에 붙는다. 여정을 누르면
`이번 작업` 팝오버 — 바뀐 화면(제목 · 한 줄 · 시각), 제출(시각 · 작성자 · 받은 개발자 ·
`제출한 내용 열기` · 제출 기록 세 줄), 개발자 코멘트(사람 · 글 · 반영 상태), `작업 기록
열기`. 지금의 상태 칩 팝오버(ScreenPanel.tsx:1140-1186) · `DiffPanel` · `HandoffCard` 의
영수증 · 코멘트 장부는 여기로 흡수된다.

**바뀐 화면의 출처.** 데몬이 이미 커밋마다 화면을 관찰한다(`screen-map.jsonl`,
README「도구가 기계에 남기는 것」). `RepoStatus.cycleScreens?: Array<{ route, title,
note, at }>` 로 이번 사이클(베이스 이후 커밋)의 화면을 내려 준다 — `note` 는 그 커밋
제목(=사용자의 말 첫 줄). 웹은 파생하지 않는다. → 열린 항목 P2.

### U3. 제출은 확인 한 장

`제출` 을 누르면 잠긴 이유가 있으면 이유 한 줄(지금의 `showWhy` 자리), 아니면 팝오버 —
`개발자에게 제출할까요?` · 보낼 화면 목록(U2 의 `cycleScreens`) · `개발자에게 한마디
(선택)` · 받을 개발자 · `그만두기` · `제출`. Enter 로 보낸다. 이미 열린 요청이 있으면
제목이 `같은 요청에 더해 제출할까요?` 이고 목록은 보낸 뒤 바뀐 곳이다.

선로: `repo.submit { sessionId?, note? }`. 데몬은 `note` 를 `handoffToolBlock`
(repo-publish.ts:543) 의 작성자 줄 뒤에 `> 한마디: …` 로 넣고, 기존 PR 에는
`mergeToolBlock` 이 갱신한다. 영수증 카드(`cycle.handed`)에 `note` 를 되돌려 준다 —
`ChatEvent cycle.handed { pr, reviewer?, note? }`.

버튼은 스스로 답한다 — `제출하는 중…` → `제출됐어요`(2초). 채팅으로 제출(L6)은 확인
없이 그대로다(사용자가 이미 분명히 말했다).

### U4. 핀은 첨부, 말풍선은 원격 조작기

핀의 상태는 `usePins` 가 쥔다(지금대로). 찍는 순간 웹이 **앱 쪽 레이어**에 말풍선을
띄운다 — 오버레이가 보고하는 `rect`(preview.ts:30)와 웹뷰의 위치 · 배율로 좌표를
잡고, 스크롤 · 이동 · 배율 변경에 닫는다(`preview.onLocation` · `onZoom`). 게스트 안에
입력창을 두지 않는다 — 한글 입력과 포커스가 웹뷰로 넘어가지 않게.

말풍선: 번호 · 요소 이름 · 화면 이름 · 입력 한 줄 · `담기`(↵) · `지금 보내기`(⌘↵) ·
휴지통(이 핀 빼기). 적은 글은 입력창의 같은 번호 칩에 그대로 비친다. 보내면 핀은
`sent` 색으로 남았다가 턴이 끝나면 사라진다 — `pinsSync` 에 `tone` · `n` 을 채운다
(usePins.ts:70). 찍기 켜짐 안내는 미리보기 아래쪽에 **떠 있는 알약**이다 — 화면을 밀지
않는다(콜드 리뷰 3번).

### U5. 답마다 `고친 화면` 카드

`lastTurnScreens`(turn-screens.ts:85) 가 답의 `[제목](주소)` 를 읽는다. 그 결과를
답 블록 아래 카드로 그린다 — 썸네일 자리 · `고친 화면` · 제목 · `미리보기에서 보기`.
누르면 `preview.navigate`. 턴 끝의 자동 이동(README「미리보기에서 확인」)은 그대로다.
같은 화면을 잇달아 고치면 카드가 반복되지만, 그것이 「이 답이 무엇을 만졌는가」 의
정직한 표시라 접지 않는다.

### U6. 홈 = 큰 입력창 + 받은 편지함

`buildHomeFeed`(home-feed.ts:111) 를 그대로 쓴다. 위는 `<이름>님, 무엇을 만들까요?`
와 입력창(프로젝트 칩 · 첨부 · 모델 칩). 보내면 `session.create` → `session.send` 로
새 대화가 열리고 작업 화면으로 넘어간다. 아래는 `답을 기다려요`(확인 카드, 홈에서
바로 답함 — 지금의 `permission.respond` · `question.respond`), `지금 진행 중`(도는
턴 + **준비 중인 프로젝트**), `방금 있던 일`. 홈 배지는 확인 요청만 센다 — 「내 손이
필요한 일」 의 수. 프로젝트 칩은 활성 프로젝트를 따라간다.

### U7. 프로젝트 전환기와 다른 프로젝트 줄

사이드바 머리는 드롭다운 전환기다(프로젝트마다 상태 점 · 기다리는 숫자 · `처음 열 때
준비해요`). 그 아래 **다른 프로젝트 줄**: 활성이 아닌 프로젝트 전부, 한 줄에 이름과
가장 급한 것 하나 — 우선순위 `준비 중 > 만드는 중 > 답을 기다려요 N > AI가 답을 못
했어요 > 코멘트 N > 반영됐어요 > 사이클 상태`. 전부 `ProjectSummary`(project.ts:39)
에서 나온다 — `phase` · `working` · `pendingCount` · `handoff` · `lastEventKind`.
순수 `projectNote(summary)`.

### U8. 처음 여는 프로젝트

등록만 된 프로젝트를 고르면 미리보기 칸이 준비 화면이다 — `내려받기 · 설치하기 ·
미리보기 켜기` 세 단계(`RepoStatus.phase` cloning/installing/starting)와 진행 막대,
`준비되는 동안 먼저 말해 두셔도 돼요`. 준비 중에 보낸 말은 데몬의 대기 줄
(`queued`, session.ts:140)에 서고 대화록에 `준비가 끝나면 바로 보낼게요` 로 보인다.
준비는 프로젝트마다 따로 돌아 다른 프로젝트로 갔다 와도 이어지고(지금의
`project.changed`), 배경에서 끝나면 OS 알림 `준비됐어요` 가 간다 — `DaemonNotice`
(notices.ts:8)에 `ready` 종류를 더한다. 준비 중에는 찍기가 잠긴다.

### U9. 작업 기록과 되돌리기

`repo.history`(messages.ts:564) 의 항목이 곧 목록이다 — 제목은 커밋 제목(=사용자의
말), 둘째 줄은 그 커밋의 화면(U2 의 `cycleScreens` 와 같은 출처). 제출 시점이
구분선이다. 항목 어디를 눌러도 확인이 열리고 버튼은 늘 보인다. 확인 문구는 시각이
아니라 제목으로 말한다:

> 「회원 목록에 검색창을 넣어 줘」 직후의 화면으로 되돌릴까요?
> 그 뒤에 한 변경 N가지(개발자 코멘트 반영 포함)가 화면에서 사라져요.
> 기록은 지워지지 않고, 개발자에게는 다음 제출 때 전해져요.

`N` 과 「코멘트 반영 포함」 은 목록에서 센다 — 코멘트 반영 커밋은 제목이
`코멘트 반영 — …` 이므로(cycle-supervisor.ts:1073 의 자동 턴 제목을 그렇게 고정한다).
되돌리기는 `repo.restore { sha }` 그대로다.

### U10. 어휘

| 지금 | 이 판 |
| --- | --- |
| 보내지 못했어요(제출) · 보냈어요 · 보낸 내용 열기 · 다시 보내는 중 | 제출하지 못했어요 · 제출했어요 · 제출한 내용 열기 · 다시 제출하는 중 |
| 보내지 못했어요(AI) · 고쳐서 다시 보내기 · 4초 · 16초 · 1분 … | AI가 답을 못 했어요 · 다시 시도 · 조금씩 기다리며 다섯 번 다시 물었어요 |
| 오래 걸린 턴만 · 모든 턴 | 오래 걸린 답만 · 모든 답 |
| git · 실행 환경 확인됨 | 필요한 도구 확인됨 |
| 최신 변경 받아오기 | 개발자가 반영한 내용 받아오기 |
| 주소창의 `/member/list` | 화면 이름만. 경로는 목록 안에서만 |
| ⌥+클릭 | option(⌥)+클릭 |
| 연결 코드 · 서비스 N개에 닿아요 | 연결 정상 · 프로젝트 N개 |
| AI 프로그램의 자기 업데이트는 … | 개발자용 폴드로 |

문자열은 `labels.ts` 한 곳에 모은다. 끝 조건: 개발 실행 밖 표면에서 `턴` · `경로` ·
`git` · `데몬` · `커밋` · `브랜치` · `PR` 이 0.

### U11. 처음 한 번

마법사 대신 한 장의 체크리스트 — `도구 준비 · AI 연결 · 초대 파일`. 항목이 스스로
채워지고 초대 파일 놓는 자리가 셋째 항목 안에 있어 `시작하기` 가 없다. AI 연결 아래에
「Claude Code 는 이 앱이 AI 를 부르는 데 쓰는 프로그램이에요. 내 Claude 계정으로
로그인해요」. 회사 정책이 막으면(`classifyInstallFailure` policy, agent-install.ts:151)
빨간 안내 · IT 담당자에게 보낼 문장 · `문장 복사` · `다시 시도`. 가져온 뒤 첫 작업
화면 위에 `초대 파일을 가져왔어요 — 파일 지우기 · 나중에` 한 줄 — 앱이 파일을 대신
지운다(데스크톱 브리지 `invite.discard(path)`; 경로는 `onOpenProject` 가 안다).
`Codex` 는 건너뛸 수 있는 줄.

다시 받기의 확인 창은 `add` · `update` · **`keep`** 세 행을 보인다 —
`planInviteRows`(invite.ts:422) 에 `keep` 을 더한다. 바뀐 것이 없으면 「연결 코드만
새로 받았어요」.

### U12. 설정과 업데이트

설정은 네 줄과 폴드다 — `AI`(프로바이더 카드, 설치되지 않은 것은 접힘), `알림`,
`연결`(작성에 적을 이름 · 연결 상태 · 초대 파일 열기), `업데이트`, `▸ 개발자용`.

`업데이트` 줄에 앱 · Claude Code · Codex 가 한 목록으로 선다. 새 버전은
`2.1.4 → 2.2.0 있어요` 와 `업데이트` 버튼, 진행은 그 줄 안의 막대(`내려받는 중 · 받은
파일 확인 · 사용자 폴더에 설치`), 끝나면 `2.2.0 · 최신이에요 · 11:27에 업데이트했어요`.
`새 AI 버전이 나오면 스스로 설치해요` 토글(기본 켬). **도는 턴이 있으면 미룬다** —
`도는 작업이 끝나면 설치해요`, 끝나는 즉시 설치. 새 버전은 다음 새 대화부터 쓴다.

선로:

- `DaemonStatus.providers[].latestVersion?: string` · `agentAutoUpdate: boolean`
  (`machine.set` 으로 바꿈)
- `agent.update { kind: "claude" | "codex" }` — `AgentInstall`(agent-install.ts:295)을
  `kind` 에 `update-claude` · `update-codex` 를 더해 재사용. 진행 · 완료 메시지는
  `onboarding.install.progress` · `.done` 그대로(이름은 유지, 뜻은 「설치 진행기」).
- 확인 박자는 앱과 같다(켤 때 · 돌아올 때 · 하루 한 번). Codex 는 releases/latest,
  Claude Code 는 → 열린 항목 P1.
- 에이전트를 띄울 때 자기 업데이트를 끈다(`DISABLE_AUTOUPDATER=1`) — 둘이 서로
  바꾸지 않게. 지금 데몬에는 이 처리가 없다.

### U13. 제출 실패의 흔적

카드는 없다. 흔적은 셋 — 상태 줄(`제출 전 · 제출하지 못했어요`), `이번 작업` 의 제출
기록(시각 · 한 줄, 최근 셋), 문제 문장(`개발자에게 알렸어요 — 제출이 막혀 …`). 두
경우:

| 경우 | 버튼 | 그 뒤 |
| --- | --- | --- |
| 잠깐(network · rejected · other, 예산 안) | `다시 제출하는 중…` | 도구가 다시 제출, 영수증 |
| 개발자 몫(auth 는 `reconnect`, 예산 소진은 `developer-notified`) | 3초 `제출하지 못했어요` 뒤 잠김 | 문제 문장 · OS 알림 한 번, 계속 만들 수 있음, 풀리면 도구가 다시 제출 |

선로: `RepoStatus.submit?: { phase: "idle" | "running" | "retrying" | "blocked";
attempts: number; lastError?: "auth" | "network" | "rejected" | "other"; log:
Array<{ at, text }> }` — `cycle.json` 의 `submit`(cycle-ledger.ts:92)과
`notices` 에서 데몬이 만든다. 문장은 웹의 순수 `submitCopy(submit)`.

### U14. 개발자 코멘트 카드

L9 그대로 AI 가 반영한다. 카드는 사람 · 화면 · 글 · 상태(`AI가 반영하는 중` →
`반영해 같은 요청에 다시 제출했어요`) · `답하기`(`comments.reply`). 반영 턴의 제목은
`코멘트 반영 — <글>` 로 고정한다(U9 가 센다). `이번 작업` 의 코멘트 칸이 장부다 —
지금의 개발자 코멘트 창(ScreenPanel.tsx:1465-1580)은 없앤다(PLAN 단계 10 과 같음).

### U15. 고쳐서 다시 보내기

보낸 말 아래 hover 로 `고쳐서 다시 보내기`. `session.branch`(messages.ts:179)로 그 말
앞까지 이어받은 새 대화를 열고 입력창에 그 말을 채운다. 화면은 그대로다(툴팁이
말한다). `여기서 새 대화`(정산 줄 `···`)와 같은 기계다.

### U16. 좁은 창

900px 아래: 상태 줄은 현재 점만 글자를 갖고, `대화 | 화면 · <이름>` 탭, 찍은 핀 수가
화면 탭 배지와 `찍은 곳을 대화에서 보내기` 알약에 선다. 입력창의 `찍기` 버튼은 좁은
창에서만 보인다(넓은 창은 미리보기 막대의 것 하나).

## 3. 프로토콜 v19

| 자리 | 바뀜 | 결정 |
| --- | --- | --- |
| `repo.submit` | `{ sessionId?, note? }` | U3 |
| `ChatEvent cycle.handed` | `note?` | U3 |
| `RepoStatus` | `cycleScreens?` · `submit?` | U2 · U13 |
| `DaemonStatus.providers[]` | `latestVersion?` | U12 |
| `DaemonStatus` | `agentAutoUpdate: boolean` | U12 |
| 클라이언트 명령 | `agent.update { kind }` · `machine.set { agentAutoUpdate? }` | U12 |
| `AgentInstallKind` | `update-claude` · `update-codex` | U12 |
| `ColoDesignPinsSync` | 웹이 `tone` · `n` 을 채움(타입은 그대로) | U4 |
| `InviteRow` | `"keep"` | U11 |
| `DaemonNotice`(데몬 내부) | `ready` · `submit-blocked` · `update-done` | U8 · U13 · U12 |
| 데스크톱 브리지 | `invite.discard(path)` | U11 |
| `PROTOCOL_VERSION` | 18 → 19 | — |

선로에 **넣지 않는 것**: `제출 전` 상태값(웹 파생, U2), 세션 이름(웹 설정 그대로),
대화 내보내기(`transcriptToMarkdown` 그대로), 핀 모드 전환(브리지 `commentsMode` 그대로).

## 4. 단계

크기는 S · M · L. 각 단계는 자기 브랜치에서, 끝 조건이 서면 main 으로.

**병행 셸.** 새 화면은 `packages/web/src/next/` 에 짓고 `DaemonStatus.dev` 의 개발
실행에서 `?shell=next` 로 연다. 단계 6 에서 기본이 되고 단계 7 이 옛 셸을 걷는다.
같은 훅(`useDaemon` · `useSessions` · `usePins`)을 두 셸이 나눠 쓰므로 데몬은 하나다.
→ 열린 항목 P4.

### 단계 0. 준비 — S

- 프로토콜 v19 의 타입을 먼저 올린다(값이 비어 있어도 됨). 웹 · 데몬이 함께 빌드된다.
- `labels.ts` 에 U10 의 어휘 표를 둔다. 새 셸은 이 파일 밖의 사용자 문자열을 쓰지
  않는다(린트 규칙 하나 — `next/` 안의 한글 리터럴 금지).
- `next/` 뼈대와 `?shell=next` 스위치.
- 목업의 콜드 리뷰 스크립트(`/tmp/ui/cold` 의 아홉 과제)를 `packages/web/test/cold/`
  로 옮겨 실제 셸에 맞게 고친다 — 단계 8 이 돌린다.

**끝.** `pnpm typecheck` · `pnpm build` 가 통과하고 `?shell=next` 가 빈 셸을 연다.

### 단계 1. 뼈대 — M

Shell · Sidebar(전환기 · 다른 프로젝트 줄 · 대화 목록 · 도구가 한 일) · 상태 줄(여정 ·
제출 버튼 자리) · 홈(U6) · 좁은 창(U16) · 팔레트. `deriveJourney` · `projectNote` 순수
함수와 시험.

**끝.** 프로젝트 전환 · 대화 열기 · 홈의 확인 카드 답하기가 새 셸에서 된다.

### 단계 2. 대화 — M

입력창(첨부 · 초대 파일 넘기기 · 모델 · 생각 시간 · **빠르게**(`session.setFastMode`) ·
`@` · 사용량은 한도가 가까울 때만), 대화록(블록 · `고친 화면` 카드(U5) · 정산 줄 ·
카드 다섯 — 화면 확인 · 개발자 코멘트(U14) · 영수증 · 확인 · AI 답 실패), `고쳐서 다시
보내기`(U15), 진행 시계, 문제 문장 셋 + 초대 파일 지우기 줄.

**끝.** 말을 보내 답이 오고, 답의 카드로 미리보기가 옮겨 간다.

### 단계 3. 미리보기 — M

막대(뒤로 · 앞으로 · 새로 고침 · 주소창=화면 목록 · 기기 · 찍기 · 기록 · `···`),
찍기 알약, **말풍선**(U4), 핀 배지의 `tone` · `n`, `제출한 때의 화면`, `AI에게 이 화면
보여 주기`, 준비 화면(U8) · 화면 다시 켜는 중 · AI 가 고치는 중 덮개, 작업 기록
서랍(U9) 과 `revertSummary` 순수 함수.

**끝.** 찍고 적고 보내면 핀이 턴을 따라 사라지고, 되돌리기가 화면을 바꾼다.

### 단계 4. 제출과 이번 작업 — M

제출 확인 팝오버(U3) · `note` 선로 · PR 본문의 `> 한마디:` · `이번 작업` 팝오버(U2) ·
`cycleScreens` 데몬 구현(screen-map 에서) · 제출 상태 선로와 `submitCopy`(U13) ·
`submit-blocked` 알림.

**끝.** 픽스처 GitHub 에서 제출 · 코멘트 도착 · 반영 · 반영됨이 새 셸에서 한 바퀴 돈다.

### 단계 5. 처음 한 번 · 준비 · 초대 — M

체크리스트 한 장(U11) · 설치 차단 안내 · 초대 파일 지우기(`invite.discard`) · 첫
프로젝트 준비 화면과 대기 줄 카드(U8) · `ready` 알림 · 다시 받기의 `keep` 행.

**끝.** 깨끗한 프로필에서 초대 파일 하나로 첫 대화까지 간다.

### 단계 6. 설정과 업데이트 — M

설정 네 줄(작성 이름 칸 포함) · 업데이트 줄(U12) · `latestVersion` 확인 · `agent.update`
와 진행기 재사용 · 자동 설치 토글 · 미루기 · `DISABLE_AUTOUPDATER=1`. 이 단계 끝에
`?shell=next` 가 기본이 된다.

**끝.** 설정에서 Codex 업데이트를 누르면 재시작 없이 새 버전이 다음 대화에 쓰인다.

### 단계 7. 걷기 — M

옛 셸 제거 — `PageWorkspace` · `ScreenPanel` · `DiffPanel` · `HandoffCard` · `PinTray`
· `FrozenStage`(새 것으로 대체) · `AttentionLine`(문제 문장으로 대체) · 옛 상태 칩.
어휘 표 밖의 문자열 0. README 의 비개발자 부분을 이 판에 맞춰 다시 쓴다.

**끝.** 개발 실행 밖 표면에서 U10 의 금칙어가 0, `pnpm test` 통과.

### 단계 8. 검증 — S

콜드 리뷰의 아홉 과제를 실제 앱(개발 실행 + 픽스처 데몬)에서 다시 돌린다 — 막힌 순간
0 이 목표가 아니라, 0.3 의 15개가 재발하지 않는 것이 끝이다. 깨끗한 기계 점검(README)에
「말풍선으로 적기 · 제출 확인 · 업데이트 버튼」 셋을 더한다.

## 5. 검증

**순수 시험** — 판정마다 시험 하나: `deriveJourney` · `projectNote` · `submitCopy` ·
`revertSummary` · `lastTurnScreens`(이미 있음) · `planInviteRows`(keep) ·
`compareVersions` · `labels` 의 금칙어 검사.

**눈 검증** — 단계 0 이 옮긴 콜드 리뷰 스크립트. 상태별 스크린샷 열여섯 장
(`mockups` 의 `shoot.cjs` 와 같은 목록)을 개발 실행에서 찍어 목업과 나란히 본다.

**깨끗한 기계** — README 의 여섯 걸음 + 셋.

## 6. 리스크

| 리스크 | 완화 |
| --- | --- |
| 말풍선 좌표가 웹뷰의 배율 · 스크롤과 어긋남 | 좌표는 `rect` + `onZoom` + `onLocation` 로만 잡고, 어긋날 수 있는 사건(스크롤 · 이동 · 배율)에는 닫는다. 첫 구현은 「찍은 직후 한 번」 만 |
| Claude Code 의 최신 버전을 알 길 | P1. 모르면 줄은 `현재 2.1.4` 만 보이고 `업데이트` 는 「최신본을 다시 설치」 로 동작 |
| 자기 업데이트를 끄지 못하는 설치본 | 환경 변수가 먹지 않으면 앱의 확인이 두 번째 업데이터가 되는 것뿐 — 깨지지 않는다 |
| 병행 셸이 오래 감 | 단계 6 이 끝나면 옛 셸을 여는 길을 막는다(스위치 제거) |
| 한글 입력 중 Enter | 목업의 `isComposing` · `keyCode 229` 처리를 그대로 — `lib/ime.ts` 가 있다 |
| `cycleScreens` 의 화면 제목이 비는 커밋(화면을 만지지 않은 턴) | 목록에서 뺀다. 제출 확인에는 「화면 밖 변경 N건」 한 줄 |

## 7. 열린 항목

권장 값으로 계획을 세웠다. 다르게 정하면 해당 단계만 바뀐다.

| # | 정할 것 | 권장 | 바뀌는 단계 |
| --- | --- | --- | --- |
| P1 | Claude Code 최신 버전의 출처 | 설치 스크립트가 받는 매니페스트의 `version` 을 읽는다. 없으면 「최신본 다시 설치」 버튼만 | 6 |
| P2 | `이번 작업` 의 바뀐 화면 출처 | 데몬 `screen-map.jsonl` → `cycleScreens`. 다른 길: 웹이 `repo.history` 제목 + `threadScreens` 로 파생(부정확) | 4 |
| P3 | 한마디를 PR 본문 어디에 | 작성자 줄 바로 아래 `> 한마디:` | 4 |
| P4 | 병행 셸 스위치 | `?shell=next`(개발 실행에서만) | 0 · 6 |
| P5 | 준비가 끝난 프로젝트의 OS 알림 | 켬 — 배경에서 끝났을 때만 | 5 |
| P6 | 사용량 표시 | 한도의 70% 를 넘을 때만 모델 칩 옆 한 단어 | 2 |
