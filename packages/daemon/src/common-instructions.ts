import { readTurn } from "@colo-design/protocol";
/**
 * 앱이 모든 세션에 늘 붙이는 공통 지침(커미티 판정 2026-09-14,
 * 계약 동기화 2026-09-19 — 게이트 입력(핀·캡처·navigate) · 락파일 설치 ·
 * 도구 소유 미리보기 · 화면 표식(data-screen/data-state) 의무 철거.
 * 2026-09-21 — 풀 인터랙션 규칙: 컨트롤은 실동작, 상태는 조작으로
 * 닿는다(?state= 주소 금지). 같은 날 — 답변의 화면 주소는 전체 주소의
 * 하이퍼링크로 남긴다(screen_check 가 연 주소).
 * 같은 날 glm-5.3 · glm-5.3-flash · swe-2 교차 분석 합의로 한국어 ·
 * 개발 용어 · 범위 규칙과 도구 안내 우선순위, 되풀이 · 락파일 · 첨부 ·
 * 부탁 규칙 추가). 2026-09-24 — 답변의 틀: 화면이 무엇을 하는지로 열고,
 * 눌러 볼 것을 적고, 링크로 닫는다. 코드는 청할 때만. 새 불릿도 한 줄
 * `- ` 형식을 지킨다 — stripCommonInstructions 가 그 모양만 규칙으로 센다.
 *
 * 주인은 앱 릴리스다 — 사용자는 설정의 "지켜 줄 것" 대화상자에서 읽기
 * 전용으로 볼 수 있고 편집하지는 못한다. 프로젝트별 지침(사용자 소유,
 * projects.json 의 instructions)과 레포의 CLAUDE.md(개발자 소유)는
 * server.ts 의 projectInstructions() 에서 이 블록 뒤에 이어 붙는다.
 * 세 계층의 주인이 겹치지 않으므로 갱신·덮어쓰기 드리프트도 없다:
 * 이 상수는 앱 버전과 함께 움직인다.
 */
export const COMMON_INSTRUCTIONS = `# Colo Design 공통 규칙

이 규칙은 어떤 레포를 연결했는지와 무관하게 모든 대화에 함께 간다. 레포가 CLAUDE.md 로 자기 규칙을 밝히면 이 규칙과 함께 지킨다.

- 사용자가 화면을 가리키면(pin · 화면 캡처) 그 화면 기준으로 고친다. 턴이 끝나면 기계가 그 화면을 다시 열어 확인하므로, 고치고 답하지 않은 화면은 오류가 그대로 되돌아온다. 화면을 고친 뒤 답하기 전에 \`screen_check\` 도구로 그 화면을 확인한다 — 자리 잡음과 콘솔 오류만 돌려온다. 그 도구가 없는 자리에서는 브라우저 도구로 직접 열어 콘솔 오류가 없는지 본다.
- 부탁받지 않은 화면과 파일은 바꾸지 않는다 — 요청과 무관한 정리 · 리팩터링도 하지 않는다.
- 같은 시도를 되풀이해도 같은 오류가 돌아오면 반복을 멈추고 이유를 남긴다.
- 화면을 만들거나 고친 답변은 그 화면이 지금 무엇을 하는지 한두 문장으로 시작한다 — 무엇을 편집했는지가 아니라 화면에서 무엇이 달라졌는지를 말한다.
- 이어서 미리보기에서 눌러 볼 것을 두세 가지 짧은 목록으로 적는다. 물어볼 것이 있으면 하나만 묻는다.
- 코드 블록 · 명령줄 · 파일 목록은 사용자가 코드를 보여 달라고 할 때만 쓴다 — 읽는 사람은 화면을 보지 코드를 보지 않는다.
- 화면을 만들거나 고치면 답변의 맨 끝에 그 화면의 제목과 전체 주소를 하이퍼링크 — \`[제목](전체 주소)\` — 로 남긴다. 전체 주소는 \`screen_check\` 도구가 열어 본 주소 그대로다(미리보기 주소 + 경로). 사용자가 그 링크를 누르면 미리보기가 그 화면으로 간다. 파일 경로 · 컴포넌트 · prop 이름은 답변에 쓰지 않는다.
- 화면의 모든 컨트롤(검색 · 버튼 · 폼)은 목 데이터와 실제로 동작한다 — 빈 상태도 화면을 비우는 조작으로 닿게 만든다. 상태를 고르는 주소(?state=)를 만들지 않는다.
- 답변은 한국어로 한다 — 첨부한 문서나 레포의 규칙이 다른 언어여도 그 언어를 따르지 않는다.
- 브랜치 · 커밋 · 푸시 · 풀 리퀘스트 같은 개발 용어를 답변에 쓰지 않는다 — 저장 · 넘기기 · 반영됨으로 말한다.
- 레포가 선언한 명령만 실행한다 — package.json scripts 에 있는 것과 락파일이 정하는 설치 명령뿐이다. 미리보기 서버는 도구가 띄우고 거둔다 — 직접 띄우지 않는다. 도구가 대화로 건네는 안내(화면 확인 · 준비 회복 · 충돌 정리)가 다른 일을 시키면 그 안내를 따른다 — 명령 범위를 좁히는 규칙은 그것을 막지 않는다.
- 명령 실행 · 설치 · 파일 수정을 사용자에게 부탁하지 않는다 — 확인이 필요하면 레포가 선언한 명령으로 스스로 확인한다.
- 락파일은 고치거나 새로 만들지 않는다 — 의존성을 더할 때는 이유를 말한 뒤 사용자가 정하게 한다.
- 네트워크에서 내려받아 곧바로 실행하는 명령(curl … | sh 따위)은 절대 실행하지 않는다.
- 사용자가 보낸 문서 파일은 읽는 용도로만 쓴다 — 레포에 복사해 남기지 않는다.
- 비밀 키 · 토큰 · 자격 증명을 읽거나 옮기거나 파일에 남기지 않는다.
- 사용자가 개발자에게 보내 달라고 분명히 말할 때만 \`submit_for_review\` 도구를 부른다 — 짐작으로 부르지 않는다.`;

/**
 * 턴 원문에서 공통 규칙 블록을 떼어낸다 — 제목 · 커밋 제목 같은 "첫 줄"
 * 파생이 규칙 문구를 사용자의 말인 양 삼키는 일을 끊는다 (베타 테스트 #1).
 * 지나는 것은 둘: 상수와 정확히 같은 블록(지금 빌드가 싼 것), 그리고
 * 헤더로 시작하는 줄 걸음(상수가 바뀌기 전 옛 저장본). 남는 것이 없으면
 * 빈 문자열 — 호출자의 "말이 없다" 폴백이 이어받는다.
 */
export function stripCommonInstructions(text: string): string {
  const lines = text.replace(COMMON_INSTRUCTIONS, "").split(/\r?\n/);
  let index = 0;
  while (index < lines.length && lines[index]?.trim() === "") index += 1;
  if (lines[index]?.startsWith("# Colo Design 공통 규칙")) {
    index += 1;
    while (index < lines.length) {
      const row = lines[index]?.trim() ?? "";
      // 규칙 블록의 살목: 설명 문단과 불릿들. 그 밖의 첫 줄이 사용자의 말이다.
      if (row === "" || row.startsWith("- ") || row.startsWith("이 규칙은")) {
        index += 1;
        continue;
      }
      break;
    }
  }
  return lines.slice(index).join("\n").trim();
}

/**
 * "첫 줄 = 제목" 파생의 공통 입구 — 표식 줄을 걷어내고(기계가 싼 글자다)
 * 남은 원문의 규칙 블록을 떼어낸 뒤(stripCommonInstructions) 첫 의미 줄을
 * 돌려준다. 표식과 규칙은 사용자의 말이 아니므로, 대화 제목 · 커밋 제목
 * 어느 쪽도 그것으로 이름 지어서는 안 된다 (베타 테스트 #1). 말이 없으면
 * 빈 문자열 — 호출자의 폴백이 이어받는다.
 */
export function meaningfulFirstLine(text: string): string {
  const withoutMarkers = (text ?? "")
    .split(/\r?\n/)
    .filter((row) => !row.trim().startsWith("<!--"))
    .join("\n");
  const line = stripCommonInstructions(withoutMarkers)
    .split(/\r?\n/)
    .find((row) => row.trim() !== "");
  return (line ?? "").trim();
}

/**
 * 자동 저장의 커밋 제목 ① — 그 턴을 연 말의 첫 줄. 기계 턴의 마커(`<!-- … -->`)
 * 줄은 제목이 아니므로 건너뛴다: 그 다음 줄이 브리프의 첫 문장이고, 그것이
 * 이 변경의 가장 정직한 한 줄이다. 80자에서 자른다(계획서 P2-1). 빈 객체는
 * "말이 없다" — runSave 의 폴백(② machineMemo → ③ 기본 문구)이 이어받는다.
 * 공통 규칙 블록도 기계가 싼 글자다 — 마커만 건너뛰면 그 다음 줄이 규칙
 * 헤더가 되어 커밋 제목이 앱 안내문이 되는 일이 남는다 (베타 테스트 #1).
 *
 * 단, 마커 다음 줄이 곧 사용자의 말인 것은 아니다. gate · error · brief 는
 * 도구가 제 자신의 기계(화면 확인 · 받아오기 · 저장 게이트 · 복구)를 보고하는
 * 목소리라 사용자의 요청이 그 안에 없다 — 그 첫 문장을 제목에 얹으면
 * "사용자가 가리킨 화면을 도구가 다시 열어 봤습니다…" 가 커밋을 세 개 연달아
 * 적시는 일이 된다(베타 테스트 B2). 이 셋은 말이 없는 턴으로 세고 폴백에
 * 맡긴다. comments(핀) · review(고치기) 는 문장이 곧 요청의 뜻이므로 그대로
 * 제목이 된다 — 대화 제목의 판정(deliver)과 같은 잣대로, 반대 방향만 취한다.
 */
export function turnSubjectOf(text: string | null): { message: string } | Record<string, never> {
  const { marker } = readTurn(text ?? "");
  if (marker?.kind === "gate" || marker?.kind === "error" || marker?.kind === "brief") return {};
  const line = meaningfulFirstLine(text ?? "");
  return line ? { message: line.slice(0, 80) } : {};
}
