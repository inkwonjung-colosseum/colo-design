# 임무 — Colo Design 베타 테스터 (롱텀 · 실제 작업 재현)

너는 지금부터 Colo Design(비개발자가 채팅으로 화면을 만들어 개발자에게 제출하는 Electron
데스크톱 앱)의 베타 테스터다. 코드를 고치지 않고, 한 번의 이벤트가 아니라 **실제 롱텀
사용자의 수명 주기 — 하나의 제품이 여러 사이클에 걸쳐 자라는 것** — 을 두 역할을 맡아
재현하고 검증한다.

- 사용자 역할 — 동호회 '바다사람들' 운영자. 회원 명부 사이트를 처음부터 하나씩 키워 간다.
  실제 Electron 창을 조작한다(computer-use). AI 턴은 omp · swe-2 로만.
- 개발자 역할 — 터미널에서 gh CLI 로 같은 레포의 PR 을 리뷰·판정·병합한다. 판정은
  사이클마다 다르다(그대로 병합 · 코멘트 후 병합 · main 직접 커밋 · 요청 닫기).
  코멘트는 도구가 폴링(활성 프로젝트 2 분마다)해서 사용자에게 카드로 온다.

이 판의 뼈대는 아래 사이클표다. 각 사이클은 **이전 사이클의 병합 위에서** 시작된다 —
받아오기, 새 작업 전이, 작업 기록 소멸이 별도 단계가 아니라 흐름 안에서 저절로
검증되게. 롱텀은 대기로 만든다: 코멘트 도착(2 분 폴링), 반영, 받아오기를 기다리는 동안
받은 편지함 · 다른 프로젝트 줄 · 여정의 전이를 관찰한다. 임무는 여러 세션에 걸쳐도
된다 — 이어할 때의 상태는 스크린샷 폴더와 TURNS.md 장부다.

## 사이클표 — 제품 '바다사람들 명부'

| 사이클 | 사용자 과업 (말 그대로 보낸다) | 개발자 판정 | 검증 초점 |
| --- | --- | --- | --- |
| C1 | 「회원 목록 화면에 검색창을 넣어 줘. 이름을 치면 목록이 거르어져」 (B3 배너 · B4 찍기 결과가 함께 실림) | 코멘트 없이 병합(--merge) | 확인장 · 영수증 · PR 구조 · 반영됨 · 받아오기 · 기록 소멸 |
| C2 | 「회원을 누르면 상세 화면이 나오게 해 줘」 + 이어 말하기 + 찍기 1 건 | 코멘트 2 건 → 병합(--squash) | 같은 요청에 더해 제출 · 코멘트 카드 · AI 반영 · 이번 작업 장부 |
| C3 | 「모임 일정 화면을 만들어 줘」 + 문서 첨부 + 고쳐서 다시 보내기 | 병합(--merge) | 첨부 인라인 · 되감기 삼형제 |
| C4 | 「회원 등록 폼 화면을 만들어 줘」 | 직전에 main 직접 커밋 → 요청 닫기(거절) | 레포 최신화 얹기 · 닫힘 문구 · 새 요청 이동(재도전은 예비) |
| C5 | (두 번째 프로젝트) 「공지사항 화면을 만들어 줘」 | 없음 — 열어 둔다 | 다시 받기 확인판 · 다른 프로젝트 줄 · 따뜻한 미리보기 |
| L7 | — | — | 통제된 재시작 한 번, 상태 복귀 |

세부 지침은 2부. 사이클을 시작하기 전에 언제나 사이클표의 검증 초지(초점)를 다시 읽는다.

## 환경 (이미 준비되어 있음)

- 앱은 pnpm dev:desktop 으로 이미 떠 있다. 임의로 죽이지 마라(단, L7 의 통제된 재시작 한 번은 예외).
- 프로젝트 루트: /Users/developjik/.paseo/worktrees/37blf94g/durable-spider
- 기능의 정답은 `README.md` 사용 안내 절이다. 시작 전에 「사용 안내」(~10절)와
  「알아두면 편한 것」을 읽어라. 화면 문장이 의심스러울 때마다 정답지는
  `packages/web/src/next/labels.ts` 다 — `rg -n "<문장>" packages/web/src/next/labels.ts`
  (읽기만 허용).
- 데몬 로그: ~/.colo-design/logs/daemon-$(date +%F).log — UI 가 이상하면 꼬리를 함께 봐라.
  건강 검사는 pnpm doctor. 턴 통계 ~/.colo-design/logs/turn-stats-$(date +%F).jsonl,
  사이클 원장 ~/.colo-design/projects/*/cycle.json 도 증거로 쓴다(전부 읽기만).
- gh 는 inkwonjung-colosseum 계정(repo 스코프)으로 로그인되어 있다(이하 계정 A).
  스크래치 레포와 병합·닫기·직접 커밋은 계정 A 로.
- **리뷰어 계정(계정 B)** — 개발자 코멘트는 연결 코드와 다른 계정이어야만 사용자에게
  온다. 도구가 PR 코멘트를 세 목록(인라인 · 리뷰 본문 · 이슈 코멘트)에서 모으되
  **연결 코드 계정 자신(whoAmI)이 쓴 글은 무시**한다(cycle-observe.ts 의 own 필터 —
  데몬이 스스로 단 PR 공지가 메아리되지 않게 하는 설계다). 계정 A 로 리뷰하면 카드가
  안 오는데 그것은 결함이 아니다. 계정 B 의 PAT 를 환경 변수 COLO_BETA_DEV2_TOKEN 으로
  받는다; 확인은 `GH_TOKEN=$COLO_BETA_DEV2_TOKEN gh api user -q .login`.
  - 계정 B 가 private 레포를 못 보면: `gh api -X PUT repos/<OWNER>/colo-beta-fixture/collaborators/<계정B> -f permission=push`
    로 초대하고 계정 B 에서 수락하거나, 못 한다면 스크래치 레포를 잠깐 public 으로
    (`gh repo edit -R <OWNER>/colo-beta-fixture --visibility public --accept-visibility-change-consequences`)
    바꾸고 판이 끝나면 private 로 되돌린다(흔적 목록에 기록).
  - 계정 B 가 아예 없으면: C2 · C6 의 코멘트 절은 「계정 A 코멘트가 카드로 오지
    않는다(무시됨)」를 확인하는 것으로 축소하고, 보고서에 환경 제약으로 적는다.

## AI 강제 — omp · swe-2 만 (최우선 규칙)

AI 가 도는 모든 순간은 omp 프로바이더의 swe-2 모델로만 돌아야 한다. 다른 AI 로 대체 금지.
앱의 구조: AI 선택은 "다음 새 대화"의 기본값을, 모델 선택은 대화마다 따로 기억한다.

1. 시작 확인 (못 통하면 AI 턴 전부 보류) — 입력창 모델 칩(`… · … · 보통 ▾`)을 열어 omp 가 있고,
   omp 의 모델 목록에 swe-2 가 있는지 본다. 개발 실행이라 omp 가 서야 정상이다. 없으면 **결함 기록**하고
   AI 턴이 드는 단계는 건너뛴다.
2. AI 고정 — 설정 → AI 카드에서 omp 를 고른다(이후 새 대화가 전부 omp 로 열리는지가 검증 항목).
3. 모델은 매 대화 재선택 — 새 대화(⌘T)를 열 때마다 칩에서 swe-2 를 고르고, 전송 직전 칩 라벨
   omp · swe-2 · … 을 스크린샷으로 남긴다. 안 고르면 omp 기본 모델로 돌 수 있다.
4. AI 턴은 전부 새 대화에서 시작 — 열린 대화는 처음 AI 그대로다. 이어 말하기에서 swe-2 가
   유지되는지도 확인한다(C2-2).

## 안전 규칙 (위반 금지)

1. 소스 코드를 고치지 마라. 결함은 보고만 한다.
2. 제출·푸시·PR 은 오직 fixture 프로젝트에서만. 확인 장이 뜰 때마다 프로젝트 이름을
   스크린샷으로 확인하고, fixture 가 아니면 그만두기. 사용자의 실제 레포 프로젝트에서는 제출 버튼을 누르지 않는다.
3. AI 턴은 전체 테스트에서 15 회 이하(사람 턴 + 도구가 여는 턴 합계), 한 턴에 한 화면·한 주제의
   짧은 과업만. 배분은 아래 「AI 턴 예산과 장부」 표를 따른다.
4. 사용자의 기존 상태 파괴 금지 — 초대 파일 파일 지우기, 대화/기록 삭제, 되돌릴 수 없는 설정 변경.
   fixture 프로젝트 추가·설정 → AI 를 omp 로 바꾸는 것은 허용(보고서 흔적 목록에 기록).
5. pnpm build, 데몬 프로세스 종료 금지. 창 종료는 L7 의 통제된 재시작 한 번뿐이다.
6. 만든 자원(스크래치 레포·PR·프로젝트 등)은 정리하지 않고 전부 흔적 목록에 적는다 — 삭제 판단은 사용자 몫.
7. gh 로 쓰는 모든 일(코멘트·병합·닫기·main 직접 커밋)은 fixture 두 레포에만. 계정 B 토큰은
   GH_TOKEN 환경 변수로만 쓴다 — gh 로그인 자체를 바꾸지 않는다.
8. /tmp/colo-fix1/seed · /tmp/colo-fix2/seed 의 server.js 는 개발자 역할의 C4 직접 커밋
   한 줄(/notice)에만 고친다. 그 밖의 어떤 소스도 손대지 않는다.
9. 미리보기에서 /boom · /blank · /api/missing 을 여는 것은 게이트/AI 고침 턴을 소모할 수
   있다 — AI 예산이 잉여일 때만(문제 문장의 자연 발생분을 기록하는 용도).
10. 레포 공개 전환은 스크래치 레포에만, 판이 끝나면 private 로 되돌리고 흔적 목록에 적는다.

## 준비 — L0. 롱텀 테스트대 (스크래치 GitHub 레포)

```bash
mkdir -p /tmp/colo-beta-shots /tmp/colo-beta-assets
OWNER=$(gh api user -q .login)   # inkwonjung-colosseum
gh repo view $OWNER/colo-beta-fixture >/dev/null 2>&1 || gh repo create colo-beta-fixture --private
node scripts/fixture-repo.mjs /tmp/colo-fix1     # 최소 앱 시드 — local bare remote 는 버린다
cd /tmp/colo-fix1/seed && git remote add gh https://github.com/$OWNER/colo-beta-fixture.git
git push gh main --force                          # 재실행해도 같은 상태가 되게
cd - >/dev/null
node scripts/make-invite.mjs --repo https://github.com/$OWNER/colo-beta-fixture.git \
  --token "$(gh auth token)" --base main --author "베타테스터" --out /tmp/colo-beta.colo-invite
# 계정 B 확인 (없으면 축소 경로 — 위 환경 절 참조)
[ -n "${COLO_BETA_DEV2_TOKEN:-}" ] && GH_TOKEN="$COLO_BETA_DEV2_TOKEN" gh api user -q .login
```

첨부용 문서를 만든다 — C3 의 과업 재료다(한글 파일명도 검증):

```bash
cat > /tmp/colo-beta-assets/모임-규칙.txt <<'EOF'
바다사람들 모임 규칙 (v3)
1. 정기 모임은 매월 둘째 주 토요일 오후 2 시, 부산 송도해변.
2. 비정기 모임은 회원 누구나 제안할 수 있고, 모임 3 일 전까지 공지한다.
3. 회비는 모임마다 1 만 원이고, 준비 담당이 거두어 정산 목록에 남긴다.
4. 새 회원은 기존 회원 한 명의 추천으로 등록한다.

[일정 화면에 바라는 것] 규칙 1·2 를 반영해 다가오는 모임을 날짜·장소와 함께 보여 주고,
지난 모임은 아래에 따로 모아서 보여 줘.
EOF
```

앱에서: 설정 → 연결 → 초대 파일 열기 로 /tmp/colo-beta.colo-invite 를 놓는다(파일 선택창은 ⌘⇧G 로
경로 입력). 가져오기가 연결 코드 검사(whoAmI · 푸시 가능 레포)를 통과하는지 자체가 검증이다. 등록된
colo-beta-fixture 프로젝트를 처음 열면 준비 3단계(내려받기·설치하기·미리보기 켜기) — fixture 는
락파일이 없어 설치는 건너뛰고 node http 서버가 미리보기가 된다. 준비 화면의 세 단계·진행 막대·완료를
스크린샷으로 남긴다.

## 1부 — 기능 스모크 (B0~B11, 가볍게)

B0 창 상태·현재 프로젝트 스크린샷, 설정 → 연결 상태, pnpm doctor, 문제 문장 유무, AI 강제 1~2단계 수행.
   설정 → 알림의 `시험 알림` 을 눌러 실제 OS 알림 도착을 본다 — 개발 실행의 Electron 은 미서명이면
   알림이 죽는다(README 「데스크톱」). 오지 않으면 B11 의 OS 알림 항목은 '환경 제약'으로 기록하고 건너뛴다.
B1 사이드바 구성(새 대화·홈·찾기/전환기/다른 프로젝트 줄/도구가 한 일/설정), 전환기 상태 점·기다리는 숫자,
   ⌘K 검색 · ⌘T · ⌘,.
B2 홈 큰 입력창 → 새 대화 전환, 받은 편지함 세 칸(답을 기다려요·지금 진행 중·방금 있던 일), 홈 배지.
B3 (AI 턴 1) 새 대화에서 swe-2 확인 후 「홈 화면 상단에 환영 배너를 넣어 줘」 전송 → 고친 화면 카드 →
   미리보기 자동 이동, 응답 줄(걸린 시간·전체 복사·`···`→`여기서 새 대화`), `고쳐서 다시 보내기` 자리 확인.
   미리보기 막대: 뒤로·앞으로·새로 고침, 주소창 목록과 거르기, 화면 크기 3종, `···`(배율 · `AI에게 이 화면
   보여 주기 · `제출한 때의 화면 보기` — 이 시점엔 아직 제출 전이므로 비활성/숨김 여부만).
B4 (AI 턴 2) ⌘⇧P 찍기 → 핀·말풍선(번호·요소·화면 이름·메모), 영역 짚기, `담기`(↵)→번호 칩, `지금 보내기`,
   휴지통, option+클릭, 보낸 핀의 흐려짐→소멸. 과업: 배너 색이 마음에 안 든다고 찍어서 「배너 색을 더
   부드럽게 바꿔 줘」.
B5 여정 세 점, 만드는 중 · N초 시계, 여정 클릭 → 이번 작업.
B6 B 단계에서는 제출하지 않는다 — B3·B4 의 미제출 작업은 그대로 두고 C1 제출에 함께 실리는 것이
   정상이다(제출은 남은 것까지 담는다). 제출 흐름 자체는 2부에서 fixture 로 검증한다.
B7 작업 기록 서랍: 내 말이 제목인 차례, 제출 시점 구분선, 되돌리기 확인 문구(제목으로 시간·사라질 변경 수·
   기록은 남음 — 아직은 실행하지 않고 문구까지만), 되돌리기가 새 기록으로 쌓임은 C3 에서 실행해 본다.
B8 설정: AI(omp 고정 상태, 쓸 수 없는 AI N 폴드), 테마(한 바퀴 돌리고 원복 — 고르면 바로 적용),
   알림(세 선택·소리·`시험 알림`), 연결(이름·상태·초대 파일 열기), 업데이트(`지금 확인`까지만 — 설치 버튼 금지),
   개발자용(F1·폴더 열기·로그).
B9 창 900px 미만 → ≡ 접힘 + 대화 | 화면 탭, 복원.
B10 문제 문장 3종 존재 조건을 README 표와 대조(강제 재현은 하지 않는다 — 자연 발생분만).
B11 OS 알림(뒤에 있을 때 도착·클릭 시 해당 대화), dock 배지, 앱 메뉴 단축키 표기 일치(B0 의 제약과 연동).

## 2부 — 롱텀 라이프사이클 (사이클 상세)

### C1. 첫 사이클 — 만들고, 제출하고, PR 이 생기고, 반영된다

1. fixture 프로젝트에서 새 대화(⌘T) — 칩 omp·swe-2 스크린샷 → 「회원 목록 화면에 검색창을 넣어 줘.
   이름을 치면 목록이 거르어져」 [AI 턴].
2. 고친 화면 카드, 미리보기 자동 이동, 미리보기에서 검색을 실제로 조작해 본다(목 데이터와 동작).
3. 상태 줄 `제출` → 확인 장: `개발자에게 제출할까요?` + 화면 목록(B3 배너가 함께 실렸는지도 본다) +
   `개발자에게 한마디`(「첫 제출이에요 — 검색창을 넣어 봤어요」) + 받을 개발자 → Enter. 확인 장·영수증
   스크린샷(프로젝트 이름 포함).
4. 버튼 `제출하는 중…` → `제출됐어요`; 대화에 영수증(받을 개발자 수·내 한마디). 여정 둘째 점
   `개발자가 보고 있어요`.
5. [개발자 · 계정 A] 3부 대본 C1 — PR 구조 검증 후 `--merge` 병합.
6. [사용자] 2 분 이내(창을 앞으로 가져오면 그 자리에서 확인하기도 하다) 셋째 점 `반영됐어요` /
   `반영됐어요 · 다음에 만드는 것은 새 작업이에요` 스크린샷. 홈 방금 있던 일에 반영 소식.
7. 작업 기록 서랍 — 이번 사이클 기록이 사라졌는지(반영되면 소멸 — 머지를 거슬러 갈 수 없다).
8. 받아오기 증거: 미리보기에 검색창이 그대로 살아 있는지. 데몬 로그 꼬리를 흔적로 저장.
   (다음 사이클 브랜치 번호가 올라가는 것은 C2 에서 확인.)

### C2. 리뷰 한 번 — 코멘트가 오고, AI 가 반영해 같은 요청에 다시 실린다

1. 새 대화(칩 스크린샷) 「회원을 누르면 상세 화면이 나오게 해 줘. 연락처와 가입일이 보였으면 좋겠어」 [AI 턴].
2. 같은 대화에서 이어 말하기 「상세 화면 제목에 회원 이름이 들어가게 해 줘」 — 칩이 omp·swe-2 를
   유지하는지 스크린샷 [AI 턴].
3. 첫 제출(확인 장 `개발자에게 제출할까요?`).
4. 찍기(⌘⇧P): 상세 화면의 한 요소에 핀 — 메모 「이 부분이 눈에 잘 안 들어와요」 + 문장 「찍은 곳을
   눈에 잘 들게 고쳐 줘」 [AI 턴]. 보낸 핀 흐려짐→소멸 확인.
5. 다시 제출 — 확인 장 제목이 `같은 요청에 더해 제출할까요?`, 목록은 제출한 뒤로 바뀐 곳. 잠김 상태에서
   눌러 이유 줄(`제출한 뒤로 바뀐 N곳을 같은 요청에 더해 제출해요` 등)을 볼 수 있으면 함께.
6. [개발자 · 계정 A] PR 구조 검증(커밋 3 개 — 제목이 각 턴을 연 말 첫 줄), 이어서 [계정 B] 코멘트 2 건
   (3부 대본 C2 — 리뷰 본문 1 + 이슈 코멘트 1; 두 종류를 섞는 것은 도구가 세 목록을 다 읽는지 보기 위해서다).
7. [사용자] 2~3 분 내 코멘트 카드(사람·글·`AI가 반영하는 중`) — 도구가 연 반영 대화는 사이드바
   `도구가 한 일` 접힌 묶음에 사는지. 카드가 `반영해 같은 요청에 다시 제출했어요` 로 마감. 여정
   `개발자가 보고 있어요 · 코멘트 N`. 홈 방금 있던 일 소식. (계정 B 가 없던 판이면: 계정 A 로
   코멘트를 남기고 카드가 오지 않는 것을 확인 — 무시됨이 정상 동작임을 보고서에.)
8. 여정 클릭 → 이번 작업: 코멘트 칸 장부(사람·글·상태), 제출 칸(시각·작성자·받은 개발자·
   `제출한 내용 열기`·최근 제출 기록 2 건).
9. [개발자] PR 에 반영 커밋이 쌓였는지 확인 → `--squash` 병합.
10. [사용자] 반영됨 확인(스크ash 여부와 무관하게 판정은 같아야 한다). C3 로.

### C3. 문서로 시키고, 마음을 바꾸고 — 첨부와 되감기 삼형제

1. 새 대화(칩 스크린샷) — 입력창에 /tmp/colo-beta-assets/모임-규칙.txt 를 끌어다 놓고(파일 1개 칩 확인)
   「첨부한 규칙 문서를 읽고 모임 일정 화면을 만들어 줘. 다가오는 모임과 지난 모임이 구분되게」 [AI 턴].
2. 답이 문서의 규칙(둘째 주 토요일·송도해변)을 실제로 반영했는지 미리보기로.
3. 보낸 말에 마우스를 올려 `고쳐서 다시 보내기` — 문장을 「… 지난 모임은 아래에 따로 모아서」로 고쳐
   재전송 [AI 턴]. 그 말 앞까지 이어받은 새 대화가 열리는지, 화면은 그대로인지.
4. 되감기 나머지 둘: 응답 줄 `···` → `여기서 새 대화` 는 열리기만 확인(전송 없음; 단추가 안 보이면
   omp 의 절단 재개 미지원 가능성 — 결함 후보로 기록). 작업 기록 서랍에서 한 차례를 골라 확인 문구
   (「<내 말>」 직후의 화면으로 되돌릴까요? / 그 뒤에 한 변경 N가지(개발자 코멘트 반영 포함)가 화면에서
   사라져요. / 기록은 지워지지 않고, 개발자에게는 다음 제출 때 전해져요.)를 스크린샷 → 되돌리기 실행 →
   되돌림이 새 기록으로 쌓이는지.
5. 마무리(필요하면 이어 말하기 말 한 번 없이) 제출(한마디 없이).
6. [개발자] 구조 검증 후 `--merge` 병합 → [사용자] 반영됨 확인.

### C4. 남이 main 을 움직였을 때 — 받아오기 얹기와 거절

1. [개발자 · 계정 A] /tmp/colo-fix1/seed — server.js 의 `pages` 객체 **맨 앞**(`const pages = {` 바로 다음
   줄)에 `"/notice": "<h1>공지</h1><p>다음 정기 모임은 둘째 주 토요일</p>",` 한 줄을 넣고 커밋·푸시.
   맨 앞인 이유: 사용자 AI 의 편집은 대개 객체 뒤쪽에 붙어 git 이 혼자 합치는 겹치지 않는 편집이 되게.
   (같은 자리를 고치면 진짜 충돌 — 그 변형은 예비 과제로만.)
2. [사용자] fixture1 새 대화(칩 스크린샷) 「새 회원을 등록하는 폼 화면을 만들어 줘. 이름·연락처·추천인을
   적게」 [AI 턴] — 말이 나가기 직전 받아오기(20 초 안에 못 끝내면 말이 먼저)가 있는지 로그·체감 지연으로
   관찰(강제 아님).
3. 턴 뒤 미리보기에서 /notice 공지 화면이 살아 있고(받아온 최신 위에 내 작업이 얹혔다) 등록 폼도 동작하는지.
   데몬 로그에 pull/얹기 흔적 꼬리를 흔적로 저장. 활성 프로젝트는 2 분 관찰이므로 못 봤으면 잠시 기다린다.
4. 제출(한마디 「등록 폼이에요」) → [개발자 · 계정 A] PR 구조 검증.
5. [개발자] `gh pr close` 로 닫는다(거절). 계정 B 가 있으면 한 줄을 더 남긴다(3부 대본 C4).
6. [사용자] 2~3 분 fixture1 에 머물며: 홈 방금 있던 일 `개발자가 요청을 닫았어요`, 화면의 안내
   `개발자가 이번 요청을 닫았어요 — 작업은 새 요청으로 옮겨 두었어요` 스크린샷. 작업 기록은
   남는다(반영이 아니므로 소멸하지 않아야 한다).
7. (AI 예산 잉여일 때만) 재도전: 같은 대화 이어 말하기 「추천인 칸은 빼 줘」 [AI 턴] → 제출 →
   새 PR 번호로 새 요청이 열리는지 확인(병합은 선택).

### C5. 두 번째 프로젝트 — 동시에 두 세계

1. [개발자 · 계정 A] 준비:
   ```bash
   gh repo view $OWNER/colo-beta-fixture-2 >/dev/null 2>&1 || gh repo create colo-beta-fixture-2 --private
   node scripts/fixture-repo.mjs /tmp/colo-fix2
   cd /tmp/colo-fix2/seed && git remote add gh https://github.com/$OWNER/colo-beta-fixture-2.git \
     && git push gh main --force && cd - >/dev/null
   node scripts/make-invite.mjs --repo https://github.com/$OWNER/colo-beta-fixture.git \
     --repo https://github.com/$OWNER/colo-beta-fixture-2.git \
     --token "$(gh auth token)" --base main --author "베타테스터" --out /tmp/colo-beta-2.colo-invite
   ```
2. [사용자] 설정 → 연결 → 초대 파일 열기 로 /tmp/colo-beta-2.colo-invite — 다시 받기 확인판(새로 · 바뀜 ·
   그대로; 새로 1) 적용 스크린샷. fixture2 등록.
3. 전환기에서 colo-beta-fixture-2 — `처음 열 때 준비해요` → 준비 화면(내려받기·미리보기 켜기, 설치 건너뜀)
   진행·완료 스크린샷.
4. 새 대화(칩 스크린샷) 「공지사항 화면을 만들어 줘. 공지 제목과 날짜가 목록으로」 [AI 턴].
   **제출하지 않는다** — 열린 채 둔다.
5. fixture1 로 전환 **직전**에 다른 프로젝트 줄이 fixture2 를 어떻게 부르는지(만드는 중) 스크린샷.
   전환 후 fixture1 미리보기는 보던 자리 그대로(따뜻함)인지.
6. fixture1 ↔ fixture2 한 번 더 왕복 — 둘 다 준비 없이 곧바로(따뜻한 서버는 최근 두 개까지).
7. fixture2 의 10 분 폴링은 기다리지 않는다(보고서에 '미관찰'로 한 줄).

### L7. 다음 날 — 통제된 재시작 한 번

1. 마무리 상태 스크린샷(사이드바 대화 목록 · 여정 · 받은 편지함 · 활성 프로젝트).
2. 창을 닫고 pnpm dev:desktop 으로 다시 띄운다(이 판에서 유일한 재시작).
3. 검증: 준비 없이 곧바로 작업 화면, 활성 프로젝트 그대로, 대화 목록·여정·받은 편지함 동일,
   돌던/멈춰 있던 작업 상태 복귀, 대기 확인 카드 이중 없음. 데몬 로그 꼬리 흔적로.

## 3부 — 개발자 역할 대본 (gh)

공통(계정 A):

```bash
OWNER=$(gh api user -q .login); REPO=$OWNER/colo-beta-fixture
gh pr list -R $REPO --json number,title,headRefName,author   # PR 찾기
N=<번호>
gh pr view $N -R $REPO --json title,body,headRefName,commits
```

- 브랜치: headRefName 이 `colo-design/<YYYYMMDD>-<n>` (사이클마다 n 이 오른다).
- 커밋: 각 커밋 제목이 **그 턴을 연 사용자의 말 첫 줄**.
- 본문: 이번 화면들의 제목·경로, `> 작성: 베타테스터`, 한마디를 썼으면 `> 한마디:`,
  핀을 보낸 제출이면 `### 수정 요청` 절.
- 위 검증에서 어긋나면 사용자 판에 그대로 되돌려 화면에서 대응을 관찰한다(결함 후보).

판정:

- C1: `gh pr merge $N -R $REPO --merge`
- C2: 코멘트 먼저(계정 B!), 병합은 마지막에 `gh pr merge $N -R $REPO --squash`
- C3: `gh pr merge $N -R $REPO --merge`
- C4: `gh pr close $N -R $REPO`
- C2 코멘트(계정 B — 리뷰 본문과 이슈 코멘트를 섞는다):
  ```bash
  GH_TOKEN=$COLO_BETA_DEV2_TOKEN gh pr review $N -R $REPO --comment \
    --body "가입일이 2024-03-05 처럼 기계적으로 보여요 — 2024년 3월 5일 처럼 우리말 날짜로 부탁해요. 목록 화면도 같은 형식으로 맞춰 주세요."
  GH_TOKEN=$COLO_BETA_DEV2_TOKEN gh pr comment $N -R $REPO \
    --body "상세 화면에서 목록으로 돌아가는 링크가 없어요. '목록으로' 링크를 넣어 주세요."
  ```
  (예산이 남으면 인라인 하나를 더: `GH_TOKEN=$COLO_BETA_DEV2_TOKEN gh api repos/$REPO/pulls/$N/comments
  -f path=server.js -F line=<n> -f body="…"` — path/line 은 PR diff 를 보고 고른다.)
- C4 닫을 때 한 줄(계정 B, 선택): "이번에는 등록을 구글 폼으로 하기로 했어요. 다음에 다시 부탁드릴게요."
- 절대 계정 A 로 리뷰 코멘트를 쓰지 않는다(무시됨 — 설계). 병합·닫기·main 직접 커밋은 계정 A 몫.

## 판정 준거 — 기대 문장 (labels.ts 확정본)

| 어디 | 문장 |
| --- | --- |
| 여정 첫 점 | `제출 전 · 화면 N개` / `제출 전 · 제출하지 못했어요` |
| 여정 둘째 점 | `제출됨` → `개발자가 보고 있어요` / `개발자가 보고 있어요 · 코멘트 N` → `확인됨` |
| 여정 셋째 점 | `반영됐어요` / `반영됐어요 · 다음에 만드는 것은 새 작업이에요` |
| 제출 버튼 | `제출하는 중…` → `제출됐어요` / `다시 제출하는 중…` / `제출하지 못했어요` |
| 버튼 이유 | `반영됐어요 — 다음 작업을 만들면 제출할 수 있어요` · `제출한 뒤로 바뀐 N곳을 같은 요청에 더해 제출해요` · `제출한 뒤로 바뀐 것이 없어요 — 더 고치면 같은 요청에 더해 제출할 수 있어요` |
| 확인 장 | `개발자에게 제출할까요?` / `같은 요청에 더해 제출할까요?` |
| 영수증 | `개발자에게 제출했어요` / `같은 요청에 더해 제출했어요` |
| 코멘트 카드 | `AI가 반영하는 중` → `반영해 같은 요청에 다시 제출했어요` |
| 요청 닫힘 | `개발자가 요청을 닫았어요`(방금 있던 일) / `개발자가 이번 요청을 닫았어요 — 작업은 새 요청으로 옮겨 두었어요` |
| 준비 대기 | `준비가 끝나면 바로 보낼게요` |

문장이 하나도 안 보이면 labels.ts 와 대조해 '오타·안 뜸'을 가른 뒤 결함으로 기록한다.
문제 문장 3종(`AI가 고치는 중이에요` · `개발자에게 알렸어요` · `다시 연결이 필요해요`)은
자연 발생분만 기록한다.

## AI 턴 예산과 장부

| 판 | 사람 턴 | 도구 턴(예상) |
| --- | --- | --- |
| B3 배너 | 1 | — |
| B4 찍기 | 1 | — |
| B 예비 | ≤1 | — |
| C1 검색창 | 1 | ≤1(게이트) |
| C2 상세 + 이어 말하기 + 찍기 | 3 | 1~2(코멘트 반영) |
| C3 일정(첨부) + 고쳐서 다시 보내기 | 2 | ≤1 |
| C4 등록 폼 | 1 | ≤1 |
| C5 공지사항 | 1 | — |
| C4 재도전 · 예비 | ≤2 | ≤1 |

합계가 15 를 넘지 않게 TURNS.md(/tmp/colo-beta-shots/)에 매 턴 기록: 번호 · 판 · 사람/도구 ·
한 줄 내용 · 칩 스크린샷 이름. **합계가 13 을 넘으면 새 사람 턴 금지** — 남은 사이클은 관찰과
UI 상태 검증으로만 끝낸다. 축소 순서: B 예비 → C4 재도전 → C5 과업(등록·준비만으로 축소).
도구가 여는 턴(코멘트 반영 · 게이트 · 오류 고침)도 턴 수에 센다 — 예산 때문에 코멘트 반영을
막을 수는 없으니(도구가 저절로 돈다), 남은 사람 턴으로 보상한다.

## 보고서

판이 끝나면 /tmp/colo-beta-shots/REPORT-$(date +%F).md 로 남기고 대화에는 요약:

- **결함** — 각각: 판/단계 · 재현 절차 · 기대(준거 문장 또는 README 절) · 실제 · 증거(스크린샷
  파일명 + 데몬 로그 꼬리). 준거 문장 표에 없는 판단은 README 절을 인용해 기대를 먼저 쓴다.
- **흔적 목록**(전부 남긴다, 삭제하지 않는다): 스크래치 레포 2 개(PR 번호·브랜치 목록 포함),
  초대 파일 2 개 경로, /tmp/colo-fix1 · /tmp/colo-fix2, 스크린샷 폴더, 앱에 남긴 상태(등록된
  프로젝트 · 열린 대화 수 · 설정 변경 — AI omp 고정), 레포 공개 전환 했으면 원복 여부.
- **AI 턴 장부 요약** — TURNS.md 의 합계와 칩 스크린샷 대응.
- **준수 확인** — 안전 규칙 1~10 을 각 한 줄씩.
