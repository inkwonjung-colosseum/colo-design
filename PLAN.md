# PLAN — 페이지 축 셸 · 프로젝트 · 개발자 핸드오프

README는 지금 배포된 것을 적고, 이 문서는 다음 판이 무엇이고 왜인지를 적는다.
결정은 번호를 달아 두어 코드 주석이 `PLAN D3`처럼 가리킬 수 있게 한다.

## 0. 진단 — 지금 구조의 어디가 새고 있나

제품이 하려는 일은 한 문장이다: **기획자가 Confluence 기획서를 쓰고, 그 기획서로
화면을 만들고, mock 데이터 위에서 인터랙션까지 끝낸 화면을 개발자에게 넘긴다.
개발자는 로직만 붙인다.** 지금 셸은 이 흐름을 세 군데서 거스른다.

1. **기획/디자인 탭은 저장소 구조가 새어나온 것.** 두 탭의 근거는 "SDK가 세션을
   cwd별로 저장한다"(README 37-40행)는 구현 사정이다. 기획자의 단위는 워크스페이스가
   아니라 *기획서 한 장의 파이프라인*이고, 기획→디자인 핸드오프(`이 문서로 화면
   만들기`)는 이미 그 방향으로 반쯤 가 있다.
2. **프로젝트 개념이 없는데 프로젝트 의존 설정은 있다.** 레포는 하나
   (`~/drafthouse/repo`, `repo.ts:63-68`), Confluence는 사이트 하나에 스페이스 N개.
   그런데 기획 규칙은 *레포의* `drafthouse.json#planning.rules`에서 온다 — 이미
   "이 레포 ↔ 이 기획서들"이라는 결합이 암묵적으로 존재한다. 회사에 제품이 여러 개고
   한 스페이스 안에 여러 제품 기획서가 서브트리로 산다.
3. **게시가 기본 브랜치에 직접 푸시한다** (`repo.ts:506-507`, `push origin HEAD`).
   개발자가 받아서 로직을 붙이려면 PR이어야 한다 — 리뷰, CI, 머지 판단이 거기서
   난다.

이미 있고 그대로 쓰는 것: 미러 엔진(낙관적 락, 3지 충돌, 백그라운드 풀), 에디터와
정규화 단일 경로, 게이트 있는 게시, 프리뷰 iframe과 코멘트 오버레이, 온보딩,
자격 증명 저장, 데스크톱. 이 판은 **표면과 스코프**를 바꾸고 엔진은 남긴다.

## 1. 결정

### D1. 축은 페이지다

트리에서 기획서를 고르면 세 컬럼이 전부 그 페이지 기준으로 바뀐다.

```
┌────────────────┬─────────────────────────┬──────────────────────────┐
│ [결제 ▾]        │ [기획 ●] [화면 만들기] [+] │  [문서 | 화면]             │
│ 프로젝트 서브트리 │                         │                          │
│ ▸ 결제수단 등록 ◉ │  활성 세션 transcript     │  문서 = TipTap 에디터      │
│   · 화면 목록    │                         │  화면 = 레포 프리뷰        │
│ ▸ 환불 ○        │                         │    [화면▾][상태][폭]       │
│                │─────────────────────────│                          │
│ 게시 (3)        │  composer               │  [저장] [개발자에게 넘기기]  │
└────────────────┴─────────────────────────┴──────────────────────────┘
```

- 기획/디자인 탭 삭제. `Shell`은 프로젝트 스위처 + 경고 + 세 컬럼.
- 오른쪽은 `문서 | 화면` 세그먼트. 문서 = 이 페이지의 에디터, 화면 = 이 페이지를
  `spec`으로 갖는 화면으로 이동한 프리뷰(D7).
- 트리 배지로 단계를 보여 준다: `○ 기획 중 / ◐ 화면 있음 / ◉ 인터랙션 완료 /
  ✓ 넘김 / 반영됨`. 판정 소스는 §2.4.

### D2. 세션은 페이지에 소속되고, 탭으로 놓인다

- 새 세션은 "어느 워크스페이스"가 아니라 "이 페이지로 무엇을"로 만든다:
  **기획서 쓰기/고치기**(cwd = 프로젝트 미러) 또는 **화면 만들기**(cwd = 레포 클론,
  기획서가 첫 턴). 데몬의 두 cwd·write policy·`workspace` 필드는 그대로 두고,
  `session.create`에 `pageId`를 붙인다.
- 채팅 컬럼 상단 탭 스트립. 백그라운드 세션은 계속 돌고 탭에 live dot, 끝나면 배지.
- **타일(창 추가)은 하지 않는다.** 동시 라이브 턴은 구조적으로 최대 2개다 — 미러에
  쓰는 기획 턴 1개(에디터 락), 레포에 쓰는 디자인 턴 1개(git 충돌). 존재할 수 없는
  상태를 위한 UI는 만들지 않는다. 필요하면 "기획 | 화면 세션 나란히" 2분할 1회.

### D3. 프로젝트 = Confluence 서브트리 N개 + 레포 1개

```json
// ~/drafthouse/config/projects.json
[
  {
    "slug": "payments",
    "name": "결제",
    "roots": [{ "space": "PROD", "rootPageId": "123456", "title": "결제 서비스" }],
    "repo": { "url": "https://github.com/org/payments-web", "baseBranch": "develop" }
  }
]
```

- Confluence 사이트·이메일·토큰은 회사 공통 1개(`confluence.json` 유지).
  레포 PAT는 프로젝트별(자격 증명 항목 `pat:<slug>`, `projects.ts#repoPatItem`).
- **활성 프로젝트는 하나.** 클론은 여러 개 유지하되 프리뷰 서버는 활성 프로젝트만
  기동한다 — `preview.port`는 레포가 선언하므로 두 레포가 같은 포트를 쓰면 충돌한다.
  비활성 프로젝트의 Claude 세션이 백그라운드에서 도는 건 무방.
- 프로젝트 전환 = 트리·세션·프리뷰가 바뀜, 이전 프리뷰 stop → 새 프리뷰 start.

### D4. 미러는 스페이스가 아니라 서브트리를, 프로젝트가 소유한다

미러는 `<space>/<제목>.md` 평면이고 트리는 frontmatter `parentPageId`로 계산된다
(`sync-engine.ts:4-5`). 서브트리를 폴더나 심링크로 잘라낼 수 없고, 필터로 하면
Claude cwd에 다른 제품 기획서가 보이며 경로 기반 write policy로 막을 수도 없다.

```
~/drafthouse/
  config/   daemon.json  confluence.json  projects.json
  projects/<slug>/
    confluence/<space>/      # rootPageId 하위 페이지만, 자기 .confluence-sync.json
    repo/                    # 클론. <repo>/confluence -> ../confluence (지금과 같은 로컬 링크)
```

- 클론·풀은 루트의 후손만: `GET /wiki/api/v2/pages/{id}/children`을 BFS로 돈다
  (`descendants` 엔드포인트는 depth 상한이 있어 쓰지 않는다). 큰 스페이스에서 필요한
  것만 받으니 오히려 가벼워진다.
- 기획 세션 cwd = `projects/<slug>/confluence/`. Claude가 보는 상대경로
  `<space>/<page>.md`는 지금과 같고, 다른 프로젝트 페이지는 보이지 않으며, SDK가
  cwd별로 저장하므로 **세션 트랜스크립트가 프로젝트별로 자동 분리**된다.
- `@confluence/<space>/<page>.md` 참조 규약(`connected-repo/CLAUDE.md:61`)은 변경
  없음. write policy의 `mirrorRoot`는 realpath 판정이므로 링크가 아닌 실제 폴더를
  넘긴다(`workspaces.ts:40-42`).
- **겹침 금지.** 프로젝트 생성 시 같은 스페이스의 다른 프로젝트 루트와 조상/후손
  관계면 거부한다("결제 프로젝트가 이미 이 페이지를 포함합니다"). 한 페이지가 두
  미러에 있으면 낙관적 락이 둘로 쪼개져 충돌 판정이 깨진다 — 이것이 유일한 규칙.
- 새 페이지의 기본 부모 = 선택된 페이지, 없으면 루트. 서브트리 밖은 만들 수 없다
  (기획 INVARIANTS에 한 줄).
- 원격에서 페이지가 서브트리 밖으로 이동하면 풀에서 "원격에서 사라짐"으로 처리한다.
  로컬 수정본이 있으면 지금 삭제 경로처럼 3지 선택에 걸린다.

### D5. git 어휘는 세 개다: 저장 · 개발자에게 넘기기 · 반영됨

브랜치·커밋·푸시·PR라는 단어는 기획자 화면에 나오지 않는다. `게시`는 Confluence 것으로
남긴다.

| 버튼/상태 | 실제 동작 |
| --- | --- |
| **저장** | 첫 변경에서 `drafthouse/<YYYYMMDD>-<n>` 브랜치 자동 생성 → `check`(선언된 것만) → commit → push. 지금 `게시`의 diff 리뷰·게이트·실패→Claude 브리프 그대로, 대상 브랜치만 바뀐다. 클론 하나 = 프로젝트 하나라 이름에 슬러그를 넣지 않는다. |
| **개발자에게 넘기기** | `build` 게이트 → GitHub API로 PR 생성(프로젝트 PAT, `repo` 스코프). 제목 = 프로젝트 이름(기획서 제목들은 여러 장이면 한 줄에 안 들어간다), 본문 = 기획서별 Confluence 링크 + `pageId` 토큰 · 화면 목록 · 화면마다 구현한 상태. 이후 저장은 같은 PR에 누적. |
| **반영됨** | 상태 칩을 누르면 그때 PR 상태를 다시 읽는다(폴링하지 않는다) → 머지면 base 풀 → 다음 변경에서 새 브랜치. 트리 배지 `✓ 넘김` → `● 반영됨`. |

- **프로젝트당 열린 PR 1개.** 클론 하나 = 브랜치 하나. 페이지별 브랜치는 worktree가
  필요하고 프리뷰 서버·포트가 배로 늘어 기획자 머신에 과하다. "이번 작업 묶음 = PR
  하나"가 기획자의 정신모델과도 맞는다.
- base가 앞서갔을 때: 세션 시작 풀에서 `merge <baseBranch>`; 충돌이면 지금 `failGate`
  패턴으로 Claude에게 한국어 브리프. 기획자는 "충돌 정리 중" 카드만 본다.
- 온보딩의 레포 게이트가 PAT 스코프(`repo`, PR 생성 가능)까지 검증한다. 지금은 clone만
  되면 통과.
- 개발자 리뷰 코멘트를 도구 안에서 보는 것은 v2. v1은 PR 링크 + 상태(열림/변경
  요청/머지됨).

### D6. 프리뷰는 지금처럼 레포의 dev 서버를 그대로 띄운다

인터랙션은 이미 진짜다 — 레포의 실제 앱이 mock 데이터로 돈다. 부족한 것은 "지금 고른
기획서의 화면으로 가서, 원하는 상태로 보기"뿐이고, 도구가 레포 코드를 파싱하지 않는
원칙은 유지한다.

### D7. 오버레이 메시지 2개 추가

코멘트 봉투와 같은 postMessage 채널(`Preview.tsx`, origin·source 검사 동일):

```
overlay → tool   drafthouse.screens    { screens: [{ route, spec, title, states: ["default","empty","loading","error"] }] }
tool → overlay   drafthouse.navigate   { route, state }
```

- 레포 쪽: 화면 레지스트리 모듈(`meta.spec`·`states`는 `CLAUDE.md` 규약에 이미 있음)을
  오버레이가 로드 시 한 번 쏜다. `connected-repo/`가 참조 구현.
- 프리뷰 툴바 `[화면 ▾] [default|empty|loading|error] [모바일|데스크톱]`. 트리에서
  페이지를 고르면 `spec`이 그 페이지인 화면으로 자동 이동. 상태 칩은 mock 상태 전환
  = "이 상태들을 다 봤다"의 증거.

## 2. 데이터 모델

### 2.1 폴더

D4의 트리. 마이그레이션: `repo.json`이 있고 `projects.json`이 없으면 데몬 기동 시
`default` 프로젝트를 만든다 — `repo/` → `projects/default/repo/` 이동, 미러된
스페이스는 각 스페이스의 홈페이지를 루트로 하는 서브트리로 재해석(= 전체). 테스트
env(`DRAFTHOUSE_REPO_DIR`, `DRAFTHOUSE_REPO_URL`, `DRAFTHOUSE_CONFLUENCE_DIR`)는
`default` 프로젝트의 경로에 매핑되어 픽스처 e2e가 그대로 통과한다.

### 2.2 세션 ↔ 페이지

SDK는 cwd별 트랜스크립트만 안다. 데몬이 사이드카를 갖는다:
`projects/<slug>/sessions.json` = `{ [sessionId]: pageId }`. 설계안은
`{ pageId, workspace, title }`이었는데 뒤 둘은 사이드카가 가질 이유가 없다 — workspace는
어느 트랜스크립트 저장소에서 왔는지가 이미 말하고, title은 트랜스크립트 자신의 것이다.
`session.create`에 `pageId`·`title`(둘 다 optional — 페이지 없는 세션은 "기타" 그룹,
`title`은 도구가 대신 연 스레드를 이름 붙일 때만 쓴다).
`session.list`는 optional `pageId` 필터.

### 2.3 프로토콜 v5

| 메시지 | 변경 |
| --- | --- |
| `project.list` / `project.create` / `project.remove` / `project.activate` | 신규. `create`는 D4 겹침 검증, 클론·설치·미러를 순차 실행하며 phase 이벤트를 흘린다. |
| `session.create` / `session.list` | `pageId?` 추가. 프로젝트는 활성 프로젝트가 암묵. |
| `confluence.sync/pull/push/status` | `space` → 프로젝트 스코프(활성 프로젝트의 roots). |
| `doc.list` | 활성 프로젝트 서브트리. |
| `repo.save` (기존 publish 대체) / `repo.handoff` / `repo.handoffStatus` | D5. |
| `repo.files` | 활성 프로젝트 루트. |
| `preview.navigate` | UI → 데몬이 아니라 UI → iframe postMessage. 프로토콜 밖. |
| `hello.status` | `projects[]`, `activeProject`, 프로젝트별 `repo`/`confluence` 상태. |

README의 "protocol v3/v4" 표기 불일치도 이때 v5로 통일한다.

### 2.4 단계 배지 판정

| 배지 | 소스 |
| --- | --- |
| `○ 기획 중` | 페이지가 미러에 있고 아래 어느 것도 아님 |
| `◐ 화면 있음` | `drafthouse.screens`에 `spec`이 이 페이지 경로인 route 존재 |
| `✓ 넘김` | 열린 PR 본문에 이 pageId |
| `● 반영됨` | 그 PR 머지됨 |

`◉ 인터랙션 완료`는 **넣지 않았다.** 판정하려면 기획서의 `상태` 절을 읽어야 하는데,
기획서를 어떻게 쓰는지는 연결 레포의 결정(`drafthouse.json#planning.rules`)이지 도구의
것이 아니다. 모델의 의견을 배지로 굳히면 도구가 확신하지 않는 것을 확신한 척하게 된다.
대신 넘기기 대화상자에 **넘기기 전 점검** 버튼을 뒀다 — 화면 대화에 "기획서에 적힌 항목과
상태가 화면에 다 있는지" 물어보고, 답은 채팅에 남아 기획자가 반박할 수 있다.

## 3. 단계

한 단계가 끝나야 다음이 흔들리지 않는다. 각 단계는 그 단계의 스위트만 돌린다.

### M1. 프로젝트 모델 + 서브트리 미러 + 폴더 이전 (토대) — **완료**

구현된 것: `projects.ts`(레지스트리·검증·마이그레이션), 프로토콜 v5 `project.*` +
`confluence.pageTree`, 프로젝트별 `RepoWorkspace`/`SyncEngine`, 서브트리 클론/풀,
온보딩 4게이트 재편(Claude · git · Confluence · 프로젝트), 프로젝트 생성 마법사,
좌상단 스위처. 수용 조건은 `pnpm test:projects`(11/11)가 소켓 위에서 그대로 검증한다.

설계에서 실제로 달라진 것 네 가지 — 다음 판이 이 문서만 읽고도 알아야 하는 것들:

- **미러는 프로젝트 폴더 안에 산다** (`projects/<slug>/confluence/<space>/`). §D4가 이미
  그렇게 정했지만, 이유가 하나 늘었다: 프로젝트마다 `.confluence-sync.json`이 따로 있어야
  낙관적 락이 갈라지지 않는다. 그래서 트리에 있던 임의 스페이스 복제 버튼(`SpaceCloner`)은
  제거했다 — 레지스트리가 모르는 폴더는 겹침 검사를 통과한 적이 없는 폴더다. 프로젝트가
  무엇을 덮는지 바꾸려면 프로젝트를 바꾼다.
- **clone과 pull은 페이지 집합을 다른 방법으로 구한다.** clone은 `children` BFS(드물게
  한 번, 큰 스페이스를 통째로 나열하지 않는다), pull은 스페이스 목록을 받아 로컬에서
  서브트리로 좁힌다. `children`은 버전을 주지 않아서 pull이 BFS를 쓰면 매 틱마다 모든
  하위 페이지 본문을 가져와야 했다 — 200페이지 서브트리면 분당 200요청.
- **마이그레이션은 레지스트리와 같은 설정 스코프만 읽는다.** `DRAFTHOUSE_PROJECTS_*`가
  설정된 실행(= 모든 오프라인 스위트)은 개발자의 실제 `~/drafthouse`를 절대 상속하지
  않는다. 이걸 놓쳤을 때 스크래치 데몬이 개발자 본인의 리모트를 웜스타트 클론했다.
- **슬러그는 한국어를 유지한다.** 경로에 위험한 문자만 걷어낸다. `결제` → `결제`.
  폴더 이름은 사람이 언젠가 들여다보는 것이고, 미러는 그 옆에 이미 `회원 관리 기획서.md`를
  쓰고 있다.

### M2. 페이지 축 셸 — **완료**

구현된 것: 탭 제거(`Shell`은 프로젝트 스위처 + 경고 + `PageWorkspace` 하나),
사이드바는 트리만, 채팅 컬럼 위의 세션 탭 스트립(`SessionTabs`), 오른쪽
`문서 | 화면` 세그먼트(`ScreenPanel`), `session.create`/`session.list`의 `pageId`,
프로젝트별 `sessions.json` 사이드카. `PlanningWorkspace`/`DesignWorkspace`는 삭제.

설계에서 실제로 달라진 것:

- **동시 기획 턴에 대기 카드를 넣지 않았다.** 수용 조건에 적어 둔 "두 번째 세션은
  대기"는 코드를 읽기 전의 가정이었다. 실제로는 `currentLock`이 *실행 중인 기획 턴이
  하나라도 있으면* 편집기를 잠그고 백그라운드 풀을 미룬다 — 보호해야 할 불변식은 이미
  지켜지고 있다. 서로 다른 페이지 파일을 고치는 두 세션은 충돌하지 않으며, 터미널에서
  claude 두 개를 띄우는 것과 같다. 큐는 존재하지 않는 제약을 발명하는 일이라 넣지 않았다.
- **`pageId`는 게시 때 따라 옮긴다.** 새 기획서는 `new-…` 로컬 id를 갖고 있다가 게시
  순간 진짜 Confluence id로 바뀐다. `SyncEngine`이 파일과 상태를 다 고친 뒤
  `onPageIdentified(이전, 진짜)`를 부르고 사이드카가 따라간다 — 안 그러면 페이지의 첫
  게시가 그 페이지의 탭을 흩어 버린다. 기획자가 가장 주목하고 있을 바로 그 순간에.
- **`게시` 버튼 이름을 갈랐다.** 탭이 없어지면서 미러 게시와 레포 게시가 한 화면에
  동시에 보인다. `기획서 게시` / `화면 게시`. M3가 후자를 `저장`·`개발자에게 넘기기`로
  다시 이름 붙인다.
- **빈 대화는 페이지를 옮기면 사라진다.** 기존 `discardIfUnused` 규칙이 페이지 전환에도
  적용된다 — 타이핑한 적 없는 대화는 목록의 소음이라는 판단은 그대로다.
- 수용 조건 검증: `test:editor-ui` 27/27(핸드오프가 탭 없이 통과, 페이지 A의 대화가
  페이지 B에 안 보임), `test:projects` 15/15(소켓에서 페이지 귀속·디스크 영속·삭제 정리).

### M3. 저장 · 넘기기 · 반영됨 — **완료**

구현된 것: 브랜치 자동 생성(`drafthouse/<YYYYMMDD>-<n>`), `repo.save`/`repo.handoff`/
`repo.handoffStatus`, `github.ts`(주입 가능한 `RestTransport` 위의 REST 클라이언트),
머지 감지 후 base 복귀·브랜치 리셋, 세션 시작 시 base 병합 + 충돌 → Claude 브리프,
온보딩 프로젝트 게이트의 PAT 권한 검증, `DiffPanel`=저장 검토 + 새 `HandoffPanel`,
`화면` 바의 저장·개발자에게 넘기기·상태 칩. `pnpm test:publish` 36/36.

설계에서 실제로 달라진 것:

- **`build`는 넘기기만 막는다. 저장은 `check`만 통과하면 된다.** 저장마다 풀 빌드를
  물리면 기획자는 저장을 덜 하게 되고, 그건 리뷰 가능한 히스토리의 정반대다. 틀렸을 때
  비싼 쪽은 개발자의 주의를 쓰는 넘기기 쪽이니 빌드는 거기에 둔다. e2e가 양쪽 다 증명한다
  — 빌드가 깨진 상태에서 저장은 성공하고 넘기기는 `failed/build`로 막히며 PR은 열리지 않는다.
- **트랜스포트를 하나로 합쳤다.** `ConfluenceTransport` → `rest-transport.ts`의
  `RestTransport`(+`PATCH`, 응답 헤더). Confluence와 GitHub이 같은 기록-재생 픽스처
  메커니즘을 쓴다 — 순서 있는 매칭과 본문 단언 로직을 두 벌 두면 반드시 어긋난다.
- **사이클 상태는 레지스트리에 산다** (`projects.json`의 `repo.branch`/`repo.handoff`).
  클론이 아니라 레지스트리인 이유: 재클론이 누군가 이미 리뷰 중인 PR을 잃으면 안 되고,
  UI가 레포 워크스페이스가 뜨기 전에도 넘김/반영됨을 보여야 한다.
- **`DRAFTHOUSE_GITHUB_SLUG` 테스트 시임을 추가했다.** 오프라인 스위트의 리모트는 로컬
  bare 레포라 url이 GitHub 프로젝트를 가리킬 수가 없다. `DRAFTHOUSE_REPO_URL`과 같은
  성격의 오버라이드이고 README 표에 그렇게 적었다.
- **PR 상태는 폴링하지 않는다.** 사람이 움직여야만 바뀌는 값을 분당 한 번씩 GitHub에
  물을 이유가 없다. 상태 칩을 누르면 그때 다시 읽는다.

이번에 잡은 실제 버그: **웜 스타트가 활성 프로젝트의 PAT를 무장하지 않았다.** `setPat`은
프로젝트 *전환* 경로에만 있었고 `start()`에는 없어서, 재시작 후 비공개 레포는 기획자가
프로젝트를 바꾸기 전까지 pull도 넘기기도 못 했다. `start()`가 `activateProject`를 거치도록
고쳤다(전환이 없으면 무장만 하고 빠져나온다).

### M4. 화면 이동 · 상태 · 배지 — **완료**

구현된 것: 레지스트리·화면 `meta`의 `spec`(미러 상대 경로)과 `pnpm check`의 일치 검사,
오버레이의 `drafthouse.screens` 송신 + `drafthouse.screens?` 응답 + `drafthouse.navigate`
수신, 프리뷰 툴바(화면 피커·상태 칩·폭 토글), 기획서 선택 → 그 화면으로 자동 이동,
트리 단계 배지, 넘기기 대화상자의 넘기기 전 점검. `pnpm test:comments-ui` 19/19.

설계에서 실제로 달라진 것:

- **`◉ 인터랙션 완료` 배지를 버렸다.** §2.4에 이유를 적었다 — 판정하려면 기획서를
  읽어야 하고, 그건 연결 레포의 영역이다. 넘기기 전 점검 버튼으로 대체했다.
- **`spec`은 Confluence pageId가 아니라 미러 상대 경로다.** 레포가 Confluence id를 들고
  있으면 스페이스 키가 바뀔 때 레포가 깨지고, 도구의 저장 방식이 레포의 문제가 된다.
  경로 → pageId 매핑은 페이지 목록을 가진 셸에서 한다.
- **`drafthouse.screens`는 재시도가 아니라 요청으로 다시 받는다.** 만료되는 재시도 창은
  목록을 영영 잃고 다시 물을 방법이 없다. 요청은 오버레이가 마운트 전일 때만 유실되는데,
  그때는 오버레이 자신의 마운트 송신이 *이미 듣고 있는* 도구에 도착한다. 두 순서가 서로를
  덮는다.
- **자동 이동은 "첫 번째로 일치하는 화면"이다.** 처음 설계는 "정확히 하나 일치할 때만"
  이었는데, 목록+상세가 한 기획서에서 나오는 건 예외가 아니라 보통이다. 보통의 모양에서
  아무 데도 안 가는 규칙은 틀린 규칙이다. 나머지는 화면 피커가 맡는다.
- **폭 토글은 iframe 바깥 CSS만 바꾼다.** 앱에 알리거나 src를 바꾸면 기획자가 화면에
  입력해 둔 것이 날아간다.

이번에 알게 된 것: `test:comments-ui`는 `connected-repo`를 **git clone** 해서 쓴다. 즉
커밋되지 않은 레포 변경은 이 스위트가 절대 보지 못한다 — M2·M3의 어휘 수정까지 포함해
그동안 스테일 HEAD로 돌고 있었다. 레포 변경을 커밋한 뒤에야 M4가 통과했다. 앞으로
`connected-repo`를 고치면 커밋까지가 한 작업이다.

### M5. 실계정 도그푸드 — **완료**

회사 Confluence(정인권 개인 스페이스)·실제 GitHub 레포(`drafthouse-first-repo`)·본인
Claude 구독으로 파이프라인 전체를 한 번 통과시켰다. 기획서 작성 → 게시 → 화면 만들기
→ 저장 → 넘기기까지 실제로 돌았고, 산출물은 Confluence 페이지 `1926987901`과 PR
[#1](https://github.com/inkwonjung-colosseum/drafthouse-first-repo/pull/1)이다.

오프라인 스위트가 한 번도 잡지 못한 것들이 여기서 나왔다. 픽스처 스페이스 키가 늘
`ENG`·`PROD`였고, 포트는 늘 비어 있었고, 첨부는 늘 받아졌기 때문이다.

- **개인 스페이스에서는 제품이 아예 동작하지 않았다.** 개인 스페이스 키는
  `~<accountId>`이고, 미러 폴더를 그 이름으로 만들면 Claude CLI가 `~`로 시작하는 경로
  성분을 홈 참조로 읽어 **자기 cwd 안의 파일을 바깥으로 판정**한다. 그 결과 미러의 모든
  Read가 권한 카드로 뜨고 턴이 멈춘다. SDK 직접 프로브로 `~디렉터리`만 카드가 뜨고
  평범한 디렉터리·이모지 파일명은 멀쩡한 것을 확인했다. 폴더는 `_<accountId>`로 적고
  스페이스 키는 그대로 둔다(`mirrorDirName`/`spaceKeyForDir`). 문서 경로의 첫 성분은
  이제 폴더 이름이고, 엔진의 공개 진입점은 두 표기를 모두 받는다.
- **첨부 404 하나가 클론 전체를 중단시켰다.** 실제 스페이스에는 blob이 사라졌거나
  권한이 막힌 첨부가 쌓인다. 36페이지 중 6페이지에서 멈췄다. 이제 건너뛰고 요약에
  이름을 적는다 — 마크다운이 일이고 이미지는 장식이다.
- **미리보기 포트가 이미 쓰이는 중이면 `exit 0`만 보였다.** 이유를 말하고, 명령의 마지막
  출력 줄을 인용한다.
- **미리보기가 죽으면 저장·넘기기까지 막혔다.** 둘 다 워크트리와 원격에서만 일한다.
  포트를 못 잡은 dev 서버가 이미 끝난 일을 인질로 잡아선 안 된다.
- **PR 초안이 `재고 실사 화면 화면` 제목에 본문은 `http://127.0.0.1:5274`였다.** 루프백
  주소는 PR을 읽는 개발자에게 열리지 않는 링크다. 지웠다.

도그푸드 직후 이어서 잡은 것:

- **핸드오프 스레드 제목이 프롬프트 원문이었다.** 제목은 첫 턴에서 나오는데
  (`session.ts`), 핸드오프의 첫 턴은 **도구가 쓴 문장**이라 탭에 미러 경로가 박혔다.
  `session.create`에 `title`을 붙이고, 핸드오프가 기획서 제목으로 스레드를 연다.
  탭 스트립이 새 셸의 주 네비게이션이라 읽히지 않으면 쓸 수 없다.
- **개발자가 기획서로 갈 방법이 없었다.** PR 본문은 `(pageId: 1926987901)`뿐이고
  `HANDOFF.md`의 `Confluence 원문:` 자리는 **아무도 채울 수 없었다** — Claude는
  siteUrl을 볼 일이 없다. 이제 데몬이 PR 본문에 페이지별 링크를 붙이고(`pageId: <id>`
  토큰은 배지 스캔이 쓰므로 링크 옆에 그대로 둔다), 스킬 템플릿에서 그 자리를 없앴다.
  링크의 URL은 **스페이스 키**로 적는다 — 개인 스페이스의 미러 폴더(`_…`)는 Confluence에
  없는 주소다.
- **PR 본문에 "구현한 상태"가 없었다** (D5). `DrafthouseScreen.states`는 프로토콜에
  있는데 본문이 안 썼다. §2.4가 `◉ 인터랙션 완료` 배지를 버린 대신 내놓은 답이
  "판정하지 말고 선언된 목록을 그대로 넘긴다"였는데, 그 목록이 개발자에게 도달하는
  유일한 통로가 비어 있었다. 이제 화면마다 `— 상태 default · empty`를 적는다.
  구성은 `handoff-draft.ts`로 빼서 단위 검증한다.
- **탭에 "끝났다" 표시가 없었다** (D2). live dot이 사라지기만 해서 "끝났다"와
  "안 돌았다"가 같은 픽셀을 썼다 — 백그라운드로 돌리라고 만든 스트립인데 끝난 걸
  알 방법이 없었다. 보고 있지 않은 탭에서 턴이 끝나면 빈 고리를 찍고, 열면 지운다.
  규칙은 `session-activity.ts#settleTransitions`에 순수 함수로 두고 전이 세 가지를
  단위 검증한다 — 시작 시 idle을 표시하지 않기, 보고 있는 탭은 표시하지 않기,
  사라진 스레드는 기록에서 빠지기.

### M5.5. 저장소 · 배포 파이프라인 — **완료**

(번호가 M5.5인 이유: M6–M9는 아래 기획자 표면 판이 이미 쓰고 있다. 이 단계는
그 판을 시작하기 전에 치워야 했던 토대다.)

M1–M5 전부가 옛 baseline 커밋 하나 위의 미커밋 워킹 트리였다(157파일). 리모트도
브랜치도 없어서 백업·히스토리·CI가 전부 없었고, `RELEASES_REPO`가 리터럴
`"OWNER/REPO"`인 것도 올릴 레포가 없었기 때문이다.

`inkwonjung-colosseum/cds-open-design`(private, `main`)에 올렸다. 한 커밋으로 한
이유는 커밋 메시지에 적었다 — 중간 상태 없는 스냅샷을 M1–M5로 쪼개면 어느 것도
그 시점에 빌드된 적 없는 가짜 히스토리가 되고 bisect가 거짓말을 한다.

올리기 전에 고친 것: 도그푸드 때 쓴 테스트에 실제 accountId·사이트·페이지 ID가
리터럴로 들어가 있었다. 히스토리는 영구라 중립값으로 바꿨다. `connected-repo`도
M4 브리지 커밋이 리모트에 없어서 밀었다 — 실사용에서 화면 피커가 비어 있던 이유가
정확히 그것이었다.

**CI가 처음 돌면서 Windows 전용 실패 두 개가 연달아 나왔다.** 설정만 완전하고 한 번도
실행된 적이 없었으니(§5), 이 머신에서는 재현할 수 없는 종류다. 둘 다 MinGit 번들
스텝이 bash 로 쓰여 있으면서 실제로는 mac 의 bsdtar 를 전제한 데서 왔다:

1. `$RUNNER_TEMP`가 `D:\a\_temp`인데 GNU tar 는 콜론이 든 인자를 `host:path`
   원격 지정으로 읽는다 — "Cannot connect to D:", exit 128.
2. 콜론을 없애도 Git-bash 의 GNU tar 는 **zip 을 못 읽는다** — "This does not look
   like a tar archive". bsdtar 만 읽는다.

`curl`·`sha256sum`은 둘 다 멀쩡히 통과해서 실패는 늘 압축 해제에서만 났다. 스텝을
PowerShell 로 바꿨다(Windows 경로도 zip 도 그냥 다룬다). 세 번째 실행에서
**macOS 2m46s · Windows 4m50s 둘 다 통과**, 설치 파일 477MB/226MB가 나왔다.

업데이트 채널도 정리했다. `RELEASES_REPO`가 실제 레포를 가리키고, 확인 실패가 HTTP
상태를 `exit` 코드라 부르던 것을 고쳤다. 무인증 fetch 라 소스가 private 인 것은
상관없지만 설치 파일을 올린 릴리스는 공개여야 읽힌다 — 404/403 은 이 피드의 정상적인
"아직 배포 안 함"이므로 그렇게 답한다. 일시적 오류처럼 보이면 사람이 계속 다시 누른다.

아직 열려 있는 것:

- **`DESIGN.md`가 없는데 코드 주석 53개가 `DESIGN §N`을 인용한다.** 이번 커밋에
  삭제로 들어갔다(`git show d125b5b:DESIGN.md`로 복구 가능). 복원하든 README·PLAN으로
  재조준하든, 근거 없는 인용을 남겨 둘 수는 없다.
- **다중 서브트리(`roots[]`)가 UI에서 도달 불가능하다.** 마법사는 루트 1개를 하드코딩
  하고(`Onboarding.tsx`), `project.update`는 `roots`를 받지 않는다 — 두 번째 서브트리는
  만들 수도 나중에 추가할 수도 없다. 프로토콜만 배열이다.
- **상위 페이지 드롭다운이 317개**다. M5에서 미룬 그대로이고, 이제 아래 판의
  D14/M9(타이핑 필터 있는 리스트)가 받는다.
- 공개 릴리스 채널이 아직 없다. 소스는 private 이어도 되지만 설치 파일을 올린 릴리스는
  공개여야 업데이트 확인이 읽는다.
- 한 기계에서 Drafthouse 두 벌이 돌면 `preview.port`가 겹친다. 한 사람 한 기계가
  제품 전제라 실사용에선 안 나고, 이제 원인을 정확히 말해 준다.

## 4. 리스크

- **서브트리 이동/재부모화**가 Confluence에서 잦은 팀이면 D4의 "밖으로 이동 = 삭제"가
  체감될 수 있다. 첫 프로젝트에서 관찰하고, 필요하면 "이 페이지가 서브트리를 벗어남"
  알림으로 바꾼다.
- **base merge 충돌을 Claude가 정리**하는 경로는 개발자 코드를 건드릴 수 있다. PR이
  리뷰 지점이므로 허용하되, 충돌 정리 커밋은 메시지에 `[conflict]`를 붙여 리뷰어가
  알아본다.
- **프로젝트당 PR 1개**는 두 기획서를 따로 넘기고 싶은 경우를 막는다. 첫 판은 이대로
  가고, 요구가 실제로 나오면 worktree를 검토한다.
- **`children` BFS**는 페이지 수만큼 요청이 든다. 서브트리 수백 페이지면 첫 클론이
  분 단위. 진행률은 phase 이벤트로 이미 흘린다.
- 프로토콜 v5는 데스크톱 배포와 함께 나가야 한다(데몬이 인프로세스이므로 버전
  불일치는 개발 경로에서만).

## 5. 열린 항목

- Anthropic 계정 담당자에게 서면 확인: 내부 도구에서 Claude Code CLI를 사용자 본인
  로그인으로 구동하는 것(README §Policy). 롤아웃 전.
- PR 리뷰 코멘트를 도구 안에서 보고 Claude 턴으로 넘기기 (v2).
- 한 프로젝트가 서브트리 여럿(`roots[]`)을 갖는 경우 — 모델만 배열이다. 마법사는
  루트 1개를 하드코딩하고 `project.update`는 `roots`를 받지 않아, 두 번째 서브트리는
  만들 수도 나중에 추가할 수도 없다(M5.5에서 확인).
- ~~Windows 빌드 미실행~~ — M5.5에서 첫 CI 실행으로 통과. 남은 것은 공개 릴리스 채널뿐.

---

# PLAN M6–M9 — 기획자 표면: 비개발자 UX 전면 개선

M1–M5는 엔진과 스코프를 바꿨다. 이 판은 **표면**을 바꾼다. 엔진(미러·게이트·브랜치·
오버레이 채널)과 README §What shipped의 계약은 그대로 두고, 기획자가 보는 픽셀과
문장만 다시 만든다. 결정 번호는 D8부터 잇는다.

## 6. 진단 — 지금 표면의 어디가 개발자 것인가

코드와 e2e 스크린샷(`packages/web/test/*.png`, 2026-09-09)에서 확인한 것. 실제 기획자
관찰은 아직 없다(M5 도그푸드는 작성자 본인) — §9의 첫 항목이 그것이다.

1. **"지금 무엇을 해야 하나"를 말하는 층이 없다.** 기획서 게시는 왼쪽 사이드바 머리,
   이 문서로 화면 만들기는 편집기 바, 저장·개발자에게 넘기기는 오른쪽 상단. 트리 배지
   `○ ◐ ✓ ●`의 뜻은 hover 툴팁에만 있다. 기획서 0장인 프로젝트에서 `+ 새 기획`은
   페이지를 골라야 켜지는데 고를 페이지가 없다.
2. **채팅에 기계 텍스트가 그대로 보인다.** 코멘트 핀 봉투는 CSS 경로와 JSON 원문으로
   렌더링된다(`ScreenPanel.tsx#commentsToTurn`, ui-comments 스크린샷). 화면 만들기
   초안은 `@confluence/ENG/….md`로 시작한다. Claude 답변에는
   `src/screens/…screen.tsx`, `onSelectMember`가 나온다 — 연결 레포 `CLAUDE.md`가
   한국어는 강제하지만 "파일 경로 대신 화면 이름"은 강제하지 않는다.
3. **컴포저가 개발자 설정을 노출한다.** 모델·생각 시간·권한 모드 칩이 매 대화에 붙고,
   그중 전부 맡기기는 `bypassPermissions`다. 사용량 필 두 개는 읽는 법을 알아야 한다.
4. **저장 검토가 git diff 원문이다.** `DiffPanel`은 파일 경로와 +/- 헝크를 보인다.
   기획자는 이것을 읽고 승인할 수 없다. 저장 메모는 커밋 메시지다.
5. **개발자 문자열이 상시 노출된다.** 헤더 "데몬: 연결됨 · <레포 url>", 미리보기 툴바의
   `http://127.0.0.1:<port>`, 상태 칩 `default/empty/loading/error`, 설정의 프로토콜
   버전·pnpm 가용성·운영체제.
6. **시각 체계가 콘솔이다.** `styles.css` 머리말이 "session log처럼 읽히는 콘솔"을
   목표로 적고 있다. 어두운 기본 테마, 버튼 12.5px 모노스페이스, 낮은 대비의 muted,
   반응형 분기 880px 하나. 1440px 노트북에서 세 컬럼이 좁다.
7. **되돌리기가 없다.** 화면을 망쳤을 때 이전 저장 시점으로 가는 UI가 없다. 비개발자의
   신뢰는 "잘못돼도 돌릴 수 있다"에서 시작한다.
8. **설정이 토큰 발급을 전제한다.** 레포 PAT·Confluence API 토큰 발급 경로 안내가
   없고, 상위 페이지 드롭다운 317개는 M5에서 미룬 그대로다.

## 7. 결정

### D8. 페이지 위에 단계가 놓인다 — 스테퍼 하나, 주 버튼 하나

오른쪽 컬럼 맨 위, `문서 | 화면` 세그먼트 위에 선택된 기획서의 진행 단계를 놓는다.

```
기획서 쓰기 ─ 게시 ─ 화면 만들기 ─ 검토·수정 ─ 저장 ─ 개발자에게 넘기기 ─ 반영됨
                          ●
                   [ 이 문서로 화면 만들기 ]        저장 기록 · 더 보기 ▾
```

| 단계 | 판정 소스(전부 기계적) | 주 버튼 |
| --- | --- | --- |
| 기획서 쓰기 | 페이지가 `isNew`이거나 본문이 비어 있음 | 기획 대화 열기 |
| 게시 | 페이지 `modified` 또는 `isNew` (트리의 수정됨/신규와 같은 값) | 기획서 게시 |
| 화면 만들기 | 게시됨 + `drafthouse.screens`에 `spec`이 이 페이지인 route 없음 | 이 문서로 화면 만들기 |
| 검토·수정 | 화면 있음 + `repo.status.pendingChanges > 0` (아래) | 저장 |
| 저장 | 화면 있음 + 변경 없음 + `repo.branch` 있음 + 넘김 아님 | 개발자에게 넘기기 |
| 개발자에게 넘기기 | `handoff.state ∈ {open, changes_requested}` | (상태 칩) 상태 다시 확인 |
| 반영됨 | `handoff.state = merged` | 새 사이클 안내 |

- 판정은 `packages/web/src/stage.ts#deriveStage(input)` 순수 함수 하나. 입력은
  `{ page, screens, pendingChanges, branch, handoff, threads }`, 출력은
  `{ step, primary, reasons }`. §2.4의 배지 판정을 이 함수가 흡수하고 `PageTree`는
  같은 함수의 `step`을 읽는다 — 배지와 스테퍼가 어긋날 길을 없앤다.
- `repo.status`에 `pendingChanges: number`를 추가한다. 데몬이 디자인 턴이 끝날 때와
  저장이 끝날 때 `git status --porcelain`을 한 번 돌려 개수를 실어 보낸다. 폴링 없음.
- 기획서 게시(사이드바), 이 문서로 화면 만들기(편집기 바), 저장·넘기기(미리보기 바)
  버튼은 **스테퍼로 이동**하고 원래 자리에서 없어진다. 단계와 무관하게 필요한 동작은
  `더 보기 ▾`(기획서 게시·저장·넘기기·저장 기록·Confluence에서 보기)에 항상 있다.
- `◉ 인터랙션 완료`는 여전히 없다(§2.4). 검토·수정 단계의 주 버튼 옆에 넘기기 전
  점검을 보조 버튼으로 둔다.

### D9. 기계 텍스트는 카드로 — 마커 한 줄로 번역한다

SDK 트랜스크립트는 사용자 메시지를 텍스트로만 저장한다. 사이드카를 하나 더 두지 않고
**턴 본문 첫 줄에 HTML 주석 마커**를 심는다:

```
<!-- drafthouse:comments {"screen":"member/MemberList","state":"default","count":2} -->
화면 수정 요청 2건 — …(지금과 같은 본문)
```

- `Transcript`가 사용자 블록 첫 줄의 `<!-- drafthouse:<kind> {json} -->`를 읽어 카드로
  그린다. 마커 종류: `comments`(핀 카드: 번호·요소 텍스트·요청만), `brief`(기획서 칩 +
  "이 기획서로 화면을 만들어 주세요"), `precheck`(대조 요청 카드), `gate`(게이트 실패
  브리프 — 데몬 `GATE_BRIEF`가 붙인다). 마커 없는 메시지는 지금처럼.
- 원문(경로·rect·JSON)은 카드의 `자세히` 접힘 안에 남는다. Claude에게 가는 텍스트는
  바뀌지 않는다 — 마커는 Claude가 무시하는 주석이다.
- 파서는 `packages/web/src/turn-marker.ts` 순수 함수, 단위 테스트. 재생(resume)된
  트랜스크립트도 같은 텍스트라 같은 카드가 나온다.
- **연결 레포 `CLAUDE.md` §답변 형식**에 한 줄 추가: "기획자에게 답할 때 파일 경로·
  컴포넌트·콜백·prop 이름을 쓰지 않는다. 화면 이름과 화면에 보이는 항목 이름으로 말한다.
  파일 이름은 `HANDOFF.md`에만 적는다." `workspaces.ts#INVARIANTS`(기획 워크스페이스)
  에도 같은 취지의 한 줄: 미러 경로 대신 페이지 제목으로 말한다. 연결 레포 변경은
  커밋까지가 한 작업이다(M4 교훈).
- `activityLine`의 어휘를 기획자 것으로: "파일 3개 생성" → "화면 파일 3개 작업",
  "조회 5개" → "5곳 확인", "명령 1개 실행" → "검사 1회 실행".

### D10. 고급 설정은 설정 대화상자에 있고 대화에는 없다

- 컴포저의 모델·생각 시간·권한 모드 칩을 뗀다. 세 값은 `SettingsDialog`의 새 그룹
  **대화**로 가고, 기본값은 `default / 보통 / 물어보고 진행`. 컴포저에는 현재 값이
  기본과 다를 때만 작은 `⋯` 칩이 떠서 같은 세 선택기를 팝오버로 보인다.
- `bypassPermissions`(전부 맡기기)는 설정에서만, 경고 문장과 함께. 컴포저 팝오버에는
  없다.
- 사용량 필 두 개는 칩 하나 `사용량`으로 합친다. 색(초록·노랑·빨강)이 상태, 클릭하면
  5시간·주간·대화 길이 세 줄을 팝오버로. `ContextRing`은 그 팝오버 안으로.
- `/명령` 팔레트는 유지(이미 기획자 어휘).

### D11. 저장은 요약으로 읽는다

`DiffPanel` → `SaveReview`. 위에서 아래로: 요약 목록 → 저장 메모(자동 채움) →
`자세히 보기`(지금의 파일·헝크). 요약은 두 소스에서 온다:

1. **`repo.summarize`**(신규): 데몬이 Claude CLI를 `-p`(비대화) 모드로 한 번 돌려,
   diff(파일별 헝크, 4KB 상한으로 잘라서)와 오버레이가 선언한 화면 목록을 주고
   "바뀐 화면과 바뀐 점을 기획자 말로 3줄 이내, 파일 이름 없이" 받는다. 구독 사용량을
   쓰므로 저장 검토를 **열 때 한 번**만 돌리고, 결과는 diff 해시로 캐시한다.
2. **폴백**: CLI가 실패하거나 3초를 넘기면 파일 경로의 두 번째 폴더(예: `member`)로
   묶어 "member: 수정 2 · 추가 1" 목록을 보인다. 이 폴백은 연결 레포의 폴더 규약을
   *읽는* 것이 아니라 경로 문자열을 자르는 것이라 계약 위반이 아니다.

- 저장 메모는 요약 첫 줄로 자동 채움. 기획자가 고칠 수 있다.
- 도구는 여전히 레포 코드를 파싱하지 않는다(D6). 요약을 쓰는 것은 모델이고 화면에는
  "요약은 Claude가 썼습니다"라고 적는다.

### D12. 저장 기록과 되돌리기 — 역사는 다시 쓰지 않는다

- **`repo.history`**(신규): `git log <base>..HEAD` — 이번 사이클 브랜치의 커밋 목록
  `{ sha, message, at, files }`. 사이클이 없으면 빈 목록.
- **`repo.restore { sha }`**(신규): 그 시점의 트리를 **새 커밋**으로 되살린다
  (`git checkout <sha> -- . && git commit -m "되돌리기: <원 메시지>"` 후 push).
  reset·revert·force-push는 쓰지 않는다 — 개발자가 이미 그 PR을 보고 있을 수 있다.
  워크트리에 저장 안 된 변경이 있으면 거부하고 "먼저 저장하거나 변경을 버려 주세요".
- **`repo.discard`**(신규): 저장 안 된 변경 버리기. 저장 정책이 허용한 경로만
  `git checkout -- <paths>` + `git clean -fd -- <paths>`. 확인 대화상자 필수, 되돌릴
  수 없다고 명시.
- UI: 스테퍼의 `저장 기록` 드로어. 항목마다 메시지·시각·"이 시점으로 되돌리기".
  되돌리기가 끝나면 미리보기는 dev 서버 핫리로드로 따라온다.
- 단어: 커밋·리셋은 나오지 않는다. "저장 기록", "되돌리기", "변경 버리기".

### D13. 보기 — 나란히, 밝게, 접기

- 세그먼트에 세 번째 값 **나란히**. 문서와 화면을 1:1로 놓고, 둘 다 마운트 상태를
  유지한다(지금도 둘 다 마운트돼 있다). 뷰포트 1440px 미만에서는 나란히를 숨긴다.
- 왼쪽 트리는 1440px 미만에서 아이콘 레일로 접힌다(클릭으로 펼침). 채팅 컬럼은 접기
  버튼으로 최소 폭으로 줄일 수 있다 — 검토 때 문서+화면을 넓게 보기 위해서.
- 시각 체계 교체. `styles.css` 머리말을 다시 쓴다: "문서 도구. 밝은 종이, 산세리프,
  넉넉한 간격. 모노스페이스는 코드와 diff 안에서만." 기본 테마 밝게(새 설치 기준;
  저장된 설정은 유지), 본문 15px, 보조 13.5px, 캡션 12px, 버튼도 `--sans`,
  `--muted`는 WCAG AA(4.5:1) 통과 값으로. 다크 테마는 같은 구조의 팔레트 스왑으로
  유지한다.
- 헤더: `Drafthouse · [프로젝트 ▾] · <선택된 기획서 제목>` 과 연결 상태 아이콘 하나.
  데몬·레포 url 문자열은 헤더에서 없어지고 설정 → 문제 해결로 간다.
- 미리보기 툴바: 상태 칩 한국어(`기본 · 비어 있음 · 불러오는 중 · 오류`, 그 밖의 상태는
  원문 그대로), URL 문자열 제거(`새 창` 링크는 유지).

### D14. 시작하기와 빠른 동작

- 기획서 0장 프로젝트: 트리·탭·문서 빈 상태를 하나로 합쳐 오른쪽 컬럼에 **첫 기획서
  만들기** 버튼 하나. 누르면 기획 대화가 열리고, 컴포저에 `planning.rules`의 절
  순서를 뼈대로 한 첫 턴이 채워진다(보내지는 않는다 — 핸드오프와 같은 규칙).
  `planning.rules`는 데몬이 이미 읽으므로 `hello.status`에 `planningRules`로 실어 준다.
- 컴포저 위 **빠른 동작 칩**(클릭 = 문장 삽입, 자동 전송 없음):
  - 기획: `기획서 뼈대로 새로 쓰기` · `상태 절 채우기` · `화면 목록 표로 정리`
  - 화면: `빈 상태 추가` · `로딩 상태 추가` · `오류 상태 추가` · `기획서와 대조`
  - 목록은 도구 상수. 연결 레포가 `drafthouse.json#quickActions`로 덧붙일 수 있게
    스키마만 열어 둔다(선택 필드, 이번 판에서 UI만 읽는다).
- 온보딩: 토큰 입력 옆에 발급 페이지 직링크와 필요한 권한 체크리스트
  (GitHub PAT: `repo`, 비공개 패키지면 `read:packages`; Atlassian API 토큰). 상위
  페이지 드롭다운은 타이핑 필터 있는 리스트로. 프로젝트 만들기의 phase 이벤트를
  진행 표시줄로(M1이 이미 흘려 준다).
- 알림: 보고 있지 않은 탭·창에서 턴이 끝나면 `Notification`(웹) / Electron
  `Notification`(데스크톱, preload 브리지 한 줄). 넘김 상태는 창 포커스 복귀 시 한 번
  다시 읽고 바뀌었을 때만 알린다 — M3의 "폴링하지 않는다"는 유지된다.

### D15. 도구 UI를 CDS로 옮기지 않는다

검토했고 하지 않는다. 이유 셋. (1) 도구는 도메인 중립이어야 한다(README: "Everything
domain-shaped is decided by the connected repo"). 도구 자체가 회사 디자인 시스템에
의존하면 다른 레포·다른 회사에 못 쓴다. (2) CDS는 비공개 레지스트리다. 도구 빌드가
`read:packages` 인증을 요구하게 된다. (3) CDS는 관리자 셸 어휘다. 문서 도구에 맞는
밀도가 아니다. 대신 D13의 자체 토큰을 다시 짠다. 나중에 미리보기와 도구의 시각 통일이
필요해지면 **테마 토큰만** 연결 레포가 `drafthouse.json#theme`로 넘기는 방식을 검토한다.

## 8. 프로토콜 v6

| 메시지 | 변경 |
| --- | --- |
| `hello.status` | `planningRules: string \| null`, `quickActions?: string[]` |
| `repo.status` | `pendingChanges: number` |
| `repo.summarize` | 신규 → `{ lines: string[]; source: "claude" \| "fallback" }` |
| `repo.history` | 신규 → `{ base: string; entries: { sha; message; at; files: string[] }[] }` |
| `repo.restore` | 신규 `{ sha }` → `diff.status` 스트림 재사용(stage `pushing` → `published`) |
| `repo.discard` | 신규 → `{ removed: string[] }` |
| `session.send` | 변경 없음 — 마커는 텍스트 안에 있다(D9) |

기존 메시지의 형태는 바뀌지 않으므로 데스크톱과 함께 나가면 된다(§4 마지막 항목과 같다).

## 9. 단계

각 단계는 그 단계의 스위트만 돌리고, 끝나면 "설계에서 실제로 달라진 것"을 M1–M5처럼 적는다.

### M6. 어휘·카드 (구조 변경 없음) — **완료**

구현된 것: `protocol/turn-marker.ts`(마커 형식·파서), 트랜스크립트의 `MachineTurn`
카드 4종, 네 군데 발신부(코멘트·brief·precheck·gate)의 마커, 기획서 칩,
컴포저에서 모델·생각 시간·권한 칩 제거 + `⋯` 칩, 사용량 칩 하나, 설정의 `대화`
그룹과 `문제 해결` 접힘, 헤더·미리보기 툴바 정리, 한국어 상태 칩,
`workspaces.ts#INVARIANTS`와 연결 레포 `CLAUDE.md`의 답변 형식 규칙.
`turn-marker` 11/11, `workspaces` 7/7, `ui-settings` 23/23, `ui-editor` 33/33,
`ui-comments` 26/26, `publish` 38/38. 오프라인 스위트 전부 통과.

설계에서 실제로 달라진 것 — 다음 판이 이 문서만 읽고도 알아야 하는 것들:

- **마커 모듈은 `packages/web`이 아니라 `packages/protocol`에 있다.** 게이트 실패
  브리프는 데몬이 쓴다(`repo.ts#failGate`). 웹에 두면 같은 형식을 두 벌 구현하게 되고,
  M3에서 트랜스포트를 하나로 합친 것과 같은 이유로 반드시 어긋난다. 웹은 읽고
  데몬은 쓰지만, 형식은 한 파일이다.
- **핸드오프 초안이 텍스트가 아니라 칩이 됐다.** 원래는 `@confluence/…`를 컴포저에
  채워 넣고 기획자가 그 앞에 말을 덧붙이는 방식이었다. 마커를 텍스트 맨 앞에 넣으면
  기획자가 입력란에서 HTML 주석을 보게 된다 — 그래서 기획서는 인용 칩과 같은 자리에
  칩으로 두고, `PageWorkspace.submit`이 보낼 때 마커와 `@confluence/` 참조를 붙인다.
  이미 인용이 쓰던 패턴이라 새 개념이 아니다. 부수 효과가 하나 좋다: 경로를 반쯤
  지워서 참조를 깨뜨리는 일이 불가능해졌다.
- **마커가 붙은 턴은 스레드 이름이 되지 않는다** (`session.ts`). 처음엔 제목에서
  마커만 벗겨 냈는데, 그러면 코멘트 봉투의 첫 줄(`화면 수정 요청 2건 —
  member/MemberList …`)이 탭 이름이 됐다. M5가 이미 고쳤던 버그를 마커로 다시
  만든 셈이다. 규칙을 뒤집었다 — 도구가 쓴 턴은 이름을 짓지 않고, 도구가 여는
  스레드는 `session.create`의 `title`로 이름을 받는다.
- **모델·생각 시간·권한이 워크스페이스별에서 하나로 합쳐졌다.** 저장 위치가
  `drafthouse.composer.<workspace>` 두 개에서 `settings.chat` 하나가 됐고, 예전 키는
  첫 로드에서 한 번 읽어 옮긴다(기획 쪽이 이긴다). 설정 대화상자는 하나뿐인데 같은
  질문을 두 번 시키는 것은 기획자가 다르게 답할 근거가 없는 질문이다.
- **`bypassPermissions`만 복원되지 않는다.** 나머지 두 모드는 저장한다. 예전 코드가
  권한 모드 전체를 저장하지 않았던 이유("리로드를 넘어 살아남은 전체 허용은 아무도
  넓히려 하지 않은 세션을 넓힌다")는 전부 맡기기 하나에만 해당한다. `loadSettings`가
  필드 단위로 되돌리는 기존 패턴 그대로다.
- **설정 변경은 열려 있는 대화에도 닿는다.** 데몬이 세션별 사본을 들고 있어서,
  안 그러면 기획자가 `화면 수정은 바로`를 켠 바로 그 대화에서 계속 카드를 받는다.
  값이 바뀐 경우에만 밀어 넣는다(`useSessions`의 `pushed` ref) — 스레드를 여는 것은
  이미 `startSession`이 처리한다.
- **수용 조건의 grep은 두 곳에서 0건이 아니다.** `Shell.tsx`의 주석(무엇을 왜 지웠는지
  적은 것)과 `App.tsx`의 브라우저 연결 화면이다. 후자는 개발자에게 `pnpm dev:daemon`을
  치라고 안내하는 화면이라 데몬이라는 단어가 정확하다 — 데스크톱 앱에는 없는 화면이다.
  기획자가 보는 표면에서는 0건이다.

원래 계획은 아래와 같았다.

D9, D10, D13의 문자열 부분(상태 칩 한국어·URL 제거·헤더 정리), 연결 레포 `CLAUDE.md`.

- 파일: `components.tsx`(카드 렌더), 신규 `turn-marker.ts`, `ScreenPanel.tsx`
  (`commentsToTurn`에 마커), `PageWorkspace.tsx`(brief·precheck 마커), `Composer.tsx`
  (칩 제거·`⋯`·사용량 칩), `SettingsDialog.tsx`(대화 그룹, 문제 해결 접힘),
  `Shell.tsx`(헤더), `Preview.tsx`(상태 라벨·URL), `repo.ts#GATE_BRIEF`(gate 마커),
  `workspaces.ts#INVARIANTS`, `connected-repo/CLAUDE.md`(+커밋).
- 테스트: `turn-marker.test.mts` 신규(마커 파싱·없을 때 통과·깨진 JSON은 원문);
  `ui-comments-e2e` — 핀 전송 후 트랜스크립트에 `수정 요청 2건` 카드가 있고 CSS 경로
  문자열이 **보이지 않는다**; `ui-editor-e2e` — 핸드오프 초안이 칩으로 보이고 전송 텍스트는
  그대로 `@confluence/…`; `ui-settings-e2e` — 대화 그룹의 세 값이 저장·복원된다;
  `workspaces.test.mjs` — INVARIANTS 문장 포함.
- 수용: 스크린샷 3장(ui-comments·ui-editor·ui-settings)에서 파일 경로·URL·영문 상태명·
  "데몬"이 한 글자도 없다. `grep -n "데몬\|127.0.0.1" packages/web/src/*.tsx`가
  설정의 문제 해결 섹션 밖에서 0건.

### M6.5. 기획자 관찰 (반나절, 코드 없음)

M6 빌드로 기획자 2~3명에게 세 여정을 시킨다: 첫 설정, 기획서에서 첫 화면, 수정 요청에서
넘기기. 멈춘 지점·물어본 질문·쓴 단어만 적는다. M7의 스테퍼 단어와 순서는 이 기록으로
확정한다 — 아래 표는 초안이다.

### M7. 구조·시각 (약 2주)

D8, D13(나란히·접기·팔레트), D14의 빈 상태.

- 파일: 신규 `stage.ts` + `StageBar.tsx`, `PageWorkspace.tsx`(스테퍼 마운트·버튼
  이동·세그먼트 3값·빈 상태), `PageTree.tsx`(배지를 `deriveStage`로), `DocEditor.tsx`
  (핸드오프 버튼 제거), `ScreenPanel.tsx`(저장·넘기기 버튼 제거, 바는 미리보기 툴바만),
  `Shell.tsx`(접기), `styles.css`(토큰·타입 스케일·분기 1280/1440·나란히 그리드),
  `settings.ts`(기본 테마), 데몬 `repo.ts`/`server.ts`(`pendingChanges`).
- 테스트: `stage.test.mts` 신규 — 표의 7행 각각 + 경계(게시 전 화면 있음은 "게시"가
  이긴다, 넘김 뒤 새 변경은 "검토·수정"으로 돌아온다); `ui-editor-e2e`·`ui-publish-e2e`·
  `ui-comments-e2e`의 버튼 셀렉터를 스테퍼로 옮긴다(테스트 이름은 유지); `ui-publish-e2e`
  에 "디자인 턴 뒤 `pendingChanges`가 0보다 크고 저장 뒤 0" 추가; `ui-settings-e2e`에
  밝은 테마 기본 스크린샷.
- 수용: 어느 시점에도 주 버튼은 하나이고 그 라벨이 표의 단계와 일치한다.
  1280px 뷰포트 스크린샷에서 세 컬럼이 겹치지 않는다.

### M8. 기능 — 저장 요약·저장 기록·빠른 동작 (약 2주)

D11, D12, D14의 빠른 동작. 프로토콜 v6.

- 파일: 데몬 `repo.ts`(`summarize`·`history`·`restore`·`discard`), `server.ts`,
  `protocol/index.ts`; 웹 `SaveReview.tsx`(DiffPanel 대체), 신규 `HistoryDrawer.tsx`,
  `Composer.tsx`(빠른 동작 칩), `daemon-client.ts`.
- 테스트: `repo.test.mjs` — 요약 폴백 묶기(경로 → 폴더 그룹), 마커 붙은 gate 브리프;
  `publish-e2e` — 두 번 저장 → `repo.history` 2건, `restore(첫 sha)` → 새 커밋 3번째,
  PR 브랜치 히스토리는 앞으로만 자란다(`git log --oneline | wc -l` = 3), 더티 워크트리
  에서 `restore` 거부, `discard`는 허용 경로만 지운다; `ui-publish-e2e` — 저장 검토를
  열면 요약 목록(폴백)이 diff보다 위에 있고 `자세히 보기` 전에는 `+`/`-` 줄이 없다.
  `summarize`의 Claude 경로는 `test:daemon`(실 Claude) 한 케이스만.
- 수용: 저장 검토 첫 화면에 파일 경로가 없다. 되돌리기 뒤 미리보기가 그 시점 화면을
  보인다(comments-ui의 실제 dev 서버로 확인).

### M9. 설정·알림 (약 1주)

D14의 온보딩·알림, 설정 재편 마무리.

- 파일: `Onboarding.tsx`(토큰 도움말·필터 리스트·진행 표시), `SettingsDialog.tsx`,
  신규 `notify.ts`, 데스크톱 `preload.ts`/`main.ts`(Notification 브리지),
  `useSessions.ts`(settle 시 알림 훅 — `session-activity.ts#settleTransitions`가 이미
  "보고 있지 않은 탭에서 끝남"을 알고 있다), `ScreenPanel.tsx`(포커스 복귀 시 넘김 재확인).
- 테스트: `ui-onboarding-e2e` — 317개 픽스처에서 타이핑 3글자로 1행; 도움말 링크의
  href가 GitHub·Atlassian 토큰 페이지; `session-activity.test.mts` — 알림 전이 추가;
  `desktop.test.mjs` — 브리지 존재.
- 수용: 기획자가 터미널·GitHub 설정 화면을 열지 않고 온보딩을 끝낼 수 있는지 M6.5의
  같은 사람에게 한 번 더 시킨다.

## 10. 리스크

- **요약에 구독 사용량이 든다**(D11). 저장 검토를 열 때 한 번, diff 해시로 캐시.
  한도가 찼을 때는 폴백이 뜨고 "지금은 요약을 만들 수 없어 파일 목록을 보입니다"라고
  말한다.
- **되돌리기가 PR 히스토리를 길게 만든다**(D12). 의도한 것이다 — 리뷰 중인 브랜치를
  다시 쓰는 것보다 낫다. 커밋 메시지가 `되돌리기:`로 시작해 리뷰어가 알아본다.
- **마커를 Claude가 따라 쓴다**(D9). 답변에 `<!-- drafthouse:… -->`가 나오면 사용자
  블록에서만 파싱하므로 카드가 되지 않고 마크다운 렌더러가 주석을 숨긴다. INVARIANTS에
  "이 주석은 쓰지 않는다" 한 줄을 넣어 둔다.
- **스테퍼 판정이 틀리면 주 버튼이 틀린다**(D8). `deriveStage`가 순수 함수이고
  `더 보기 ▾`에 모든 동작이 항상 있으므로 틀려도 막히지는 않는다. 어긋난 사례는
  단위 테스트 행으로 추가한다.
- **밝은 기본 테마로 바꾸면 기존 스크린샷 기반 기대가 전부 깨진다.** e2e는 픽셀 비교를
  하지 않으므로 코드상 영향은 없다. README의 스크린샷 문장만 갱신한다.
- **M6.5 관찰을 건너뛰고 M7로 가고 싶어질 것이다.** 스테퍼의 단어 7개는 이 도구에서
  가장 자주 읽히는 문장이 된다. 반나절이다.

## 11. 열린 항목 (이 판 이후)

- 코멘트 v2: 핀 주변 스크린샷 크롭 첨부, 화면별 코멘트 이력, 넘기기 전 점검 답변을
  고정 형식(체크리스트)으로 받기.
- 개발자 리뷰 코멘트를 도구 안에서 보고 Claude 턴으로 넘기기(§5에서 이어짐).
- 프로젝트 정의 가져오기: 개발자가 만든 `project.json`/링크로 기획자는 이름만 고른다.
- `drafthouse.json#quickActions`·`#theme`를 연결 레포가 실제로 쓰는 첫 사례.
- Anthropic 계정 담당자 서면 확인(§5 첫 항목)은 여전히 롤아웃 차단 항목이다.
