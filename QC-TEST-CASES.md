# Colo Design 전체 기능 QC 테스트 케이스

> 기준: main (desktop v0.3.14) · 작성 2026-09-26
> 대상 환경: **omp 전용**(개발 실행에서만 노출, `COLO_DESIGN_DEV_AGENTS=1`) —
> 모델은 **`glm-5.3` 계열**(빠른 응답은 `빠르게` 토글)과 **`swe-2`** 만 사용.
> 연결 레포: 개인 레포 **`cds-design-first-repo`**(정상 시나리오 전반) ·
> **`cds-design-second-repo`**(병행/전환 · 부정 시나리오) — PR·코멘트·머지 등 무제한 가공 허용.

---

## 0. QC 환경 준비 (모든 TC의 공통 사전 작업)

### 0.1 사전 준비물 체크리스트

| # | 항목 | 확인 방법 |
|---|---|---|
| 1 | Node 22+ · pnpm · git | `node -v` · `pnpm -v` · `git --version` |
| 2 | omp CLI 설치 & 로그인 | `omp --version`, 대화 한 번 응답하는지 |
| 3 | omp 모델 확인 | `omp models --json` 에 `glm-5.3` 계열과 `swe-2` 행 존재 |
| 4 | classic PAT 1개(무기한, `repo` 스코프 — 2026-09-26 검증) | 셸에서 `export QC_PAT=ghp_…` 한 번. 읽기전용·만료임박 토큰은 없음 → A-11 Block, A-09는 무기한 문구만 확인. A-10용 가짜 토큰 문자열은 별도(아무 값) |
| 5 | 두 레포에 push 권한 · 기본 가지에 `dev` 스크립트 + 커밋된 락파일 | `package.json` scripts.dev, `pnpm-lock.yaml` 존재 |
| 6 | Claude/Codex 미설치 상태 유지(선택) | 설정 화면의 `쓸 수 없는 AI N` 폴드 TC를 위해 미설치 권장 |
| 7 | 빌드 | `pnpm install && pnpm build` |

### 0.2 실행 경로 (둘 다 돌려야 함 — TC에 경로 지정)

```bash
# 경로 A — 브라우저 개발 경로
pnpm dev:daemon        # 출력의 client url(ws://…?token=…) 복사
pnpm dev:web           # http://127.0.0.1:29173 접속 → ConnectScreen.tsx 또는 열린 붙여넣기 칸에 url 붙여넣기

# 경로 B — 데스크톱(HMR)
pnpm dev:desktop       # 웹만 HMR. main/preload 고치면 재빌드·재시작
# 릴리스와 같은 코드 경로가 필요할 때: pnpm --filter @colo-design/desktop dev
```

- `dev:daemon`/`dev:desktop`이 `COLO_DESIGN_DEV_AGENTS=1`을 스스로 켜므로 omp가 프로바이더 목록에 오른다. (패키징된 앱에서는 절대 노출되지 않음 — 체크 TC: M-06)
- 게이트(턴 끝 화면 확인)는 **미리보기 창이 있는 데스크톱 경로에서만** 동작한다. 브라우저 경로에서는 게이트가 조용히 비활성(AGENTS.md 명시).

### 0.3 초대 파일 만들기

```bash
# 초대장 1 — 정상(두 레포 동시, 첫 실행 TC용): 첫 프로젝트만 열리고 나머지는 등록만
node scripts/make-invite.mjs \
  --repo https://github.com/inkwonjung-colosseum/cds-design-first-repo \
  --repo https://github.com/inkwonjung-colosseum/cds-design-second-repo \
  --token $QC_PAT --author "QC-테스터" \
  --reviewer inkwonjung-colosseum --provider omp --model glm-5.3 --effort medium \
  --out qc-main.colo-invite

# 초대장 2 — 재수신(새로/바뀜/그대로 판정용): 두 번째 레포의 base만 바꿔 만듦
#   (second-repo에 미리 broken 같은 이름의 분리 브랜치를 하나 밀아 둔다)
node scripts/make-invite.mjs \
  --repo https://github.com/inkwonjung-colosseum/cds-design-first-repo \
  --repo https://github.com/inkwonjung-colosseum/cds-design-second-repo --base broken \
  --token $QC_PAT --author "QC-테스터" --out qc-renew.colo-invite

# 초대장 3 — 가짜 토큰(A-10 부정 TC용). 생성기의 토큰 확인은 최선의 노력이라
#   실패해도 파일이 만들어진다(기본 가지 조회 실패 시 main 폴백).
node scripts/make-invite.mjs --repo https://github.com/inkwonjung-colosseum/cds-design-second-repo \
  --token ghp_invalid-token-for-qc --author "QC-테스터" --out qc-invalid.colo-invite
```

- second-repo 부정 브랜치 준비(직접 push): `dev` 스크립트가 없는 브랜치 `noscript`,
  락파일을 지운 브랜치 `nolock`, 서버가 주소를 출력하지 않는 브랜치 `quiet`
  (준비 실패 → AI 위임 TC용). 각 브랜치를 base로 하는 초대장을 별도 생성.

### 0.4 QC 실행 격리 (반복 가능한 리셋)

실제 `~/.colo-design`을 망치지 않고 라운드를 초기화하려면 데몬을 이 환경으로 띄운다:

```bash
QC_HOME=~/qc-run-$(date +%m%d-%H%M)
mkdir -p $QC_HOME
COLO_DESIGN_PROJECTS_DIR=$QC_HOME/projects \
COLO_DESIGN_PROJECTS_SETTINGS=$QC_HOME/projects.json \
COLO_DESIGN_LOG_DIR=$QC_HOME/logs \
COLO_DESIGN_DEV_AGENTS=1 pnpm dev:daemon
```

- 완전 초기 상태(온보딩부터)가 필요한 TC: 새 `QC_HOME`으로 시작.
- 이어서 하되 초대장만 갱신하는 TC(재수신 판정): 같은 `QC_HOME` 유지.
- 데스크톱 경로도 같은 env로 띄우면 같은 격리가 적용된다.
- 리셋 대신 전체 삭제: `rm -rf $QC_HOME`.

### 0.5 모델 세팅

- 새 대화마다 입력창 모델 칩: `모델 거르기` 칸에 `glm` / `swe-2` 조각 검색으로 선택.
- `빠르게`는 모델과 별개 토글(문구: `같은 모델을 더 빨리 답하게 해요 — 모델은 그대로예요`).
- 기본 루틴은 `glm-5.3` 계열, 비교/일부 TC는 `swe-2`(TC에 지정). `생각 시간` 짧게/보통/길게도 TC 지정대로.

### 0.6 검증 도구

```bash
REPO=$QC_HOME/projects/<slug>/repo
git -C $REPO branch --show-current      # colo-design/YYYYMMDD-n 인지
git -C $REPO log --format='%s' -5       # 커밋 제목 = 턴을 연 사용자의 말 첫 줄
gh pr list -R inkwonjung-colosseum/cds-design-first-repo --author inkwonjung-colosseum
gh pr view <n> -R inkwonjung-colosseum/cds-design-first-repo --json title,body,reviewRequests
ls $QC_HOME/projects/<slug>/            # cycle.json, screen-map.jsonl
ls $QC_HOME/logs/                        # daemon-*.log, turn-stats-*.jsonl
```

---

## 1. 표기 · 판정 규칙

- **ID**: `<영역>-<번호>` (아래 영역표 참조)
- **P0** = 핵심 경로(하나라도 깊으면 릴리스 블록) · **P1** = 주요 기능 · **P2** = 심층/조건부(환경 조작 필요)
- **판정**: Pass(기대결과 전부 충족) / Fail(하나라도 어긋남 — 증시 캡처+로그 첨부) / Block(사전조건 못 만듦 — 사유 기록)
- 모든 TC는 기본적으로 **데스크톱 경로(B)** 기준. 브라우저 경로(A) 별도 표기.
- [부정] 표시 = 고의로 깨뜨리는 TC. second-repo 변형 브랜치 · 잠금(Archive) · 가짜 토큰 초대장을 사용.
- 기대문구는 `packages/web/src/next/labels.ts` 의 실제 문장임 — 글자 하나까지 대조.

| 영역 | 이름 |
|---|---|
| A | 온보딩 · 초대 파일 |
| B | 프로젝트 준비(처음 열기) |
| C | 홈 |
| D | 대화 · 입력창 · 첨부 |
| E | 미리보기 |
| F | 찍기(핀) · 말풍선 |
| G | 상태 줄 · 이번 작업 · 제출 |
| H | 코멘트 · 반영 사이클 |
| I | 자동 보관 · git 동작 |
| J | 화면 확인 게이트 · 자동 고침 |
| K | 사이드바 · 멀티 프로젝트 · 찾기 |
| L | 설정 |
| M | 문제 문장 · 장애 복구 |
| N | 알림 |
| O | 데스크톱 전용 |
| P | 엣지 · 파괴 · 관찰 지표 |

---

## 2. 테스트 케이스

### A. 온보딩 · 초대 파일

공통 사전조건: **완전 초기 QC_HOME**(0.4)으로 데몬+앱 첫 실행. `qc-main.colo-invite` 준비.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| A-01 | P0 | ① 앱 첫 실행 | 체크리스트 한 장: `Colo Design 을 시작해요` + `세 가지가 채워지면 저절로 넘어가요. 터미널을 열 일은 없어요.` 머리글, 항목 `도구 준비`·`AI 연결`·`초대 파일`. `시작하기` 버튼 없음. 터미널 안 열림 |
| A-02 | P0 | ① 도구 준비 항목 관찰 | `확인하는 중…` 후 `필요한 도구 확인됨` 으로 자동 완료 |
| A-03 | P1 | ① omp가 이미 설치·로그인된 상태에서 AI 연결 항목 관찰(게이트는 설정에서 고른 프로바이더를 따름 — 첫 실행 기본은 Claude Code) | omp 선택 상태면 `Oh My Pi 준비됨 (<버전>)` 으로 자동 통과. omp 로그인이 끝났으면 `Oh My Pi 로그인이 필요합니다 — 터미널에서 로그인한 뒤 다시 확인해 주세요.` 안내 후 `다시 시도`. omp 미선택(기본 claude)이면 Claude Code 설치/로그인 흐름이 표시됨 — omp-only QC는 설정에서 AI 를 omp로 고른 뒤 재확인 |
| A-04 | P1 | ① `Codex 도 쓰시나요?` 줄의 `건너뛰기` 클릭 | `건너뛰어도 모든 일이 돼요` 부제 유지, 건너뛴 뒤 `Codex 는 건너뛰었어요 · 설정에서 언제든 설치해요` 문구 |
| A-05 | P0 | ① 셋째 항목의 `파일을 여기에 놓으세요` 자리에 `qc-main.colo-invite` 드래그앤드롭 ② 같은 상태를 다른 QC_HOME에서 `파일 고르기`로 재현 | 두 방법 모두: 가져오기 진행 → **전부 새 프로젝트면 확인판 없이 바로 적용** → 첫 프로젝트(first-repo)만 열리고 작업 화면으로 자동 전환. 안내 문장 `<first>를 먼저 열어요. 나머지는 처음 고를 때 준비해요.` |
| A-06 | P0 | ① A-05 직후 첫 작업 화면 상단 관찰 | 문제 문장 자리에 `초대 파일을 가져왔어요` + `파일 지우기` · `나중에` 한 줄 |
| A-07 | P1 | ① A-06에서 `파일 지우기` 클릭 | `초대 파일을 지웠어요` 토스트, 다운로드 폴더의 실제 파일 삭제 확인(데스크톱 경로) |
| A-08 | P1 | ① A-06에서 `나중에` 클릭 | 줄이 사라짐(파일은 남음). 이후 TC 진행에 지장 없음 |
| A-09 | P1 | ① 무기한 PAT 기준 연결 상태 줄 확인(만료 임박 토큰 없음) | `expiresAt` 없음 → 녹색 점 `연결 정상 · 프로젝트 N개`(날짜 꼬리 없음). 15일 이상 남은 토큰이면 `· N월 N일까지` 가 붙고, 14일 이내 노란 `연결이 N일 뒤 끝나요` 경로는 재발급 없이 불가 → Block 기록 |
| A-10 | P0 | ① [부정] `qc-invalid.colo-invite`(가짜 토큰 문자열로 생성) 가져오기 | 초대장 게이트 실패 안내(개발자에게 받은 코드가 이 레포에 닿지 않습니다 계열 문구). 앱이 죽지 않고 체크리스트 유지 |
| A-11 | P1 | ① [부정] 읽기 전용 토큰 초대장 — **PAT 1개 정책으로 실행 불가** | Block 기록(사유: 읽기전용 토큰 미보유). 쓰기 가능 레포 0개 안내 경로는 본 라운드에서 검증 제외 — G-13이 같은 실패 분류 경로를 대신 검증 |
| A-12 | P1 | ① first/second가 이미 등록된 QC_HOME에서 `qc-renew.colo-invite` 가져오기(설정→연결→`초대 파일 열기` 경로) | 확인판: `초대 파일을 가져왔어요` 제목, `연결 코드를 새 것으로 바꿨어요.`, 행 꼬리표 first=`그대로` · second=`바뀜`(+`기본 가지 main → broken`), `그대로인 프로젝트 1` + `초대장에 없는 프로젝트도 지우지 않아요.` `가져오기`/`그만두기` 버튼 |
| A-13 | P1 | ① A-12 확인판에서 프로젝트 **이름/지켜 줄 것**을 미리 수정해 둔 상태로 `가져오기` | 이름·지켜 줄 것은 사용자 몫이라 그대로(초대장 값으로 덮지 않음). base/리뷰어/명령 허용만 갱신. 진행 문장 `N개 중 M개 연결됨…` |
| A-14 | P1 | ① [부정] second만 담은 초대장을 새로 만들되, 레포 이름은 같고 대소문자·`.git` 접미만 다르게 해서 가져오기 | 표기 차이는 무시하고 같은 레포로 짝지어 `바뀜` 처리(중복 프로젝트 생성 안 됨) |
| A-15 | P2 | ① 작업 화면에서 입력창/창 전체에 초대장 드래그앤드롭 | 입력창은 초대장을 첨부하지 않고 **가져오기 확인판**으로 넘김 |
| A-16 | P2 | ① 옛 형식(v1/v2 평문) 초대장 — 없으면 스킵 가능(Block 기록) | 읽는 쪽이 연다: 정상 가져오기. `이 작업에 적을 이름 (선택)` 칸이 확인판에 노출(이름 실어 오지 않은 초대장이므로) |

### B. 프로젝트 준비 (처음 여는 프로젝트)

공통 사전조건: A-05 완료 상태(두 레포 등록, first만 열림). 데스크톱 경로.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| B-01 | P0 | ① 사이드바 전환기에서 second-repo 선택(첫 오픈) | 미리보기 칸이 준비 화면: `이 서비스를 이 컴퓨터에서 처음 켜요`, `준비에 몇 분 걸려요. 창을 닫아도 다음에 이어서 해요.`, 3단계 `내려받기`·`설치하기`·`미리보기 켜기`, 진행 막대/`2분쯤` ETA |
| B-02 | P0 | ① B-01 준비 중, 입력창에 말 먼저 보내기 | 대화에 `준비가 끝나면 바로 보낼게요` 줄이 서고, 준비 완료 즉시 턴이 출발. 빈 입력 오류 없음 |
| B-03 | P1 | ① B-01 준비 중 first-repo로 전환했다 돌아오기 ② 준비 중 창을 닫았다 다시 열기 | 준비가 이어서 진행(재시작 안 됨). 완료 시 OS 알림 1회(데스크톱). 다른 프로젝트 줄에 `준비 중` 표시 유지 |
| B-04 | P1 | ① B-01 준비 중 `찍기`(⌘⇧P) 시도 + ⌥+클릭 시도 | 둘 다 잠김: `준비가 끝나면 화면을 찍을 수 있어요` 안내. 핀 생성 안 됨 |
| B-05 | P1 | ① 처음 연결하는 레포의 install/preview 명령 승인 흐름 — 클린 홈에서 프로젝트 생성 경로 | 승인 카드가 돌릴 명령을 문장으로 보여주고, `프로젝트 만들기`누름이 첫 승인으로 간주되어 재문의 없음. 승인 기록은 레지스트리에 남아 두 번째 오픈부터 묻지 않음 |
| B-06 | P1 | ① [부정] `quiet` 브랜치를 base로 하는 second-repo 초대장으로 첫 오픈(서버가 주소를 출력하지 않도록 미리 조작) | 준비가 멈추고 **사용자 클릭 없이** 실패가 대화로 넘어가 AI가 고침. 문제 문장 `AI가 고치는 중이에요`. 같은 단계에서 두 번 실패 시 한 번 더 위임, 그래도 실패하면 `개발자에게 알렸어요` 전환 |
| B-07 | P1 | ① [부정] `noscript` 브랜치(dev 스크립트 없음)를 base로 하는 초대장 — 초대장 **생성 단계**에서 확인 | 생성기/페이지가 개발 서버 스크립트 없음을 배지로 알림(스크립트 없으면 진행은 되되 경고). 가져오기 후 오픈하면 준비 실패로 B-06과 같은 위임 경로 |
| B-08 | P2 | ① [부정] `nolock` 브랜치(락파일 없음) 첫 오픈 | 설치 단계를 돌리지 않음(락파일 없으면 install 자체를 안 돌림). 사용자 커밋에 락파일이 몰래 올라가지 않음 |

### C. 홈

공통 사전조건: first-repo 활성, 온보딩 완료.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| C-01 | P0 | ① 사이드바 `홈` 클릭 | `QC-테스터님, 무엇을 만들까요?`(초대장 `--author` 이름) + 큰 입력창 `만들고 싶은 화면을 말해 주세요` + 힌트 `그림이나 문서를 끌어다 놓아도 돼요 · 보내면 그 프로젝트의 새 대화가 열려요` |
| C-02 | P0 | ① 홈 입력창에서 "회원 목록 화면 만들어 줘" 전송 | 지금 프로젝트의 **새 대화**가 열리고 작업 화면으로 전환. AI 턴 시작(모델: 기본 glm-5.3) |
| C-03 | P1 | ① 대화에서 AI가 질문/허락 카드를 유도(예: "화면에 뭐 넣을지 먼저 물어봐 줘") ② 홈으로 이동 | 받은 편지함 `답을 기다려요`에 카드(`AI가 물어봐요` / `AI가 이것을 하려고 해요 · <what>`). 질문이 여러 개면 `물어볼 것이 여러 개예요 — 대화에서 답해 주세요` |
| C-04 | P1 | ① C-03 카드에서 `허용`/`거절` 또는 답변 후 `보내기` | 카드가 사라지고 대화가 이어서 진행. 홈 행 숫자 배지가 **이 카드 수만** 센다 — 다른 항목(진행 중·방금 있던 일)은 세지 않음 |
| C-05 | P1 | ① 턴 1개 실행 중 홈 이동 ② second-repo 준비 병행 | `지금 진행 중`에 도는 대화 + `처음 켜는 준비`(second) 표시. 없을 때는 `도는 작업이 없어요` / `기다리는 일이 없어요` |
| C-06 | P1 | ① 턴 완료 → 홈 ② PR에 코멘트 달고 대기 → 홈 ③ PR 머지 후 → 홈 | `방금 있던 일`에 각각 `답이 왔어요 — 확인해 보세요` · `코멘트가 왔어요` · `반영됐어요` 행. 비면 `아직 있던 일이 없어요` |
| C-07 | P2 | ① [부정] 홈 카드가 열려 있는 상태에서 웹 소켓 끊었다 재접속(앱 새로고침) | 카드가 **새 소켓에만 다시 내려앉아 이중으로 쌓이지 않음**. 홈 배지 수 부풀지 않음 |

### D. 대화 · 입력창 · 첨부

공통 사전조건: first-repo 활성 · 미리보기 ready. 모델 지정 없으면 glm-5.3.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| D-01 | P0 | ① ⌘T 새 대화 → 빈 상태 확인 ② "회원 목록 화면 만들어 줘. 이름·상태·가입일 컬럼" 전송 | 빈 상태 문구 `무엇을 만들까요?` + `<프로젝트>의 실제 화면이에요.` 안내. 턴 완료 후 답 변 아래 `고친 화면` 카드(화면 제목 + `미리보기에서 보기`), 미리보기가 그 화면으로 자동 이동(이미 보고 있으면 유지). 정산 줄: `<초> 걸렸어요` · `전체 복사` · `···` |
| D-02 | P0 | ① 모델 칩 클릭 → 팝 확인 ② 거르기 칸에 `swe-2` 입력 ③ 다시 `glm` 입력 ④ 없는 이름 `zzz` 입력 | 모델 목록 + `생각 시간`(짧게/보통/길게). `swe-2` 조각 검색으로 해당 모델만 남음. `glm` 도 부분 일치. `zzz` → `맞는 모델이 없어요` |
| D-03 | P0 | ① swe-2 선택 후 턴 1개 ② 새 대화에서 glm-5.3 선택 후 턴 1개 | 두 모델 모두 턴 정상 완결. 열린 대화는 처음 고른 모델 유지 — 칩에 `열린 대화는 처음 고른 AI 그대로예요. 바꾸면 다음 새 대화부터 써요.` 계열 안내 |
| D-04 | P1 | ① `빠르게` 토글 on → 토스트 확인 → off | on: `빠르게 켜졌어요 · 같은 모델이 더 빨리 답해요`. off: `빠르게를 껐어요`. 턴 동작에 영향만 있고 모델 표시는 그대로 |
| D-05 | P0 | ① 입력창에 이미지(PNG 스크린샷) 드롭 → 전송 "이 디자인처럼 만들어 줘" | 대기줄 표시 `그림 1장`. 턴이 이미지를 참조한 답(비전 전달 확인 — 이미지 내 요소가 결과에 반영됨) |
| D-06 | P1 | ① 텍스트 문서(기획 문서 .md/.txt, 100KB) 첨부 전송 | 문서 내용이 턴에 반영. 전송 줄에 `파일 1개` |
| D-07 | P1 | ① 300KB 텍스트 파일 첨부 전송 (256KB 예산 초과) | 잘림 없이 동작: 앞부분 인라인 + `잘렸다 — 나머지는 이 경로의 파일로 이어 읽으라` 표식 경로 전달. AI가 파일을 열어 이어 읽었는지 답 내용으로 확인 |
| D-08 | P1 | ① PDF/zip 첨부 전송 | 디스크에 적힌 뒤 절대 경로가 전달(.git 안). 턴 정상. 화면 커밋에 첨부 파일이 **절대 포함되지 않음**(I-03 에서 재확인) |
| D-09 | P0 | ① [부정] 8MB 초과 파일 드롭 | `<name> — 8MB 보다 큰 파일은 보낼 수 없어요`. 대기열에 올라가지 않음. 8MB-1바이트 경계 파일은 통과 |
| D-10 | P1 | ① 보낸 말에 마우스 호버 → `고쳐서 다시 보내기` 클릭 | 그 말 앞까지 이어받은 **새 대화**가 열리고 입력창에 그 말이 채워짐. 토스트 `이 말 앞까지 이어받은 새 대화예요 · 고쳐서 보내면 돼요 · 화면은 지금 그대로`. 화면(파일)은 되돌려지지 않음 |
| D-11 | P1 | ① 턴 정산 줄 `···` → `여기서 새 대화` | 이 답까지를 남기는 포크. `이 답까지 이어받은 새 대화를 열었어요 · 원래 대화도 그대로 있어요`. 메뉴에 `작업 기록에서 되돌리기` 링크 존재. 파일은 되돌리지 않음(안내 문구 `화면은 지금 모습 그대로예요`) |
| D-12 | P0 | ① 턴 도는 중 `멈추기` 클릭 | 버튼 `멈추는 중…` 후 정지. 줄에 `멈췄어요 · 하던 곳까지는 화면에 남아 있어요` |
| D-13 | P0 | ① 턴 도는 중 다음 말 입력 → 전송 | 대기줄로 들어감: `지금 답이 끝나면 바로 보낼게요` + `고쳐서 보내기`/`지금 보내기`. 앞 턴 종료 후 자동 출발 |
| D-14 | P1 | ① 턴 도는 중 Wi-Fi 차단 등으로 일시 실패 유도 | `잠깐의 문제로 조금씩 기다리며 다시 묻는 중이에요 · n/5` 진행. 회복 시 같은 말로 자동 재전송. 5회 실패 시 `AI가 답을 못 했어요` 카드 + `다시 시도`(같은 말 재전송 확인) |
| D-15 | P2 | ① 사용량 한도 근접 상태 강제(실계정 여부에 따라 Block 가능) | 사용량이 한도에 가까울 때만 모델 칩 옆 한 단어(`사용량 N%`). 한도 도달 시 `사용량이 다시 채워지는대로` 계열 문구 + `사용량을 다 썼어요…` 실패 문구 |
| D-16 | P1 | ① 대화 메뉴(`···`) → `이름 바꾸기` / `내보내기` / `지우기` | 이름 변경 즉시 반영. 내보내기: `<file> 파일로 내보냈어요`(markdown, 판정·카드 포함). 지우기: 확인 한 줄 `이 대화를 목록에서 지울까요? 화면은 그대로예요` → 목록에서만 사라짐 |
| D-17 | P1 | ① 같은 대화에서 10+턴 장기 대화 | 길어지면 `대화가 길어졌어요 — 새 대화에서 이어가면 더 빨라요` 배너. 기능 정상 유지 |
| D-18 | P1 | ① first-repo 대화 열린 상태에서 second-repo로 전환 후 대화 목록 확인 | 대화가 프로젝트별로 분리 — second에는 first의 대화가 보이지 않음 |

### E. 미리보기

공통 사전조건: first-repo ready, 화면 1개 이상 생성된 상태.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| E-01 | P0 | ① 미리보기의 실제 컨트롤 조작(검색 입력, 버튼, 폼) | 모두 실제 목 데이터로 동작(풀 인터랙션 목업). 죽은 UI 없음 |
| E-02 | P0 | ① 주소창 클릭 | `이 대화에서 만든 화면` / `다른 화면` 두 제목의 목록. 글자 입력 시 실시간 거르기. 하단 `글자를 치면 거르고, / 로 시작하면 그 주소로 가요` |
| E-03 | P1 | ① 주소창에 `/members` 입력 → 이동 ② 존재하지 않는 화면명 검색 | `/` 시작은 그 주소로 직접 이동. 없으면 `찾는 화면이 없어요` |
| E-04 | P1 | ① [부정] 주소창에 외부 주소(`https://example.com`) 입력 | 외부 주소 거절: `미리보기 안의 화면 주소만 열 수 있어요` |
| E-05 | P1 | ① 화면 이동 후 `뒤로`/`앞으로` ② `새로 고침` | 브라우저와 같은 이동. 새로 고침은 **보고 있던 자리 그대로**(루트로 튕기지 않음) |
| E-06 | P1 | ① `화면 크기` PC/태블릿/휴대폰 전환 | 실제 뷰포트 에뮬레이션(반응형 레이아웃 변화). 상태 유지는 크기 전환에도 무관 |
| E-07 | P1 | ① `···` 메뉴 → 배율 `작게`/`실제 크기로`/`크게` | 미리보기 칸만 배율 변화(앱 전체는 그대로 — ⌘= 와 별개임을 확인) |
| E-08 | P1 | ① `···` → `AI에게 이 화면 보여 주기` | 현재 화면이 입력창에 담김(토스트 `지금 화면을 입력창에 담았어요 — 이상한 점을 말해 주세요`). 턴 중 재시도 시 `이미 보여 줬어요 — 답을 기다려 주세요` |
| E-09 | P1 | ① [부정] 제출 전 `···` → `제출한 때의 화면 보기` | 잠김: `제출한 뒤에 볼 수 있어요`. 제출 후에는 개발자가 받은 모습 그대로 표시 + 상단 `제출한 때의 화면이에요` 바 + `지금 화면으로` 복귀 버튼 |
| E-10 | P1 | ① 늦게 뜨는 화면(의도적으로 무거운 화면 요청 또는 서버 지연) | 30초 초과 시 도구가 스스로 한 번 새로 고침 + `화면이 늦게 뜨고 있어요 — 잠시만 기다려 주세요` 계열 상태. 오류 배너는 뜨지 않음 |
| E-11 | P1 | ① [부정] preview 서버 프로세스 kill(`lsof -i :<port>`로 찾아 kill) | 카드 `화면을 다시 켜는 중이에요`(`n/2` 진행). 도구가 먼저 2회 재시작 → 성공 시 `화면이 다시 켜졌어요`. 2회 실패 시에만 AI 고침 턴 (`AI가 막힌 곳을 고치고 있어요`). 서버 마지막 출력은 화면에 노출되지 않음 |
| E-12 | P1 | ① 웹뷰 게스트 위에서 레포 앱의 모달/팝오버 열기(데스크톱 경로) | 모달·팝오버가 게스트 위에 그대로 그려짐(게스트가 별 프로세스). 레포 앱이 멈춰도 도구 UI는 응답 |
| E-13 | P2 | ① 프로젝트 전환 후 복귀(미리보기 warm) — K-03과 연동 | 돌아오면 보던 자리 그대로(reload 없음). 서버 재준비 없이 `ready` |

### F. 찍기(핀) · 말풍선

공통 사전조건: first-repo ready, 화면 1개 이상.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| F-01 | P0 | ① 막대 `찍기`(⌘⇧P) 켜기 | 미리보기 아래 `찍기 켜짐` 알약(`화면을 누르면 그 자리가 입력창에 담겨요 · 끌면 영역을 짚어요` + `끄기`). **화면이 밀리지 않음** |
| F-02 | P0 | ① 찍기 켠 상태에서 요소 클릭 | 핀 + 말풍선: 번호 · 요소 정체(컴포넌트 이름, 없으면 testid) · 화면 이름 · 메모 칸(`어떻게 바꿀까요? (안 적어도 돼요)`) · `↵ 담기` · `지금 보내기` · 휴지통 |
| F-03 | P0 | ① ⌥+클릭(찍기 꺼짐 상태) ② ⌥+드래그로 빈 여백 짚기 | ⌥+클릭도 언제든 핀 생성. 드래그는 `영역` 핀(요소 없는 자리도 가능) |
| F-04 | P0 | ① 말풍선에 메모 입력 후 ↵(`담기`) | 메모가 입력창 같은 번호 칩에 반영. 토스트 `1번을 담았어요 — 더 찍거나 입력창에서 보내요`. 이어서 2번·3번 찍기 가능 |
| F-05 | P0 | ① 핀 2개 담은 상태에서 문장과 함께 전송("이 버튼들 오른쪽으로 옮겨 줘") | 핀이 첨부로 함께 나감. 도는 동안 회색 배지로 잔류 → 턴 종료 시 사라짐. 입력창 칩도 비워짐. `comments.json`에 전달 기록만 남음(재노출 없음) |
| F-06 | P1 | ① 말풍선 휴지통 클릭 | 그 핀만 제거(`핀을 뺐어요`). 번호 재정렬 확인 |
| F-07 | P1 | ① 이 도구로 만지지 않은 원래 페이지(레포 원래 화면)에서 ⌥+클릭 | 어느 페이지에서나 핀 발생. 그 페이지 경로가 화면 id가 됨(코멘트→수정 고리 열림). 턴 정상 |
| F-08 | P1 | ① 좁은 창(<900px)에서 핀 담기 | `1번을 담았어요 — 대화 탭에서 메모를 남기고 보내요`(좁은 창 전용 문구). 화면 탭에 찍은 곳 수 표시 |
| F-09 | P2 | ① 핀 턴 직후 `$QC_HOME/logs/turn-stats-*.jsonl` 확인 | 해당 턴 행에 `pinBytes` · `pinHit`(후보 적중 여부) 기록. 사용자 말/파일 경로는 기록되지 않음 |

### G. 상태 줄 · 이번 작업 · 제출

공통 사전조건: first-repo, 턴 1개 이상 완료(화면 1~2개), glm-5.3.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| G-01 | P0 | ① 턴 완료 직후 상태 줄 확인 ② 턴 도는 중 확인 | 제출 전: `제출 전 · 화면 N개`(첫 점에 점 문자). 도는 중: `만드는 중 · <초>`(창 새로 열어도 이어 센다). 멈춤 없는 경과 표기 |
| G-02 | P0 | ① 상태 줄 여정 클릭 | `이번 작업` 팝오버: 머리 `<프로젝트> · <시작일>부터 · <지금 자리>`, `바뀐 화면` 목록(제목·한 줄·시각), 제출 칸(`아직 제출하지 않았어요. 다 만들었으면 위의 제출을 눌러요.`), 코멘트 칸(`제출하면 개발자의 코멘트가 여기에 모여요.`), `작업 기록 열기` |
| G-03 | P0 | ① `제출` 클릭 | 확인 한 장: `개발자에게 제출할까요?`, `제출할 화면 N개`, `개발자에게 한마디`(`선택`), `받을 개발자 · <이름>`(초대장 `--reviewer`), `그만두기`/`제출`. `제목과 설명은 도구가 써요 · 원본은 건드리지 않아요` |
| G-04 | P0 | ① G-03에서 한마디 적고 Enter(또는 `제출`) | 버튼 `제출하는 중…` → `제출됐어요`. 대화에 영수증: `개발자에게 제출했어요` · `받을 개발자 1명` · `내 한마디 · “…”`. 여정 첫 점 `제출됨` |
| G-05 | P0 | ① G-04 후 GitHub에서 PR 확인 | PR 열림. 브랜치 `colo-design/<YYYYMMDD>-n`. 본문에 화면 제목·경로 목록과 `> 작성: QC-테스터`, 리뷰어로 `inkwonjung-colosseum` 지정. **베이스 브랜치에는 커밋 없음**(I-01 참조) |
| G-06 | P1 | ① 제출 후 추가 턴(화면 더 고침) ② 다시 `제출` | 확인판 제목 `같은 요청에 더해 제출할까요?`, `제출한 뒤 바뀐 N곳`, 버튼 사본 `제출한 뒤로 바뀐 것을 같은 요청에 더해 제출해요`. 영수증 `같은 요청에 더해 제출했어요`. **같은 PR**에 커밋 추가(새 PR 안 열림) |
| G-07 | P1 | ① 화면 밖 파일(예: README)을 건드리는 턴 수행 후 제출판 확인 | 목록 아래 `화면 밖 변경 N건` 으로 별도 집계 — 화면 목록에 섞이지 않음 |
| G-08 | P1 | ① 잠긴 제출 버튼의 각 상태 확인: 턴 실행 중 / 준비 중 / 반영 직후 / 변경 없음 | 누를 때마다 이유 한 줄: `AI가 고치는 중 — 끝나면 제출할 수 있어요` / `준비가 끝나면 제출할 수 있어요` / `반영됐어요 — 다음 작업을 만들면 제출할 수 있어요` / `제출한 뒤로 바뀐 것이 없어요 — 더 고치면 같은 요청에 더해 제출할 수 있어요` · `아직 바뀐 화면이 없어요` |
| G-09 | P1 | ① 대화에 "개발자에게 보내 줘" 입력 | 확인창 없이 AI가 제출을 대신 수행(채팅 제출 — 초대장 기본값). 영수증 동일 |
| G-10 | P1 | ① 제출된 요청(PR)에서 `이번 작업` 팝오버 확인 | 제출 칸: 시각·작성자·`받은 개발자 · <이름>`·`제출한 내용 열기`. 머리 부제 `개발자에게 가 있어요` |
| G-11 | P1 | ① 열린 요청이 있는 상태에서 `개발자에게 한마디 더` 사용(영수증/이번 작업 하단) | 상자 `잘못 보냈거나 덧붙일 말을 적어요` → 전송 시 `<시각> 개발자에게 보냈어요`. PR에 코멘트로 반영 |
| G-12 | P2 | ① [부정] 제출 직후 네트워크 차단 | 버튼 `다시 제출하는 중…`. 오프라인 푸시 재시도(30초×3) 후 제출이 밀어냄. 3회 실패 밀린 커밋은 제출이 기다렸가 밀음. 회복 후 최종 `제출됐어요` |
| G-13 | P0 | ① [부정] second-repo를 GitHub에서 **잠금(Archive)** 한 뒤 그 프로젝트에서 화면 만들고(로컬 커밋은 됨) 제출 시도 ② 확인 후 잠금 해제 | 화면 작업은 되고, 제출이 막힘(푸시·PR 생성 403) → 문제 문장 `제출이 막혀 개발자에게 알렸어요. 풀리면 도구가 다시 제출해요 — 지금은 계속 만들어도 돼요` + 여정 `제출 전 · 제출하지 못했어요`. 잠금 해제 시 자동 재제출 확인. (읽기전용 토큰 대신 아카이브로 같은 실패면을 검증 — 분류가 다르게 관찰되면 그 자체가 QC 결과) |

### H. 코멘트 · 반영 사이클

공통 사전조건: G-04~G-05 완료(first-repo PR 열림). 개발자 역할은 QC 본인(GitHub에서 수행).

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| H-01 | P0 | ① GitHub PR에 리뷰 코멘트 작성("이 버튼 색을 파랑으로 바꿔 주세요") ② 앱 대기 | 대화에 코멘트 카드: 사람·글·상태 `AI가 반영하는 중` → 완료 시 `반영해 같은 요청에 다시 제출했어요`. 동의 버튼 없음(AI가 바로 반영). 같은 PR에 재제출 커밋 추가. 여정 둘째 점 `개발자가 보고 있어요 · 코멘트 1` |
| H-02 | P1 | ① `이번 작업` 코멘트 칸 확인 | 장부: 사람·글·상태(`반영됨`/`고치는 중`). 부제 `AI가 스스로 반영해요` |
| H-03 | P1 | ① 코멘트 카드의 `답하기` → 문구 전송 | `<이름>님에게 답하기` 상자 → `<이름>님에게 답을 달았어요`. PR에 답변 코멘트 게시 |
| H-04 | P0 | ① GitHub에서 PR **머지** ② 활성 프로젝트에서 대기(최대 2분) | 감독자가 받아옴: 여정 셋째 점 `반영됐어요`, 대화에 `반영됐어요 · 다음에 만드는 것은 새 작업이에요` 마일스톤. 클론이 베이스로 복귀. 작업 기록 서랍이 비며 `반영된 작업의 기록은 여기서 사려져요…` 문구 |
| H-05 | P1 | ① H-04 직후 입력창 확인 ② 다음 턴 수행 | 입력창 안내 `다음에 만들 것을 말해 주세요 — 새 작업으로 시작해요`. 다음 커밋이 **새 사이클 브랜치**(번호 증가)에서 시작 |
| H-06 | P1 | ① second-repo(비활성)에 열린 PR을 머지한 뒤 방치 | 최대 10분 내 받아오기. `다른 프로젝트` 줄이 `반영됐어요` 표시 → 받은 뒤 해소 |
| H-07 | P1 | ① 앱을 앞으로 가져온 직후(비활성 프로젝트 PR 머지 상태) | 창 복귀 시 그 자리에서 즉시 받아오기 확인(폴링 대기 없음) |
| H-08 | P1 | ① [부정] PR을 머지하지 않고 close ② 베이스에서 같은 파일을 직접 수정 후 다시 시나리오 | `개발자가 이번 요청을 닫았어요 — 작업은 새 요청으로 옮겨 두었어요` 카드. 받아오기 중 미커밋 변경이 있으면 임시 보관 후 얹기 — 겹치지 않는 편집은 자동 병합, 진짜 충돌만 AI 첫 과제(게이트 카드) |

### I. 자동 보관 · git 동작 (클론 검증)

공통 사전조건: G/H 시나리오 수행 중인 클론. 검증은 0.6 명령.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| I-01 | P0 | ① 턴 2개 완료 후 `git log --format='%s'` 확인 | 턴마다 자동 커밋. 커밋 제목 = **그 턴을 연 사용자의 말 첫 줄**. 모델 턴 추가 소비 없음 |
| I-02 | P0 | ① 브랜치명 확인 ② 베이스 브랜치 로그 확인 | `colo-design/<YYYYMMDD>-n`(원격에 없는 번호로 상승). 베이스(main)에는 앱이 만든 커밋이 하나도 없음 |
| I-03 | P1 | ① 첨부 파일을 쓴 턴(D-08) 후 `git status`/`git log` 확인 | 첨부는 클론의 `.git` 내부에만 존재 — `git status`에 절대 보이지 않고 커밋에 포함되지 않음 |
| I-04 | P1 | ① 오프라인 상태에서 턴 완료 | 커밋은 즉시(로컬), 푸시는 백그라운드 재시도(30초 간격 3회). 턴 종료가 밀리지 않음 |
| I-05 | P1 | ① 핀 턴 후 `screen-map.jsonl` 확인 ② 사이클 진행 중 `cycle.json` 확인 | screen-map에 그 화면을 고친 커밋이 건드린 파일 기록(500행 상한). cycle.json에 제출 의도·코멘트 장부·예산 이어짐 |
| I-06 | P1 | ① `$QC_HOME/logs/daemon-*.log`에서 경로·이메일 노출 검사 | 값의 비밀·계정 경로가 `~/{path}` 식으로 눌러 닫혀 있음(새니타이저). PAT 원문 없음 |
| I-07 | P2 | ① 턴 통계 파일 확인(D·F·G 시나리오 후) | `turn-stats-*.jsonl`에 턴 종류·길이·firstDeltaMs·firstEditMs·failure·pinBytes·pinHit·게이트 행(gateset: 판정 상태·rescued·skipped) 기록. 사용자 말·화면 내용 없음 |

### J. 화면 확인 게이트 · 자동 고침

공통 사전조건: **데스크톱 경로**(게이트는 미리보기 창이 있을 때만). first-repo.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| J-01 | P0 | ① "버튼을 누르면 `/api/missing` 으로 요청하는 회원 화면 만들어 줘" 전송(미리보기 서버 대상 실패 요청 유도) | 턴 종료 시 완료 알림 대신 `화면 확인` 게이트 카드. 같은 대화에서 AI가 고침(문제 화면 그림이 브리프에 포함 — 상한 3장). 완료 카드 `AI가 고쳤어요` |
| J-02 | P1 | ① 콘솔 오류 유도("존재하지 않는 변수를 참조하는 코드 잠깐 넣어 줘" 식 요청 또는 직접 시나리오) | 게이트가 콘솔 **오류**만 센다 — 경고는 세지 않음(레포 개발 빌드 기본 소음). 게이트 후 자동 커밋 1회 |
| J-03 | P1 | ① 외부 이미지/폰트가 깨지는 화면 요청(외부 URL 하드코딩) | 외부 리소스 실패는 잡음 — 게이트가 걸리지 않음. **미리보기 서버의 요청만** 문제로 셈 |
| J-04 | P1 | ① 한 턴에서 문제 화면 2개 유도 | 게이트는 **사용자의 한 턴에 한 번만** — 두 번째 문제는 다음 사람 턴이 잡음(기계 둘이 서로 답하며 구독 태우지 않음) |
| J-05 | P2 | ① 완전히 빈 화면(글자·그림 없음) 유도 — "디버깅용으로 아무것도 렌더하지 않는 빈 화면 추가해 줘" | 다 로드됐는데 빈 화면 판정 → 게이트 → AI 고침 |
| J-06 | P2 | ① AI 턴 중 화면을 고친 뒤 `screen_check` 도구 호출 여부 관찰(로그/대화) | 공통 규칙에 따라 답하기 전 자가 확인 — 게이트 실패 재턴이 줄어드는지 정성 확인 |

### K. 사이드바 · 멀티 프로젝트 · 찾기

공통 사전조건: first+second 등록. first 활성.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| K-01 | P0 | ① 사이드바 전환기 드롭다운 | 프로젝트마다 상태 점·기다리는 숫자. 미개설 프로젝트에 `처음 열 때 준비해요`. 형광 등 위계 확인 |
| K-02 | P0 | ① `다른 프로젝트` 줄(second)의 상태 변화 관찰: 준비 중 → 만드는 중 → 답을 기다려요 N → AI가 답을 못 했어요 → 코멘트 N → 반영됐어요 | 우선순위가 문서 순서대로 하나만 표시: `준비 중` > `만드는 중` > `답을 기다려요 N` > `AI가 답을 못 했어요` > `코멘트 N` > `반영됐어요` > 작업 상태 |
| K-03 | P0 | ① first에서 화면 B를 보다가 second로 전환 → first 복귀 | 복귀 시 보던 화면·스크롤 그대로(재준비 없음). 전환 토스트 `옮겼어요 · <이름> · 미리보기는 보던 자리 그대로예요` |
| K-04 | P1 | ① third+ 레포가 없다면: first·second 외 임시 레포 1개 추가 등록(초대장 재수신) → 세 프로젝트를 순환 | 따뜻한 미리보기는 **최근 두 개까지** — 가장 오래된 것이 멈춤(복귀 시 재준비). 들어오는 서버는 빈 포트 자동 선택(포트 충돌 없음) |
| K-05 | P0 | ① first에서 긴 턴 실행 중 second로 전환하여 다른 작업 | first 턴이 **끝까지 완주**(세션 죽지 않음). second의 `다른 프로젝트` 줄(first)이 `만드는 중` → 완료 표시. OS 알림 도착 시 알림 클릭하면 first 대화까지 전환되어 열림 |
| K-06 | P1 | ① 도구가 스스로 연 대화 확인(준비 위임 B-06, 코멘트 반영 H-01, 오류 고침 J-01 후) | 사람 대화 목록과 분리된 맨 아래 `도구가 한 일 N` 접힌 그룹에 쌓임. 펼치면 보이고 클릭하면 열림 |
| K-07 | P0 | ① ⌘K 팔레트: 대화 이름·프로젝트명 검색 ② ⌘T 새 대화 ③ ⌘, 설정 | 세 단축키 모두 동작. 팔레트에서 대화·프로젝트 전환 |
| K-08 | P1 | ① 전환기 머리의 프로젝트 **이름 클릭**(카드) | 카드: `저장소 열기` · `미리보기 열기` · `초대 파일 열기…` · `지켜 줄 것` 안내장(예문 `버튼은 항상 화면 오른쪽 위에 둬 주세요`) |
| K-09 | P1 | ① `지켜 줄 것`에 규칙 저장("모든 목록 화면 상단에 검색창을 둬 주세요") ② 새 대화에서 화면 요청 ③ 기존 대화에서 화면 요청 | 저장 토스트 `저장했어요 — 새로 시작하는 대화부터 적용돼요`. **새 대화**에서만 규칙이 반영(검색창 있음), 저장 전 대화는 영향 없음. 비우면 규칙 삭제 |
| K-10 | P1 | ① 창 폭을 900px 미만으로 | 사이드바 `≡` 뒤로, `대화 | 화면 · <이름>` 두 탭. 찍은 곳의 수가 화면 탭에 표시 |

### L. 설정

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| L-01 | P0 | ① ⌘, 설정 → `AI` 줄 | 카드 목록에 **omp(개발 실행)** 표시. Claude/Codex 미설치 시 `쓸 수 없는 AI N` 폴드에 `설치되지 않았어요 · 없어도 모든 일이 돼요` + `설치` 버튼 |
| L-02 | P1 | ① AI 카드에서 omp 선택 | 다음 새 대화부터 omp 사용. 이미 열린 대화는 유지(D-03 안내와 동일 원칙) |
| L-03 | P0 | ① `테마`에서 7개 순차 선택: Claude(기본) · Codex · 밝음 · 어두움 · GitHub 밝음 · GitHub 어두움 · 시스템 따르기 | 즉시 전체 적용(사이드바·대화·상태 줄·미리보기 게스트 외 UI). 재시작 후 유지 |
| L-04 | P1 | ① `알림` — `다 만들었을 때` 끔/오래 걸린 답만/모든 답 전환 ② `소리` 토글 ③ `시험 알림` | 세 값 저장·유지. 시험 알림 도착(본문 `실제 알림은 이렇게 도착해요`) — OS가 차단했으면 `컴퓨터가 알림을 막았어요` + `시스템 알림 설정 열기`. 확인 요청·멈춤 알림은 설정값과 무관하게 항상 옴 |
| L-05 | P0 | ① `연결` 줄 확인 ② `작업에 적을 이름` 수정 후 제출 | `연결 정상 · 프로젝트 N개`, `연결 코드` 존재 표시, `초대 파일 열기`. 이름 수정 후 다음 제출 PR 본문 `> 작성:` 에 반영 |
| L-06 | P1 | ① `업데이트` 줄: `지금 확인` 클릭 | `확인하는 중` → `모두 최신이에요 · 방금 확인` 또는 `새 버전 N개 · 방금 확인`. `새 AI 버전이 나오면` 자동 설치 토글 존재(도는 작업이 있으면 `도는 작업이 끝나면 설치해요`) |
| L-07 | P1 | ① `개발자용` 폴드 펼치기 | `도구 폴더`·`폴더 열기`·`하루 로그`·`로그 폴더 열기` + 데몬 상태 줄. 버튼이 실제 폴더를 엶(F1). 기본 설정 화면에서는 접혀 있음 |

### M. 문제 문장 · 장애 복구

문제 문장은 상태 줄 바로 아래 한 줄. 셋만 존재.

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| M-01 | P0 | ① B-06/J-01 등 AI 위임 상태 | 문장 1 `AI가 고치는 중이에요` — 사용자 할 일 없음. 완료 시 자동 소멸 |
| M-02 | P1 | ① G-13(제출 막힘) 상태 | 문장 2 `개발자에게 알렸어요` + `제출이 막혀 개발자에게 알렸어요. 풀리면 도구가 다시 제출해요 — 지금은 계속 만들어도 돼요`. 계속 작업 가능. 풀리면 자동 재제출 |
| M-03 | P0 | ① [부정] GitHub에서 PAT 폐기(또는 Regenerate) → 받아오기/제출 트리거 — **유일한 토큰이므로 라운드 3 맨 끝에만 실행** | 문장 3 `다시 연결이 필요해요` + `연결 코드가 만료됐어요. 개발자에게 받은 새 초대 파일을 이 창에 놓아 주세요. 대화와 작업은 그대로예요.` + `초대 파일 열기`. 회복 단계(새 초대장 드롭 → `만료됐던 연결이 새 코드로 다시 이어졌어요`)는 토큰 Regenerate 후 초대장을 다시 만들어야 하므로, 재발급하지 않을 경우 거기까지 Block 기록 |
| M-04 | P1 | ① [부정] omp 로그인 만료 유도(토큰 만료) | 같은 문장 3의 로그인 변형: `AI 로그인이 끝났어요. 브라우저에서 한 번만 다시 로그인해 주세요.` + `브라우저에서 로그인` |
| M-05 | P1 | ① [부정] omp 프로세스 강제 종료(`pkill -f omp`) | 도구가 재시작: 대화에 `AI 프로그램이 멈춰 다시 일으켰어요 · 하던 일을 그대로 이어서 해요`. 하던 턴 이어서 완결. 10분 3회 초과 시 개발자 알림 전환 |
| M-06 | P0 | ① 앱/웹 새로고침으로 소켓 끊었다 재접속(턴 대기 카드 열린 상태) | 카드 이중 적재 없음(재접속 복원은 이 소켓만). 멈춰 있던 턴 상태 그대로 복원. 배너 `연결하는 중이에요` → 정상 |
| M-07 | P1 | ① 데몬 프로세스 종료 후 재실행(데스크톱에서 데몬 in-process이므로 앱 재시작) | 기동 복구: 2시간 내 중단 턴은 브리프로 이어받고, 그 외는 회복 패널/`다시 시도` 카드(`말이 닿기 전에 AI가 멈췄어요. 다시 시도하면 같은 말을 다시 보내요.`). 임시 보관(stash)에 남은 변경이 있으면 클론에 자동 복원 — 사용자가 손으로 만든 stash는 건드리지 않음 |
| M-08 | P1 | ① 문제 문장이 없는 평시 상태 확인 | 그 자리는 방금 가져온 초대 파일의 `초대 파일을 가져왔어요 — 파일 지우기 · 나중에` 줄이 쓰거나 비어 있음 — 오류 배너·수정 요청 버튼은 절대 뜨지 않음 |
| M-09 | P1 | ① [부정] 미리보기가 쓰는 자리를 다른 프로세스가 점유한 상태에서 준비 | 도구가 그 프로그램을 정리하고 미리보기를 띄움(사용자 개입 없음) |

### N. 알림 (데스크톱 경로)

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| N-01 | P0 | ① 앱을 **뒤로** 두고 턴 완료 대기 | OS 알림 도착. 클릭 시 창이 앞으로 오고 그 대화가 열림(다른 프로젝트면 전환까지). 뒤에 있는 동안 도착분은 dock 배지가 누적 |
| N-02 | P1 | ① 앱이 **앞에** 있는 상태에서 턴 완료 | 알림이 조용히 생략(안 옴) |
| N-03 | P1 | ① 확인 요청 카드·멈춤 이벤트 | 알림 설정값(끔 포함)과 무관하게 항상 알림 |
| N-04 | P2 | ① mac 개발 빌드(미서명 Electron)에서 시험 알림 | 알림이 오지 않음(UNNotification이 미서명 거부 — 문서화된 제한). 안내 문구로 설명됨. 서명 후 동작 확인은 선택 |
| N-05 | P2 | ① 알림 클릭으로 second-repo의 대화 열기 | 해당 프로젝트로 자동 전환 후 대화 개방(전환 토스트와 일관) |

### O. 데스크톱 전용

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| O-01 | P0 | ① 데스크톱 경로에서 전체 루틴 스모크(A→G 하이라이트) | 브라우저 경로와 동일 동작 + 게이트/알림/키체인이 살아 있음. safeStorage: 연결 코드가 키체인에만 존재(`security find-generic-password` 확인 — 화면 코드/로그에 없음, I-06과 중복 검증) |
| O-02 | P1 | ① ⌘= / ⌘- / ⌘0 | 앱 전체(사이드바·대화·상태 줄·미리보기) 배율 변화. ⌘0 초기화. **재시작 후 유지**(desktop-settings.json) |
| O-03 | P1 | ① 턴 실행 중 창 닫기(Windows 실행 시) | 닫으면 작업이 멈춘다는 확인 대화 1회 |
| O-04 | P2 | ① 설정→업데이트 `지금 확인` → 릴리스 존재 시 `지금 다시 시작해 설치` | 진행 → 앱 재시작 → `재시작합니다 — 새 버전으로 다시 열려요`. 실제 릴리스가 없으면 Block 기록(로컬 latest.json 서버로 대체 가능: `COLO_DESIGN_CLAUDE_LATEST_API` 등 검증용 env 활용) |
| O-05 | P1 | ① 프로젝트 전환 후 이전 프로젝트 페이지 복귀 | 페이지가 숨김 상태로 보존 — reload 없이 보던 자리 복귀. 페이지 상한 8장(`previewEpoch` 변경 시 돌아와서 다시 읽음 — 내부 동작은 로그로 확인) |
| O-06 | P1 | ① 패키징 경로(`pnpm --filter @colo-design/desktop pack` 후 언팩 앱) 실행 | omp가 프로바이더 목록에 **절대 노출되지 않음**(개발 전용 게이트). claude/codex만 목록 — 실사용 배포 확인. omp 선택이 남아있던 설정은 첫 쓸 수 있는 프로바이더로 이동 |

### P. 엣지 · 파괴 · 관찰

| ID | P | 단계 | 기대결과 |
|---|---|---|---|
| P-01 | P0 | ① 턴 실행 중 **앱 강제 종료** → 재실행 | M-07과 동일 기준: 2시간 내 브리프 복구, 이후 회복 패널. 화면·대화 손실 없음 |
| P-02 | P1 | ① 핀 2개 담은 상태에서 미리보기 새로 고침·화면 전환 | 핀 상태가 웹이 쥐고 살아남 — 새로고침·전환 후에도 칩·배지 유지. 전달 시점에만 정리 |
| P-03 | P1 | ① 8MB 경계 파일(8MB 정확히 / 초과 1바이트) 첨부 | 정확히 8MB는 통과, +1바이트는 거부(D-09 경계 확정) |
| P-04 | P2 | ① 재수신 초대장(qc-renew)의 `broken` base로 second-repo 전환 | 해당 base로 클론 갱신 — 베이스 브랜치 원격 기록/갈라진 커밋을 절대 덮어쓰지 않고 이름을 밝힘(로그에서 확인) |
| P-05 | P2 | ① 현재 사이클(미제출 변경 있음)인 상태에서 원격 베이스에 같은 파일 커밋(개발자가 반영) → 받아오기 트리거 | 임시 보관 → 최신화 → 얹기. 겹치지 않으면 자동 병합, 겹치면 충돌이 AI 첫 과제(게이트 카드). 열린 대화 없으면 한국어 이유가 재시도 패널에 남음 |
| P-06 | P2 | ① 대화 목록에서 이미 지워진 세션·동시 두 창(같은 대화) 열기 | 두 창 모두 정상 스트리밍, 카드 이중 없음(명령 멱속: 같은 `id` 재전송 시 한 번만 실행) |
| P-07 | P2 | ① 같은 명령(제출 등) 타임아웃 후 재전송 | 멱속 키로 두 번 실행되지 않음 — PR이 2개 열리지 않음 |
| P-08 | P2 | ① QC 라운드 종료 후 turn-stats 종합 | `firstEditMs`(첫 편집까지) 분포 — CLAUDE.md/지켜 줄 것(K-09) 유무 비교 자료로 기록. 게이트 행의 `skipped`(돌지 못한 이유) 없는지 |

---

## 3. 권장 실행 순서

라운드 1일차(콜드 스타트 — 새 QC_HOME)
1. **A-01~A-08** (온보딩·첫 초대)
2. **B-01~B-05** (first/second 준비 — B-02 말 먼저 두기 포함)
3. **D-01~D-13, D-16** (대화 핵심 — glm-5.3)
4. **E-01~E-08** (미리보기)
5. **F-01~F-07** (핀)
6. **G-01~G-05, G-08, G-10** (제출 첫 사이클)
7. **H-01~H-05** (코멘트→반영 — PR 머지)
8. **I-01~I-03** (git 검증)
9. **K-01~K-03, K-07, K-10** · **L 전체** · **M-08**
10. **O-01~O-02** (데스크톱 하이라이트)

라운드 2일차(심화 — 같은 QC_HOME 이어서)
1. **D-03(swe-2)·D-14·D-15·D-17** · **E-09~E-12** · **F-08~F-09**
2. **G-06~G-07, G-09, G-11~G-13** · **H-06~H-08** · **I-04~I-07**
3. **J-01~J-04** (게이트 — 데스크톱 필수)
4. **K-04~K-06, K-08~K-09** · **M-01~M-07** · **N 전체**
5. **P 전체** (파괴·경계)

라운드 3일차(부정 전용 — second-repo 변형 브랜치 · 잠금(Archive) · 가짜 토큰 초대장)
- **A-10(qc-invalid) · A-12~A-16** — A-11은 Block 기록 · **B-06~B-08** · **G-13(second-repo 잠금 방식)** · **M-04** · **P-04~P-05** · **O-06**
- 맨 마지막에 **M-03**(PAT 폐기 — 이후 토큰 Regenerate·초대장 재생성 필요)

의존 규칙: A-05 → B 전체 → (D·E·F) → G → H → I·J. H-04(머지)를 마친 뒤에야 H-05/K-09의 "새 사이클" 계열 확인 가능. 게이트(J)와 알림(N)은 데스크톱 경로에서만 유효.

## 4. 기록 양식

실행 결과 표 (같은 파일 또는 시트에 복사):

| ID | 실행일 | 경로(A/B) | 모델 | 판정 | 비고·증적 |
|---|---|---|---|---|---|
| D-01 | 2026-XX-XX | B | glm-5.3 | Pass | 스크린샷 q-d01.png |

버그 리포트 템플릿:

```
[QC-<라운드>-<ID>] <한 줄 요약>
경로/모델: 데스크톱 / glm-5.3 (빠르게 on)
재현 단계: 1. … 2. …
기대: <labels.ts 문장 그대로>
실제: <관찰>
증적: 스크린샷/녹화 + $QC_HOME/logs/daemon-*.log 해당 구간 + turn-stats 행
빈도: n/5
```

## 5. 부록 — 검증 커맨드 치트시트

```bash
# 클론 상태
REPO=$QC_HOME/projects/<slug>/repo
git -C $REPO branch --show-current          # colo-design/YYYYMMDD-n
git -C $REPO log --oneline -8               # 커밋 제목 = 사용자 말 첫 줄
git -C $REPO log origin/main --oneline -3   # 베이스 무결(앱 커밋 없음)

# PR 검증
gh pr list -R inkwonjung-colosseum/cds-design-first-repo --state open
gh pr view <n> -R inkwonjung-colosseum/cds-design-first-repo --json number,title,body,reviewRequests,headRefName

# 데몬 상태/로그
tail -f $QC_HOME/logs/daemon-$(date +%F).log
grep -n "colo-design/" $QC_HOME/logs/daemon-$(date +%F).log | tail

# 턴 통계 / 관찰 지도 / 사이클 원장
tail -5 $QC_HOME/logs/turn-stats-$(date +%F).jsonl | jq .
wc -l $QC_HOME/projects/<slug>/screen-map.jsonl
jq . $QC_HOME/projects/<slug>/cycle.json | head -40

# 키체인(연결 코드가 화면·로그 밖에만 있는지)
security find-generic-password -s "colo-design" -w 2>/dev/null | head -c 8   # 접두만 확인 후 파기

# preview 서버 프로세스(E-11)
lsof -nP -iTCP -sTCP:LISTEN | grep node

# omp 프로세스(M-05)
pgrep -fl omp
```
